# Dependencies
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Environment
.env
.env.local
.env.*.local

# Build
dist/
build/
*.tsbuildinfo

# Storage
storage/output/
storage/whatsapp-sessions/
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
coverage/
.nyc_output/

# Prisma
# Note: Phase 11 (v1.3) onward — migrations ARE committed so prod can apply
# them via `prisma migrate deploy`. Historical migrations applied directly via
# `db:push` are recorded in the live `_prisma_migrations` table but not in the
# repo; only forward-going migrations from Phase 11 onward live here.
