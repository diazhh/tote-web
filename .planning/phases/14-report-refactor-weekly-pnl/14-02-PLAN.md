---
phase: 14
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - backend/src/services/monitor.service.js
  - backend/src/services/accounting-report.service.js
  - backend/src/services/draw.service.js
  - backend/src/controllers/monitor.controller.js
  - backend/src/__tests__/daily-report-legacy-snapshot.test.js
  - backend/src/__tests__/daily-report-materialized.test.js
  - backend/src/__tests__/pnl-shadow-comparison.test.js
  - backend/src/__tests__/draws-getById-financial.test.js
  - backend/src/__tests__/fixtures/legacy-report-snapshot.json
  - backend/.env
autonomous: true
requirements:
  - FIN-REPORT-01
  - FIN-REPORT-02
  - FIN-REPORT-03
  - FIN-REPORT-04
user_setup: []

must_haves:
  truths:
    - "monitor.service.js#getDailyReport accepts a useMaterialized flag and dispatches to _getDailyReportMaterialized or _getDailyReportLegacy with identical input signatures"
    - "_getDailyReportLegacy contains the CURRENT body (lines 443-612) moved verbatim — byte-for-byte, no refactoring — preserving the buggy multi-draw attribution under flag=false (per assumption 7 in 14-CONTEXT)"
    - "_getDailyReportMaterialized aggregates from DrawFinancial + DrawFinancialProvider via $queryRaw, eliminating the v1.2 multi-draw attribution bug at the source (FIN-REPORT-02)"
    - "accounting-report.service.js#getAccountingReport applies the same legacy/materialized branching pattern"
    - "Both services share a single resolveApiSystemFilter(apiSystemId) helper extracted in monitor.service.js (per O4) — DRY across both branches and both services"
    - "monitor.controller.js reads process.env.REPORT_USE_MATERIALIZED === 'true' and passes useMaterialized to the service — env read lives in the controller, not the service (Inversion of Control)"
    - "draw.service.js#getDrawById returns financial + financialProviders fields (with apiSystem included) — FIN-REPORT-04 backend half via O3"
    - "Legacy snapshot test pins the current legacy response shape from a fixture captured BEFORE the refactor — any byte-level diff fails CI (P-A)"
    - "Shadow-comparison Jest test seeds a multi-draw webhook ticket (Virtuales-style: details spanning 2 draws) and asserts materialized=correct AND legacy=buggy — the bug closing is demonstrably visible"
    - "Empty-data path: getDailyReport with useMaterialized=true returns a clean zero-totals response for a date with no draws (P-C precursor for monitor service)"
    - "Endpoint response shapes for /api/monitor/reporte and /api/monitor/reporte-contable are UNCHANGED under both flag values (FIN-REPORT-03)"
    - "REPORT_USE_MATERIALIZED=true is set in backend/.env ONLY in the final task, after all preceding tests pass"
  artifacts:
    - path: "backend/src/services/monitor.service.js"
      provides: "getDailyReport branched on useMaterialized; _getDailyReportLegacy + _getDailyReportMaterialized methods; resolveApiSystemFilter shared helper"
      contains: "_getDailyReportMaterialized|_getDailyReportLegacy|resolveApiSystemFilter"
    - path: "backend/src/services/accounting-report.service.js"
      provides: "getAccountingReport branched on useMaterialized; _getAccountingReportLegacy + _getAccountingReportMaterialized methods"
      contains: "_getAccountingReportMaterialized|_getAccountingReportLegacy"
    - path: "backend/src/services/draw.service.js"
      provides: "getDrawById include financial + financialProviders relations (per O3) for FIN-REPORT-04 backend"
      contains: "financial:|financialProviders:"
    - path: "backend/src/__tests__/fixtures/legacy-report-snapshot.json"
      provides: "Frozen pre-refactor legacy response capture for the P-A regression test"
    - path: "backend/src/__tests__/pnl-shadow-comparison.test.js"
      provides: "D-06 shadow test demonstrating the multi-draw bug closes under flag=true"
      min_lines: 80
    - path: "backend/.env"
      provides: "REPORT_USE_MATERIALIZED=true set in the final task only, after tests pass"
      contains: "REPORT_USE_MATERIALIZED"
  key_links:
    - from: "monitor.controller.js"
      to: "process.env.REPORT_USE_MATERIALIZED"
      via: "Reads env var, passes useMaterialized boolean to service call"
      pattern: "REPORT_USE_MATERIALIZED"
    - from: "_getDailyReportMaterialized"
      to: "DrawFinancial + DrawFinancialProvider tables"
      via: "Single $queryRaw join — replaces the include:{tickets} aggregation at lines 497-508"
      pattern: "DrawFinancial|DrawFinancialProvider"
    - from: "draw.service.js#getDrawById"
      to: "DrawFinancial table"
      via: "Prisma include relation chain"
      pattern: "include.*financial"
    - from: "shadow-comparison test"
      to: "monitor.service.js#getDailyReport"
      via: "Calls service twice with useMaterialized=true|false and asserts both paths run side-by-side"
      pattern: "useMaterialized"
---

<objective>
Refactor `monitor.service.js#getDailyReport` and `accounting-report.service.js#getAccountingReport` so each function branches on a `useMaterialized` boolean — preserving the current legacy code path BYTE-FOR-BYTE under flag=false (per assumption #7 in 14-CONTEXT and Pitfall P-A), and adding a new materialized branch that reads from `DrawFinancial + DrawFinancialProvider` to eliminate the v1.2 multi-draw attribution bug (FIN-REPORT-02). Extend `draw.service.js#getDrawById` with `financial` + `financialProviders` includes per O3 to back the FIN-REPORT-04 financial card without adding a new route. Write four Jest tests: a frozen-fixture legacy snapshot guard (P-A), a materialized-branch correctness check (FIN-REPORT-01), a shadow-comparison test that seeds a multi-draw webhook ticket and demonstrates the bug closes only under flag=true (D-06, FIN-REPORT-02), and a getDrawById financial-include integration test (FIN-REPORT-04). Only after all tests pass does the final task enable `REPORT_USE_MATERIALIZED=true` in `backend/.env`.

Purpose: This plan delivers FIN-REPORT-01..04 — the read-side refactor that makes the materialized aggregates from Phase 11 actually drive the daily and accounting reports. The flag-gated structure preserves a one-line rollback path. The shadow test documents WHY the refactor matters: it shows the bug exists under flag=false and is gone under flag=true.

Output: Two refactored service files with both branches present, one extended draw service, three new test files plus a frozen JSON fixture, one updated controller, and `.env` flipped at the very end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-report-refactor-weekly-pnl/14-CONTEXT.md
@.planning/phases/14-report-refactor-weekly-pnl/14-RESEARCH.md
@.planning/phases/14-report-refactor-weekly-pnl/14-01-SUMMARY.md
@backend/src/services/monitor.service.js
@backend/src/services/accounting-report.service.js
@backend/src/services/draw-financial.service.js
@backend/src/services/draw.service.js
@backend/src/controllers/monitor.controller.js
@backend/prisma/schema.prisma
@./CLAUDE.md

<interfaces>
<!-- Key contract surfaces the executor needs in hand — do NOT explore the codebase for these. -->

From backend/src/services/monitor.service.js:
- Current method signature (line 443):
  `async getDailyReport({ date = null, dateFrom = null, dateTo = null, gameId = null, source = null, apiSystemId = null } = {}) { ... }`
- The body of that method spans lines 443-612.
- The BUG location is lines 497-508 (`prisma.draw.findMany` with `include: { tickets: ticketsInclude }`) — `Ticket.drawId` join, missing per-detail attribution for multi-draw webhook tickets.
- The apiSystem PULL-vs-PUSH/SCRAPE resolution lives at lines 463-485 — extract this into a shared helper.

From backend/src/services/accounting-report.service.js:
- Current method signature (line 36):
  `async getAccountingReport({ dateFrom, dateTo, gameId = null, source = null, apiSystemId = null } = {}) { ... }`
- The BUG location is lines 100-107 (mirror code).
- The apiSystem resolution mirror lives at lines 60-88.
- The `buildAccountingExcel` builder at lines 191-290 is UNCHANGED by this plan (it consumes `getAccountingReport`'s response shape).

From backend/src/services/draw.service.js:
- `getDrawById(id)` at line 92 with `include: { game: { include: { items: ... } }, preselectedItem, winnerItem, template, publications }`.
- Add to the include: `financial: true, financialProviders: { include: { apiSystem: { select: { id: true, name: true, slug: true, mode: true } } } }` (per O3 + 14-RESEARCH file table row "frontend/components/admin/DrawDetailModal.js").

From backend/src/services/draw-financial.service.js (lines 65-92):
- `computeAndUpsertSales` aggregates `SELECT SUM(td.amount) FROM "TicketDetail" td JOIN "Ticket" t ON t.id = td."ticketId" WHERE td."drawId" = ${drawId} AND t.status != 'CANCELLED'` — this is the canonical materialized totalSales.
- Read this file in full before writing the materialized branch so the column names and Decimal types match.

From backend/src/controllers/monitor.controller.js:
- `getDailyReport` controller handler — calls `monitorService.getDailyReport(query)` and returns res.json. Add ONE line: read `process.env.REPORT_USE_MATERIALIZED === 'true'` into a `useMaterialized` const and pass it to the service.
- Same for `getAccountingReport` and the Excel/PDF download handlers that delegate to those services.

From backend/prisma/schema.prisma lines 1149-1196:
- `DrawFinancial`: totalSales Decimal(12,2), totalPrize Decimal(12,2), utility Decimal(12,2), ticketCount Int, closedAt DateTime?, totalizedAt DateTime?
- `DrawFinancialProvider`: drawId, apiSystemId (NULLABLE = bucket TAQUILLA_ONLINE per D-06 in Phase 11), totalSales, totalPrize, ticketCount
- Relations: `Draw.drawFinancials` and `Draw.drawFinancialProviders` back-relations exist (verify by `grep -n "drawFinancial\|financialProvider" backend/prisma/schema.prisma` in the Draw model around lines 150-180).

From backend/src/__tests__/ existing files:
- `monitor.service.test.js` — pattern for service-level Jest tests
- `draw-financial.service.test.js` (in `src/services/__tests__/`) — pattern for Prisma-backed integration tests
- Test invocation: `NODE_OPTIONS='--experimental-vm-modules' npx jest --testPathPattern=<name>` (project uses ES modules)

Shadow test seed pattern (from 14-RESEARCH Code Examples section, lines 446-487):
- Create two draws (drawA, drawB) on the same date but different drawTime
- Create one Ticket with source='WEBHOOK_PUSH', apiSystemId=virtualesSystem.id, totalAmount=200, drawId=drawA.id (originating)
- Create two TicketDetail rows on that ticket: { gameItemId, amount: 100, drawId: drawA.id } and { gameItemId, amount: 100, drawId: drawB.id }
- Call `computeAndUpsertSales(drawA.id, drawA.closedAt)` and `computeAndUpsertSales(drawB.id, drawB.closedAt)` to populate DrawFinancial
- Assertion: under flag=true drawA.totalSales=100 and drawB.totalSales=100; under flag=false drawA.totalSales=200 and drawB.totalSales=0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture legacy snapshot fixture, extract resolveApiSystemFilter, branch monitor.service.js</name>
  <files>backend/src/services/monitor.service.js, backend/src/__tests__/fixtures/legacy-report-snapshot.json, backend/src/__tests__/daily-report-legacy-snapshot.test.js</files>
  <behavior>
    - Before any source-code change, capture the current legacy response into a fixture by invoking the existing `monitorService.getDailyReport({ dateFrom: '<a real local date with data>', dateTo: '<same date>' })` and writing the JSON to `backend/src/__tests__/fixtures/legacy-report-snapshot.json`. Pick a date that has draws AND has the v1.2 bug visible if possible (any historical date with multi-draw webhook tickets); fall back to any date with draws.
    - Legacy snapshot test calls `monitorService.getDailyReport({ ..., useMaterialized: false })` with the SAME input params used during capture and asserts deep-equal to the fixture (byte-level JSON.stringify match).
    - After refactor, `getDailyReport({ ..., useMaterialized: false })` returns response IDENTICAL to the captured snapshot (P-A regression net).
    - `getDailyReport({ ..., useMaterialized: true })` returns the same SHAPE (same top-level keys: dateFrom, dateTo, gameId, source, apiSystemId, draws, totals, byGame, bySource) — value differences only.
    - `resolveApiSystemFilter(apiSystemId)` is a module-level helper that returns `{ pushProviderFilter: boolean, drawIdsForPull: string[] | null }` and is called identically by both branches.
  </behavior>
  <read_first>
    - backend/src/services/monitor.service.js (FULL file — the entire `getDailyReport` method body lines 443-612 must be copied verbatim into `_getDailyReportLegacy`)
    - backend/src/services/draw-financial.service.js (the $queryRaw aggregation pattern at lines 65-92 — the materialized branch reuses this shape)
    - backend/prisma/schema.prisma lines 1149-1196 (DrawFinancial + DrawFinancialProvider columns)
    - .planning/phases/14-report-refactor-weekly-pnl/14-RESEARCH.md "Pattern 2: Materialized Aggregation" (lines 188-216) — the exact $queryRaw template to use
    - backend/src/__tests__/ — directory layout for tests
  </read_first>
  <action>
    Step 1 — Snapshot capture (BEFORE refactoring):
    - Write a one-off node script inline or use `jest` with a single capture test that calls the CURRENT `monitorService.getDailyReport({ dateFrom: '<chosen date>', dateTo: '<same>' })` against the local DB and writes the result to `backend/src/__tests__/fixtures/legacy-report-snapshot.json`.
    - Choose the date by querying which dates in the local mirror have the most draws: `docker exec tote_postgres psql -U tote_user -d tote_db -c "SELECT \"drawDate\", COUNT(*) FROM \"Draw\" WHERE status='DRAWN' GROUP BY 1 ORDER BY 1 DESC LIMIT 5"` — pick the most recent date with ≥10 draws.
    - The fixture file MUST include the input params at the top under a `_input` key plus the response under `_response` so the test re-runs with the same inputs deterministically.

    Step 2 — Extract `resolveApiSystemFilter` (per Open Question O4):
    - At the top of the `MonitorService` class (or as a module-level function above the class), add:
      ```javascript
      async function resolveApiSystemFilter(apiSystemId) {
        if (!apiSystemId) return { pushProviderFilter: false, drawIdsForPull: null };
        const apiSystem = await prisma.apiSystem.findUnique({ where: { id: apiSystemId }, select: { mode: true } });
        if (apiSystem?.mode === 'PUSH' || apiSystem?.mode === 'SCRAPE') {
          return { pushProviderFilter: true, drawIdsForPull: null };
        }
        const mappings = await prisma.apiDrawMapping.findMany({
          where: { apiConfig: { apiSystemId } },
          select: { drawId: true },
        });
        return { pushProviderFilter: false, drawIdsForPull: mappings.map(m => m.drawId) };
      }
      ```
    - This is the ONLY refactor of legacy code allowed — extraction without behavior change. The legacy branch reads `resolveApiSystemFilter`'s return and applies the same `where.id = { in: ... }` or `ticketsInclude.where.apiSystemId = apiSystemId` decisions inline as today.

    Step 3 — Branch `getDailyReport`:
    - Change the method signature to accept `useMaterialized = false`:
      ```javascript
      async getDailyReport({ date = null, dateFrom = null, dateTo = null, gameId = null, source = null, apiSystemId = null, useMaterialized = false } = {}) {
        if (useMaterialized) {
          return this._getDailyReportMaterialized({ date, dateFrom, dateTo, gameId, source, apiSystemId });
        }
        return this._getDailyReportLegacy({ date, dateFrom, dateTo, gameId, source, apiSystemId });
      }
      ```
    - Move the ENTIRE current method body (lines 443-612 minus the signature) into a new `_getDailyReportLegacy({ date, dateFrom, dateTo, gameId, source, apiSystemId })` method — VERBATIM, with one exception: the apiSystem PULL/PUSH resolution block (current lines 463-485) is replaced by a call to `resolveApiSystemFilter` plus the same downstream `where.id` / `ticketsInclude.where.apiSystemId` assignments. Do NOT rename variables, do NOT reformat the code, do NOT change error messages — the snapshot test will fail on any drift.

    Step 4 — Implement `_getDailyReportMaterialized`:
    - Read `draw-financial.service.js` lines 65-92 for the canonical aggregation pattern.
    - The method body uses ONE `prisma.$queryRaw` against `Draw LEFT JOIN DrawFinancial` per 14-RESEARCH Pattern 2 (lines 396-426), with `Prisma.sql` interpolation for the optional `gameId` filter.
    - Provider filter (`apiSystemId`): call `resolveApiSystemFilter` first; if `pushProviderFilter` is true, after the main query, fetch matching `DrawFinancialProvider` rows for those draws and REPLACE the per-draw `totalSales`/`totalPrize`/`ticketCount` with the provider-specific values. If `pushProviderFilter` is false and `drawIdsForPull` is non-empty, narrow the main query's `WHERE d.id IN (...)` clause.
    - Source filter (`source` param): when materialized branch is asked to filter by source (e.g., `EXTERNAL_API`), the per-ticket attribution is no longer available — fall back to the LEGACY branch in that case with a logger.warn one-liner. (Source filter is rarely combined with apiSystemId in practice; this is a documented limitation. The legacy branch handles it.)
    - Tripleta prize attribution: the materialized branch does NOT need tripleta handling because Phase 11's prize-processor upstream already attributed tripleta winners correctly when it wrote `TicketDetail.prize` and thus `DrawFinancial.totalPrize`. The tripleta bypass at current lines 510-524 is a LEGACY-ONLY concern (see 14-RESEARCH "Anti-Patterns to Avoid" point 3).
    - Response shape: identical top-level keys to legacy (`dateFrom`, `dateTo`, `gameId`, `source`, `apiSystemId`, `draws`, `totals`, `byGame`, `bySource`). The `bySource` map under materialized is built from the `DrawFinancialProvider` rows: `apiSystem.mode='PUSH'`→source='WEBHOOK_PUSH', `'SCRAPE'`→'EXTERNAL_SCRAPE', `'PULL'`→'EXTERNAL_API', `NULL apiSystemId`→'TAQUILLA_ONLINE'.
    - `COALESCE(SUM(...), 0)::numeric(12,2)` in EVERY SUM to handle empty-data per P-C.

    Step 5 — Write `daily-report-legacy-snapshot.test.js`:
    - Read the fixture: `const fixture = JSON.parse(fs.readFileSync('backend/src/__tests__/fixtures/legacy-report-snapshot.json', 'utf8'))`.
    - Call `await monitorService.getDailyReport({ ...fixture._input, useMaterialized: false })`.
    - Assert `JSON.stringify(result) === JSON.stringify(fixture._response)`. If not, dump the first 2 KB of the diff so a regression is debuggable.
    - This is the P-A regression net. CI MUST run it.

    Threat-model mitigations:
    - SQL injection ($queryRaw): all dynamic fragments use `Prisma.sql` and `${ }` template parameters — never string concatenation
    - BOLA: route-level auth gate (`authorize('ADMIN', 'OPERATOR')` at `monitor.routes.js:13`) unchanged — service-level branching does not weaken authz
    - Excel formula injection: out of scope here (Excel builder is in `accounting-report.service.js#buildAccountingExcel` and is not modified by this plan)
  </action>
  <verify>
    <automated>cd backend && NODE_OPTIONS='--experimental-vm-modules' npx jest --testPathPattern='daily-report-legacy-snapshot' --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/__tests__/fixtures/legacy-report-snapshot.json` exists with both `_input` and `_response` keys
    - `monitor.service.js` exports `resolveApiSystemFilter` (or has it as a module-level helper called by both branches)
    - `getDailyReport` signature accepts `useMaterialized = false` default
    - `_getDailyReportLegacy` method body is byte-equivalent to the current `getDailyReport` body MINUS the extracted resolveApiSystemFilter block (verified by the snapshot test passing)
    - `_getDailyReportMaterialized` method exists and uses `prisma.$queryRaw` against `DrawFinancial`
    - Legacy snapshot test passes: `JSON.stringify(result) === JSON.stringify(fixture._response)` for the captured date and inputs
  </acceptance_criteria>
  <done>
    Service is branched, helper is extracted, legacy body is verbatim, materialized body reads DrawFinancial, snapshot test passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch accounting-report.service.js + extend draw.service.js#getDrawById + update controllers</name>
  <files>backend/src/services/accounting-report.service.js, backend/src/services/draw.service.js, backend/src/controllers/monitor.controller.js, backend/src/__tests__/daily-report-materialized.test.js, backend/src/__tests__/draws-getById-financial.test.js</files>
  <behavior>
    - `getAccountingReport({ ..., useMaterialized = false })` branches identically to `getDailyReport`: legacy verbatim, materialized via DrawFinancial.
    - `_getAccountingReportLegacy` is the current body (lines 36-183) moved verbatim except the apiSystem resolution which uses the SAME shared helper from Task 1 (re-export it from `monitor.service.js` or duplicate it here — recommendation: import from `monitor.service.js` to avoid drift).
    - `_getAccountingReportMaterialized` produces rows aggregated by (date, gameId) reading from DrawFinancial, with COALESCE wrappers.
    - `getDrawById(id)` returns `draw.financial` (DrawFinancial single row or null) and `draw.financialProviders` (array with apiSystem joined via include).
    - `monitor.controller.js` reads `process.env.REPORT_USE_MATERIALIZED === 'true'` once at handler entry and passes the boolean to both service calls (`getDailyReport`, `getAccountingReport`, `getReportePdf`, `downloadAccountingExcel`).
    - daily-report-materialized test: seeds a single-provider date with known totals, calls `getDailyReport({ ..., useMaterialized: true })`, asserts totals match the seeded values.
    - draws-getById-financial test: calls `drawService.getDrawById(<a known draw id from local mirror>)` and asserts `result.financial` is a non-null object with `totalSales`, `totalPrize`, `utility`, `ticketCount` and `result.financialProviders` is an array (may be empty for older draws).
  </behavior>
  <read_first>
    - backend/src/services/accounting-report.service.js (FULL file — its body is moved verbatim into `_getAccountingReportLegacy`)
    - backend/src/services/monitor.service.js after Task 1 (to see the `resolveApiSystemFilter` export shape)
    - backend/src/services/draw.service.js lines 86-118 (current `getDrawById` body)
    - backend/src/controllers/monitor.controller.js (current handlers — find the lines calling `monitorService.getDailyReport` and `accountingReportService.getAccountingReport`)
    - backend/prisma/schema.prisma — verify `Draw.drawFinancials` / `Draw.drawFinancialProviders` back-relations exist by reading the Draw model around lines 150-180
  </read_first>
  <action>
    Step 1 — Branch `accounting-report.service.js`:
    - Modify `getAccountingReport` signature to accept `useMaterialized = false`.
    - Dispatch:
      ```javascript
      if (useMaterialized) return this._getAccountingReportMaterialized({ dateFrom, dateTo, gameId, source, apiSystemId });
      return this._getAccountingReportLegacy({ dateFrom, dateTo, gameId, source, apiSystemId });
      ```
    - `_getAccountingReportLegacy`: move the CURRENT method body verbatim. The apiSystem resolution block at current lines 60-88 is replaced with `await resolveApiSystemFilter(apiSystemId)` (imported from `monitor.service.js`) — same downstream assignments.
    - `_getAccountingReportMaterialized`: a single `prisma.$queryRaw` that joins `Draw` and `DrawFinancial` and aggregates by `(d."drawDate", d."gameId")`:
      ```sql
      SELECT to_char(d."drawDate", 'YYYY-MM-DD') AS date,
             d."gameId", g.name AS game,
             COALESCE(SUM(df."totalSales"), 0)::numeric(12,2) AS "totalSales",
             COALESCE(SUM(df."totalPrize"), 0)::numeric(12,2) AS "totalPrize",
             (COALESCE(SUM(df."totalSales"), 0) - COALESCE(SUM(df."totalPrize"), 0))::numeric(12,2) AS utility,
             COALESCE(SUM(df."ticketCount"), 0)::int AS "ticketCount"
      FROM "Draw" d
      JOIN "Game" g ON g.id = d."gameId"
      LEFT JOIN "DrawFinancial" df ON df."drawId" = d.id
      WHERE d."drawDate" BETWEEN ${...} AND ${...}
        ${gameId ? Prisma.sql`AND d."gameId" = ${gameId}` : Prisma.empty}
        ${drawIdsFilter}
      GROUP BY 1, 2, 3
      ORDER BY date ASC, game ASC
      ```
    - Apply provider filter via DrawFinancialProvider when `pushProviderFilter=true` — replace `df` join target with a CTE summing `DrawFinancialProvider` for the apiSystemId.
    - Source filter fallback: same as monitor — if `source` is set, log a warn and dispatch to legacy.
    - Response shape: identical to legacy (`dateFrom`, `dateTo`, `gameId`, `source`, `apiSystemId`, `rows`, `totals`). `rows` array shape: `{ date, gameId, game, totalSales, totalPrize, utility, ticketCount }`.

    Step 2 — Extend `draw.service.js#getDrawById`:
    - Modify the `include` to add:
      ```javascript
      financial: true,
      financialProviders: {
        include: { apiSystem: { select: { id: true, name: true, slug: true, mode: true } } }
      }
      ```
    - Confirm Prisma relation names by grepping the Draw model in `schema.prisma`. If the relation is named `drawFinancials` (plural) or `drawFinancialProviders`, use those names — adapt to the actual schema (the relation NAMES are authoritative).
    - The response shape change is additive — existing consumers (DrawDetailModal `loadDrawDetails`) tolerate extra fields.

    Step 3 — Update `monitor.controller.js`:
    - For `getDailyReport`, `getAccountingReport`, `getReportePdf` (PDF builder that calls `monitorService.getDailyReport`), and `downloadAccountingExcel` (Excel builder that calls `accountingReportService.getAccountingReport`), read `const useMaterialized = process.env.REPORT_USE_MATERIALIZED === 'true';` at the top of each handler.
    - Pass `useMaterialized` in the params object to the service call.
    - The change is ONE line at handler entry plus appending one property to the params object — surgical, no other refactor.

    Step 4 — Write `daily-report-materialized.test.js`:
    - Seed: create a Draw + Game + a Ticket with two non-cancelled TicketDetail rows (single-provider, totalAmount=100). Run `computeAndUpsertSales(draw.id, draw.closedAt)` to populate DrawFinancial.
    - Call `monitorService.getDailyReport({ dateFrom, dateTo, useMaterialized: true })` and assert `result.totals.totalSales === 100` and `result.draws[0].totalSales === 100`.
    - Clean up created rows in `afterAll`.

    Step 5 — Write `draws-getById-financial.test.js`:
    - Query a known draw from the local mirror that has a `DrawFinancial` row: `const draw = await prisma.draw.findFirst({ where: { financial: { is: null } NOT — invert: where: { drawFinancials: { some: {} } } } })` (adapt to the actual relation name).
    - Call `drawService.getDrawById(draw.id)`.
    - Assert: `result.financial` is non-null (`totalSales`, `totalPrize`, `utility`, `ticketCount` are present and numeric/decimal); `result.financialProviders` is an array; if non-empty, each element has `apiSystem` with `id`, `name`, `slug`, `mode` populated (or apiSystem is null for the TAQUILLA_ONLINE bucket per Phase 11 D-06).

    Threat-model mitigations:
    - Tampering (SQL injection): `Prisma.sql` for dynamic fragments — see Task 1
    - BOLA: getDrawById is consumed by an authenticated admin route (`draw.routes.js` mounts with `authenticate` + `authorize('ADMIN', 'OPERATOR')`); adding financial fields to the response does not change authz
    - Disclosure: financial fields are admin-only by virtue of the route guard; the new fields do not leak through any public route
  </action>
  <verify>
    <automated>cd backend && NODE_OPTIONS='--experimental-vm-modules' npx jest --testPathPattern='(daily-report-materialized|daily-report-legacy-snapshot|draws-getById-financial)' --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `accounting-report.service.js` exports a `getAccountingReport` that branches on `useMaterialized`; legacy body is byte-equivalent to current
    - `draw.service.js#getDrawById` includes `financial` and `financialProviders` (with `apiSystem` join)
    - `monitor.controller.js` reads `REPORT_USE_MATERIALIZED` env var in all four handlers that delegate to the refactored services
    - Materialized test passes against seeded data
    - getDrawById financial test passes against a real draw with DrawFinancial
    - Legacy snapshot test from Task 1 STILL passes (no regression)
  </acceptance_criteria>
  <done>
    Both services branched, getDrawById extended, controller routes env var to services, all three Jest tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shadow-comparison test (D-06) + enable REPORT_USE_MATERIALIZED flag</name>
  <files>backend/src/__tests__/pnl-shadow-comparison.test.js, backend/.env</files>
  <behavior>
    - Shadow test creates two real Draws on the same date but different drawTime (drawA earlier than drawB), in a real Game.
    - Test seeds a WEBHOOK_PUSH ticket on a real ApiSystem of mode='PUSH' (Virtuales or similar — query `await prisma.apiSystem.findFirst({ where: { mode: 'PUSH' } })`; if none exists, create one in the test's beforeAll). Ticket.drawId=drawA.id; Ticket.totalAmount=200; two TicketDetail rows: { amount: 100, drawId: drawA.id } and { amount: 100, drawId: drawB.id }.
    - Test calls `computeAndUpsertSales(drawA.id, drawA.closedAt)` and `computeAndUpsertSales(drawB.id, drawB.closedAt)` to populate DrawFinancial.
    - Test then calls `getDailyReport({ dateFrom, dateTo, useMaterialized: true })` and `getDailyReport({ dateFrom, dateTo, useMaterialized: false })` for the SAME date.
    - Assertions (per 14-RESEARCH lines 462-486):
      - MATERIALIZED: `draws.find(d => d.drawId === drawA.id).totalSales === 100` AND `draws.find(d => d.drawId === drawB.id).totalSales === 100` (correct split via TicketDetail.drawId)
      - LEGACY: `draws.find(d => d.drawId === drawA.id).totalSales === 200` AND `draws.find(d => d.drawId === drawB.id).totalSales === 0` (bug — full ticket attributed to drawA)
    - Single-provider day sanity sub-test: pick a real historical date with single-provider draws and assert `materialized.totals.totalSales === legacy.totals.totalSales` within 0.01 BsF tolerance (`toBeCloseTo(legacy, 2)`).
    - `afterAll` cleans up the seeded rows (delete tickets, details, draws, the financial rows, and the ApiSystem if created).
    - After all preceding tests in Tasks 1, 2, 3 pass, append `REPORT_USE_MATERIALIZED=true` to `backend/.env`.
  </behavior>
  <read_first>
    - backend/src/__tests__/fixtures/legacy-report-snapshot.json (verify the chosen capture date — pick the shadow-comparison single-provider sub-test date based on it)
    - backend/src/services/draw-financial.service.js (computeAndUpsertSales signature for the seeding step)
    - backend/src/queue/workers/__tests__/draw-financial-pipeline.integration.test.js (if it exists — clone its test fixture cleanup pattern; otherwise use `src/services/__tests__/draw-financial.service.test.js` as analog)
    - backend/prisma/schema.prisma (Ticket + TicketDetail + ApiSystem models for the seed shape)
    - backend/.env (current content — append, do NOT overwrite)
  </read_first>
  <action>
    Step 1 — Write `pnl-shadow-comparison.test.js`:
    - `describe('D-06 shadow comparison — getDailyReport materialized vs legacy')`
    - `beforeAll`:
      - Find or create a Game (use the first existing — `await prisma.game.findFirst()`).
      - Find or create an ApiSystem with `mode='PUSH'` (test the Virtuales row first; if not present, create one with a unique slug like `shadow-test-${Date.now()}`).
      - Create two Draws: drawA at `drawTime='09:00:00'`, drawB at `drawTime='10:00:00'`, both on a unique test date far in the past (e.g., `'1999-01-01'`) to avoid colliding with real data. Status='DRAWN', `prizesProcessed=true`, `closedAt` and `drawnAt` set to that date.
      - Create one Ticket: source='WEBHOOK_PUSH', apiSystemId=virtualesSystem.id, drawId=drawA.id, totalAmount=200, status='COMPLETED' (or whichever non-cancelled status the project uses).
      - Create two TicketDetail rows on that ticket with the per-draw split.
      - Call `computeAndUpsertSales(drawA.id, drawA.closedAt)` and `computeAndUpsertSales(drawB.id, drawB.closedAt)`.
    - **Test 1 (the bug demonstration):** call `getDailyReport({ dateFrom: '1999-01-01', dateTo: '1999-01-01', useMaterialized: true })` AND `getDailyReport({ ..., useMaterialized: false })`. Assert per the matrix in <behavior>.
    - **Test 2 (single-provider day sanity):** pick a real recent date (`fixture._input.dateFrom`) and assert the totals match between materialized and legacy within 0.01.
    - `afterAll`: delete the seeded ticket → details → financial rows → draws → (only-if-created) ApiSystem in correct FK order.

    Step 2 — Run the full test suite for refactored surface:
    - `cd backend && NODE_OPTIONS='--experimental-vm-modules' npx jest --testPathPattern='(daily-report|pnl-shadow|draws-getById)' --runInBand`
    - ALL must pass: legacy snapshot, materialized correctness, shadow comparison (both sub-tests), getDrawById financial.

    Step 3 — Enable the flag (ONLY after step 2 is fully green):
    - Append `REPORT_USE_MATERIALIZED=true` to `backend/.env`.
    - Run `cd backend && npm run dev` in a separate terminal (or rely on the operator running it manually), verify with a smoke curl that `/api/monitor/reporte?dateFrom=<recent>&dateTo=<same>` returns a 200 with a valid response shape (the SUMMARY should record the smoke output).
    - Smoke command (executable, non-fatal if backend not running — record output): `curl -s -o /tmp/reporte-smoke.json -w "%{http_code}" -H "Authorization: Bearer <token-or-skip>" http://localhost:10000/api/monitor/reporte?dateFrom=2026-05-14\&dateTo=2026-05-14 ; cat /tmp/reporte-smoke.json | head -c 500`. The smoke step is OPTIONAL — if backend is not running locally, document that and proceed.

    Threat-model mitigations:
    - Tampering: shadow test seeds only test-namespace data (unique date 1999-01-01, unique apiSystem slug if created); afterAll cleanup verified by FK order
    - Disclosure: test fixtures contain no PII; no production secrets touched
    - DoS: test runs in a single Jest worker (`--runInBand`); seeded rows are deleted in afterAll
  </action>
  <verify>
    <automated>cd backend && NODE_OPTIONS='--experimental-vm-modules' npx jest --testPathPattern='(daily-report-legacy-snapshot|daily-report-materialized|pnl-shadow-comparison|draws-getById-financial)' --runInBand && grep -E "^REPORT_USE_MATERIALIZED=true" backend/.env</automated>
  </verify>
  <acceptance_criteria>
    - `pnl-shadow-comparison.test.js` exists with both sub-tests (multi-draw bug demonstration + single-provider sanity)
    - All 4 test files pass under `--runInBand` (legacy snapshot, materialized, shadow, getDrawById)
    - The shadow test's Test 1 demonstrably FAILS if `_getDailyReportLegacy` were "fixed" — i.e., the test asserts the BUG exists under flag=false (this is intentional documentation of why the refactor matters)
    - `backend/.env` ends with a line `REPORT_USE_MATERIALIZED=true` — verified by grep
    - Test cleanup runs cleanly: no seeded `1999-01-01` rows remain after the test (verify by `SELECT COUNT(*) FROM "Draw" WHERE "drawDate"='1999-01-01'` returning 0)
  </acceptance_criteria>
  <done>
    Shadow test passes; full refactor test suite green; flag enabled in backend/.env; seeded test data cleaned up.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → API | admin-only reports endpoints; auth gate at route layer unchanged |
| Service → DB ($queryRaw) | dynamic SQL fragments must use `Prisma.sql`, not string concat |
| Env var → service | `REPORT_USE_MATERIALIZED` toggles aggregation path; read at controller entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | `$queryRaw` in `_getDailyReportMaterialized` / `_getAccountingReportMaterialized` | mitigate | All dynamic fragments use `Prisma.sql` with `${param}` interpolation per existing pattern in `draw-financial.service.js:85-94`; no string concatenation |
| T-14-02-02 | Tampering | Legacy branch regression on rollback | mitigate | P-A regression net: `daily-report-legacy-snapshot.test.js` pins JSON.stringify byte-equality against pre-refactor fixture; CI fails on any diff |
| T-14-02-03 | Information disclosure | Verbose error in P&L-adjacent response | mitigate | Existing controller try/catch returns generic 500; full stack logged via Winston (pattern from `monitor.controller.js:79-83`) |
| T-14-02-04 | Authorization | Draw financial fields exposed via `getDrawById` | mitigate | Route already gated by `authenticate` + `authorize('ADMIN', 'OPERATOR')`; additive include does not change authz |
| T-14-02-05 | Spoofing | Env var `REPORT_USE_MATERIALIZED` controls business logic path | accept | Local env; production rollout gated by ROADMAP 2-week minimum-live-data window (out of session scope) |
| T-14-02-06 | Repudiation | Flag flip mid-investigation | mitigate | Plan structure requires tests-green BEFORE flag flip in Task 3; SUMMARY records the flip timestamp explicitly |
</threat_model>

<verification>
- `cd backend && NODE_OPTIONS='--experimental-vm-modules' npx jest --testPathPattern='(daily-report-legacy-snapshot|daily-report-materialized|pnl-shadow-comparison|draws-getById-financial)' --runInBand` returns exit 0
- `grep -E "^REPORT_USE_MATERIALIZED=true" backend/.env` returns one line
- `grep -nE "_getDailyReportLegacy|_getDailyReportMaterialized" backend/src/services/monitor.service.js | grep -v '^#' | grep -cE "_getDailyReport" >= 4` (signature, dispatch, both method declarations)
- `grep -nE "_getAccountingReportLegacy|_getAccountingReportMaterialized" backend/src/services/accounting-report.service.js | grep -v '^#' | grep -cE "_getAccountingReport" >= 4`
- `grep -nE "financial: true|financialProviders:" backend/src/services/draw.service.js` returns matches
- `grep -nE "REPORT_USE_MATERIALIZED" backend/src/controllers/monitor.controller.js | grep -v '^#' | wc -l` returns ≥ 1
</verification>

<success_criteria>
- Both report services support the materialized branch; legacy branch is byte-equivalent under flag=false (P-A pinned)
- The multi-draw attribution bug is demonstrably closed under flag=true via the shadow test
- `getDrawById` returns financial + financialProviders, ready for FIN-REPORT-04 in plan 14-04
- The flag is ENABLED in `backend/.env` at end of plan
- Endpoint response shapes (`/api/monitor/reporte`, `/api/monitor/reporte-contable`) remain UNCHANGED
</success_criteria>

<output>
After completion, create `.planning/phases/14-report-refactor-weekly-pnl/14-02-SUMMARY.md` recording:
- Files modified with one-line summary each
- Test files added and their pass status
- Whether the shadow test's bug-demonstration sub-test produced the expected matrix (mat: 100/100, legacy: 200/0)
- Final `.env` flag state (must read `REPORT_USE_MATERIALIZED=true`)
- Any deviations (e.g., source-filter fallback to legacy in materialized branch)
</output>
