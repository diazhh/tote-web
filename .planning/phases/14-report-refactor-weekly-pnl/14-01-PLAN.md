---
phase: 14
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/scripts/backfill-td-drawid.mjs
  - backend/src/scripts/fix-prizes-processed.mjs
  - backend/storage/backfill-reports/.gitkeep
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Script backfill-td-drawid.mjs flips TicketDetail.drawId from NULL to Ticket.drawId for ~621,689 legacy rows (P-D mitigation) BEFORE any other write"
    - "Script fix-prizes-processed.mjs flips Draw.prizesProcessed=true for ~5,804 DRAWN draws that already have Prize rows (D-05 step 1)"
    - "Both scripts follow the Phase 11 backfill safety pattern: refuse-without-confirm, --dry-run inspection mode, before/after counts logged, exit code 0 on success"
    - "After running 14-01's three-step sequence (td.drawId backfill → prizesProcessed flip → Phase 11 backfill rerun), SELECT COUNT(*) FROM \"TicketDetail\" WHERE \"drawId\" IS NULL = 0"
    - "After 14-01, SELECT COUNT(*) FROM \"DrawFinancial\" equals SELECT COUNT(*) FROM \"Draw\" WHERE status='DRAWN' AND \"prizesProcessed\"=true"
    - "Reconciliation CSV produced by backfill-draw-financials.mjs shows zero mismatches across all materialized rows"
    - "REPORT_USE_MATERIALIZED flag is NOT enabled at the end of this plan — flag flip happens in 14-02 after tests pass"
    - "Idempotent: re-running any of the three scripts a second time is a no-op (no row changes, no duplicate writes)"
  artifacts:
    - path: "backend/src/scripts/backfill-td-drawid.mjs"
      provides: "P-D mitigation: fills TicketDetail.drawId from Ticket.drawId where NULL using a single UPDATE...FROM"
      min_lines: 80
    - path: "backend/src/scripts/fix-prizes-processed.mjs"
      provides: "D-05 step 1: flips Draw.prizesProcessed=true for DRAWN draws that have Prize rows but were never flagged"
      min_lines: 80
  key_links:
    - from: "backfill-td-drawid.mjs"
      to: "TicketDetail.drawId column"
      via: "UPDATE \"TicketDetail\" td SET \"drawId\" = t.\"drawId\" FROM \"Ticket\" t WHERE td.\"ticketId\" = t.id AND td.\"drawId\" IS NULL"
      pattern: "UPDATE \"TicketDetail\""
    - from: "fix-prizes-processed.mjs"
      to: "Draw.prizesProcessed column"
      via: "UPDATE \"Draw\" SET \"prizesProcessed\" = true WHERE status='DRAWN' AND \"prizesProcessed\"=false AND EXISTS (SELECT 1 FROM \"Prize\" p WHERE p.\"drawId\" = \"Draw\".id)"
      pattern: "UPDATE \"Draw\""
    - from: "backfill-draw-financials.mjs (Phase 11 reuse)"
      to: "DrawFinancial + DrawFinancialProvider tables"
      via: "node src/scripts/backfill-draw-financials.mjs --confirm --chunk-size=200 — idempotent UPSERT path verified in 11-04-SUMMARY"
      pattern: "computeAndUpsertSales|computeAndUpsertPrizes"
---

<objective>
Prepare the local prod-mirror database so the materialized branch (added in 14-02) returns correct totals across the full historical window (5937 DRAWN draws), not just the 133 draws that Phase 11 was able to backfill due to sparse `prizesProcessed`. This plan delivers TWO new safety-gated scripts plus a re-run of the existing Phase 11 backfill, in a specific order locked by pitfall P-D from 14-RESEARCH.md.

Purpose: D-05 of 14-CONTEXT.md mandates that Phase 14 owns the prerequisite data fix so the operator does not have to remember a pre-step. 14-RESEARCH.md uncovered (via empirical SQL on the local mirror) that 94% of TicketDetail rows have `drawId IS NULL` — re-running the Phase 11 backfill against those would write `totalSales=0` for the historical range and the D-06 shadow test in 14-02 would become a noisy false negative. This plan closes that gap BEFORE the service refactor begins.

Output: Two scripts following the Phase 11 dry-run-gated pattern, a re-run of the existing Phase 11 backfill, and an in-DB state where the 14-02 shadow test will produce meaningful comparisons. The REPORT_USE_MATERIALIZED env flag stays disabled at the end of this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-report-refactor-weekly-pnl/14-CONTEXT.md
@.planning/phases/14-report-refactor-weekly-pnl/14-RESEARCH.md
@.planning/phases/11-drawfinancial-foundation/11-04-SUMMARY.md
@backend/src/scripts/backfill-draw-financials.mjs
@./CLAUDE.md

<interfaces>
<!-- Phase 11 backfill safety pattern — copy structure verbatim into both new scripts. -->

From backend/src/scripts/backfill-draw-financials.mjs (lines 1-172):
- Header comment block documenting purpose + safeguards + invocation examples
- `argv` parsing: `--dry-run` and `--confirm` flags
- Refuse-without-confirm gate at top-level: `if (!DRY_RUN && !CONFIRM) { process.stderr.write(...refusal...); process.exit(2); }`
- `log(msg, data?)` helper using `[ISO timestamp]` prefix
- `async function main()` body
- `main().catch(err => { logger.error(...); process.exitCode = 1; }).finally(async () => { await prisma.$disconnect(); })`
- Imports: `import { prisma } from '../lib/prisma.js'; import logger from '../lib/logger.js';` (logger is DEFAULT export — see 11-04-SUMMARY deviation #1)

From backend/src/services/draw-financial.service.js (the consumer that depends on these scripts):
- `computeAndUpsertSales` aggregates `WHERE td."drawId" = ${drawId}` (no COALESCE) — confirms why P-D mitigation must run first
- `computeAndUpsertPrizes` refuses to run when `Draw.prizesProcessed=false` (throws `PrizesNotProcessedError`) — confirms why D-05 step 1 must run before the Phase 11 backfill rerun

Local DB connection (Docker container `tote_postgres`, port 5433, db `tote_db`):
- Inspect rows via `docker exec tote_postgres psql -U tote_user -d tote_db -c '<sql>'`
- The mirror currently has: 5937 DRAWN draws, 175,805 tickets, 659,509 TicketDetail rows of which 621,689 have drawId=NULL, 133 DrawFinancial rows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backfill-td-drawid.mjs script</name>
  <files>backend/src/scripts/backfill-td-drawid.mjs</files>
  <read_first>
    - backend/src/scripts/backfill-draw-financials.mjs (full file — clone its structure: argv parsing, refuse-without-confirm gate, log helper, main/finally, prisma disconnect)
    - .planning/phases/14-report-refactor-weekly-pnl/14-RESEARCH.md (Pitfall P-D section — empirical SQL evidence + recommended UPDATE statement)
    - .planning/phases/11-drawfinancial-foundation/11-04-SUMMARY.md (deviation #1: logger is DEFAULT export, not named)
  </read_first>
  <action>
    Create `backend/src/scripts/backfill-td-drawid.mjs` mirroring the Phase 11 backfill script structure. Per P-D (14-RESEARCH.md lines 320-374), this script fills `TicketDetail.drawId` from `Ticket.drawId` for all rows where `td.drawId IS NULL`. Multi-draw webhook tickets already have `td.drawId` set per-detail (Phase 8 adapter writes it explicitly), so the `WHERE td.drawId IS NULL` clause skips them safely.

    Structural requirements (clone from Phase 11 backfill):
    1. Header JSDoc-style comment block titled "Phase 14 — P-D mitigation: backfill TicketDetail.drawId" listing the safeguard rationale, invocation examples (`--dry-run` and `--confirm`), and a one-line description of what the UPDATE does.
    2. Imports: `import { prisma } from '../lib/prisma.js';` and `import logger from '../lib/logger.js';` (default import — per 11-04-SUMMARY deviation #1).
    3. `argv = process.argv.slice(2)`; parse `DRY_RUN`, `CONFIRM`. Refuse-without-confirm gate that exits 2 with the same refusal message style.
    4. `log(msg, data?)` helper identical to Phase 11 backfill.
    5. `async function main()` with the following ordered steps:
       a. Log "Phase 14 P-D — TicketDetail.drawId backfill starting — mode=${DRY_RUN ? 'DRY-RUN' : 'WRITE'}".
       b. **Before-count:** `SELECT COUNT(*) AS null_count FROM "TicketDetail" WHERE "drawId" IS NULL` via `prisma.$queryRaw`. Log the count.
       c. **Dry-run sample:** if DRY_RUN, also query `SELECT "ticketId", t."drawId" AS target_draw_id FROM "TicketDetail" td JOIN "Ticket" t ON t.id = td."ticketId" WHERE td."drawId" IS NULL LIMIT 5` and log the sample.
       d. **Early exit if DRY_RUN:** log "DRY-RUN complete — no changes written. Re-run with --confirm to apply." and return.
       e. **The UPDATE** (CONFIRM path):
          ```sql
          UPDATE "TicketDetail" td
          SET    "drawId" = t."drawId"
          FROM   "Ticket" t
          WHERE  td."ticketId" = t.id AND td."drawId" IS NULL
          ```
          Use `prisma.$executeRaw` (NOT `$queryRaw` — this is a write). Capture the returned affected-row count.
       f. **After-count:** `SELECT COUNT(*) AS null_count FROM "TicketDetail" WHERE "drawId" IS NULL`. Log the result.
       g. **Idempotency assertion:** assert `after_null_count === 0`; if not, exit non-zero with an error message naming the residual count. (Residual NULLs would mean a Ticket with a NULL drawId itself, which should not exist — but defensively log and refuse if so.)
       h. Log "Backfill complete: updated=${affected_rows}, null_count_after=${after_null_count}".
    6. `main().catch(...).finally(async () => { await prisma.$disconnect(); })` block identical to Phase 11 backfill.

    Threat-model mitigations:
    - SQL injection (Tampering): the UPDATE uses Prisma `$executeRaw` with no user-controlled interpolation — the SQL string is fully static. No `${argv}` reaches the query body.
    - Info disclosure (Disclosure): logs print counts only, never ticket IDs in bulk. The dry-run sample is capped at 5 rows.
    - Privilege escalation (E): script requires CLI execution as the local DB user; no API surface.

    Do NOT touch the deferred ideas (no provider-specific filter, no per-game stats, no audit row writes — keep the script single-purpose).
  </action>
  <verify>
    <automated>node --check backend/src/scripts/backfill-td-drawid.mjs && node backend/src/scripts/backfill-td-drawid.mjs 2>&1 | grep -q "Refusing to write without --confirm"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check backend/src/scripts/backfill-td-drawid.mjs` exits 0
    - Running the script with no flags exits 2 and prints the refusal message
    - Running with `--dry-run` exits 0, prints null_count before, prints up to 5 sample rows, writes nothing (verifiable: null_count unchanged after dry-run)
    - Running with `--confirm` updates exactly the NULL-drawId rows; after-count is 0
    - Header comment names "Phase 14" and references P-D explicitly
  </acceptance_criteria>
  <done>
    Script exists, refuses without --confirm, dry-run inspects without writing, --confirm fills all NULL TicketDetail.drawId rows, after-count is 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create fix-prizes-processed.mjs script</name>
  <files>backend/src/scripts/fix-prizes-processed.mjs</files>
  <read_first>
    - backend/src/scripts/backfill-td-drawid.mjs (just created — copy its structure)
    - .planning/phases/14-report-refactor-weekly-pnl/14-CONTEXT.md (D-05 — the retroactive prizesProcessed fix specification)
    - .planning/phases/11-drawfinancial-foundation/11-04-SUMMARY.md ("Data hygiene surfaced: prizesProcessed flag is sparse" — the root finding this script addresses)
    - backend/src/queue/workers/step-process-prizes.worker.js (READ-ONLY — confirm pre-check at top that skips if prizesProcessed=true; this confirms the flip is safe and does NOT retrigger prize processing per 14-RESEARCH source #3 secondary)
  </read_first>
  <action>
    Create `backend/src/scripts/fix-prizes-processed.mjs` cloning the Task 1 script's structural template (argv parsing, refuse-without-confirm, log helper, main/finally). This implements D-05 step 1 from 14-CONTEXT.md.

    Header comment titled "Phase 14 — D-05 step 1: retroactive prizesProcessed flip for historical DRAWN draws".

    `async function main()` steps:
    1. Log "fix-prizes-processed starting — mode=${DRY_RUN ? 'DRY-RUN' : 'WRITE'}".
    2. **Before-count:** `SELECT COUNT(*) FROM "Draw" WHERE status='DRAWN' AND "prizesProcessed"=false AND EXISTS (SELECT 1 FROM "Prize" p WHERE p."drawId" = "Draw".id)`. Log it (expected ~5804 per 14-RESEARCH).
    3. **Dry-run sample:** if DRY_RUN, also `SELECT id, "drawDate", "drawTime" FROM "Draw" WHERE status='DRAWN' AND "prizesProcessed"=false AND EXISTS (SELECT 1 FROM "Prize" p WHERE p."drawId" = "Draw".id) ORDER BY "drawDate" DESC LIMIT 5` and log the sample.
    4. Early exit if DRY_RUN.
    5. **The UPDATE** (CONFIRM path) via `prisma.$executeRaw`:
       ```sql
       UPDATE "Draw"
       SET    "prizesProcessed" = true
       WHERE  status='DRAWN'
         AND  "prizesProcessed"=false
         AND  EXISTS (SELECT 1 FROM "Prize" p WHERE p."drawId" = "Draw".id)
       ```
       Capture affected-row count.
    6. **After-count:** repeat the SELECT — must equal 0.
    7. **Sanity:** also query `SELECT COUNT(*) FROM "Draw" WHERE status='DRAWN' AND "prizesProcessed"=true` AND log the result so the operator can compare it to the DrawFinancial count after the Phase 11 backfill rerun (D-05 step 3 sanity check).
    8. Log "Flip complete: updated=${affected}, draws_with_prizesProcessed_true_total=${total}".

    Defensive notes in the header comment:
    - Worker `step-process-prizes.worker.js` pre-checks the flag and SKIPS if already true — flipping it on draws that already have Prize rows will NOT cause prize reprocessing (verified in 14-RESEARCH secondary source #3).
    - Worker `calculate-draw-financials.worker.js` reads the flag only on its own invocation — flipping it does NOT spontaneously trigger that worker either.

    Threat-model mitigations (same shape as Task 1): static SQL, no user input, log counts only.
  </action>
  <verify>
    <automated>node --check backend/src/scripts/fix-prizes-processed.mjs && node backend/src/scripts/fix-prizes-processed.mjs 2>&1 | grep -q "Refusing to write without --confirm"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check backend/src/scripts/fix-prizes-processed.mjs` exits 0
    - No-flag run exits 2 with refusal message
    - `--dry-run` prints before-count, sample (≤5 rows), writes nothing, exits 0
    - `--confirm` flips the flag; after-count is 0
    - Header comment names "Phase 14" and "D-05" explicitly
  </acceptance_criteria>
  <done>
    Script exists, refuses without --confirm, dry-run inspects without writing, --confirm flips all qualifying Draw rows and after-count is 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Execute the three-step data preparation sequence and verify reconciliation</name>
  <files>backend/storage/backfill-reports/.gitkeep</files>
  <read_first>
    - backend/src/scripts/backfill-draw-financials.mjs (Phase 11 — reused unmodified; verify --confirm + --chunk-size=200 flags work as designed)
    - backend/storage/backfill-reports/ (verify the directory exists from Phase 11 — if missing, create the .gitkeep)
    - .planning/phases/14-report-refactor-weekly-pnl/14-CONTEXT.md (D-05 — the four-step sequence)
    - .planning/phases/14-report-refactor-weekly-pnl/14-RESEARCH.md (P-D "Order of operations for Plan 14-01" — six steps including `prisma generate`)
  </read_first>
  <action>
    Execute the data preparation sequence end-to-end against the local prod-mirror DB. This task is the operational closure of the plan — it runs the scripts and verifies the outcome.

    Ordered steps (capture output to console; capture key counts in the SUMMARY):

    1. `cd backend && npm run db:generate` — defensive Prisma client regeneration per 14-RESEARCH "Runtime State Inventory" (Phase 11 backfill failed initially because the client was stale; do not skip this).
    2. Ensure `backend/storage/backfill-reports/.gitkeep` exists (Phase 11 created it — re-create if missing so the reconciliation CSV has a destination directory).
    3. `cd backend && node src/scripts/backfill-td-drawid.mjs --dry-run` — capture and log the before-count (expected ~621,689) and the 5-row sample. Confirm no writes happened.
    4. `cd backend && node src/scripts/backfill-td-drawid.mjs --confirm` — capture affected-row count; confirm after-count is 0.
    5. `cd backend && node src/scripts/fix-prizes-processed.mjs --dry-run` — capture before-count (expected ~5804) and 5-row sample.
    6. `cd backend && node src/scripts/fix-prizes-processed.mjs --confirm` — capture affected-row count; confirm after-count is 0; record the "draws with prizesProcessed=true total" for cross-check.
    7. `cd backend && node src/scripts/backfill-draw-financials.mjs --dry-run` — capture the remaining-draws count (expected ~5937 - 133 = 5804 NEW + 133 idempotent upsert, but D-01 LEFT JOIN `WHERE df.totalizedAt IS NULL` may exclude the 133 already-totalized; verify by reading the log line "Remaining draws to backfill: N").
    8. `cd backend && node src/scripts/backfill-draw-financials.mjs --confirm --chunk-size=200` — run the full Phase 11 backfill. Capture the reconciliation CSV path printed at the end.
    9. **Sanity check** via direct DB query:
       ```sql
       SELECT COUNT(*) FROM "DrawFinancial";
       SELECT COUNT(*) FROM "Draw" WHERE status='DRAWN' AND "prizesProcessed"=true;
       -- These two MUST be equal.
       ```
       Use `docker exec tote_postgres psql -U tote_user -d tote_db -c '<sql>'`.
    10. **P-D regression check:**
        ```sql
        SELECT COUNT(*) FROM "DrawFinancial" WHERE "ticketCount" > 0 AND "totalSales" = 0;
        -- MUST be 0 (per 14-RESEARCH "Warning signs"). Any positive count indicates P-D was not fully mitigated.
        ```
    11. Inspect the reconciliation CSV: `tail -20 backend/storage/backfill-reports/draw-financial-recon-*.csv | head -20 ; awk -F, 'NR>1 && $4!=0 {c++} END {print "mismatches:", c+0}' backend/storage/backfill-reports/draw-financial-recon-*.csv` — expected `mismatches: 0`.

    All counts and the reconciliation outcome are recorded verbatim in the SUMMARY produced at end-of-plan. The REPORT_USE_MATERIALIZED env flag stays UNCHANGED (still absent from .env, defaults to legacy path). Flag flip is deferred to plan 14-02 last task per quality_gate.

    Do NOT push anything to ssh 94, do NOT run pm2 commands, do NOT git push. Local execution only.

    Threat-model mitigations:
    - Tampering: scripts use static SQL with no user input (verified by Tasks 1 and 2 acceptance criteria).
    - Info disclosure: counts are logged but no ticket-level data leaks to stdout in bulk.
    - Repudiation: each script logs an ISO timestamp prefix for every line; SUMMARY records every count for audit replay.
  </action>
  <verify>
    <automated>test -f backend/storage/backfill-reports/.gitkeep && docker exec tote_postgres psql -U tote_user -d tote_db -t -c "SELECT (SELECT COUNT(*) FROM \"DrawFinancial\") = (SELECT COUNT(*) FROM \"Draw\" WHERE status='DRAWN' AND \"prizesProcessed\"=true) AS counts_match" | grep -q "t"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:generate` exits 0
    - After Task 3 step 4: `SELECT COUNT(*) FROM "TicketDetail" WHERE "drawId" IS NULL` returns 0
    - After Task 3 step 6: no DRAWN draw with Prize rows has prizesProcessed=false
    - After Task 3 step 8: `SELECT COUNT(*) FROM "DrawFinancial"` equals `SELECT COUNT(*) FROM "Draw" WHERE status='DRAWN' AND "prizesProcessed"=true`
    - P-D regression check: zero `DrawFinancial` rows have `ticketCount > 0 AND totalSales = 0`
    - Reconciliation CSV produced by backfill-draw-financials.mjs has zero mismatches (awk diff column count = 0)
    - Backend `.env` file is UNCHANGED — REPORT_USE_MATERIALIZED is NOT enabled at end of plan
    - SUMMARY records: TicketDetail before/after null counts, Draw before/after prizesProcessed counts, DrawFinancial row count, reconciliation CSV path and mismatch count, and a "Flag status: NOT YET ENABLED" line
  </acceptance_criteria>
  <done>
    All counts match; recon CSV shows zero mismatches; P-D regression check passes; flag remains disabled; SUMMARY records every count.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Database | scripts run as the local DB user against the prod-mirror DB; no network surface |
| Filesystem → reconciliation CSV | output path is constructed from `process.cwd() + fixed string + ISO timestamp`; no untrusted input enters the path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | UPDATE statements in both new scripts | mitigate | SQL is fully static — no `${argv}` interpolation into query body; `prisma.$executeRaw` template literal is parameterless |
| T-14-01-02 | Information disclosure | Script stdout when run by operator | mitigate | Counts only; dry-run sample capped at 5 rows; no ticket-level data dumped in bulk |
| T-14-01-03 | Repudiation | Operator runs script with wrong flag accidentally | mitigate | refuse-without-confirm exit-2 gate per Phase 11 D-02; ISO-timestamp prefix on every log line |
| T-14-01-04 | Denial of Service | UPDATE on 621k TicketDetail rows holds a lock | accept | local-only execution outside live pipeline; single transaction acceptable for one-shot operation; ~30s on local mirror per Phase 11 backfill scale |
| T-14-01-05 | Elevation of privilege | Script invoked from elsewhere in app code | accept | scripts live under `src/scripts/`; not imported by services; CLI-only entry via `node` |
</threat_model>

<verification>
End-of-plan checks (all must pass):
- `node --check` exits 0 for both new scripts
- Refuse-without-confirm exits 2 for both new scripts
- `SELECT COUNT(*) FROM "TicketDetail" WHERE "drawId" IS NULL` returns 0
- `SELECT COUNT(*) FROM "Draw" WHERE status='DRAWN' AND "prizesProcessed"=false AND EXISTS (SELECT 1 FROM "Prize" p WHERE p."drawId"="Draw".id)` returns 0
- `SELECT COUNT(*) FROM "DrawFinancial"` equals `SELECT COUNT(*) FROM "Draw" WHERE status='DRAWN' AND "prizesProcessed"=true`
- `SELECT COUNT(*) FROM "DrawFinancial" WHERE "ticketCount" > 0 AND "totalSales" = 0` returns 0
- Reconciliation CSV exists at `backend/storage/backfill-reports/draw-financial-recon-*.csv` with zero mismatches
- `grep -E "^REPORT_USE_MATERIALIZED" backend/.env` returns nothing OR a value of `false` (flag is NOT yet enabled)
</verification>

<success_criteria>
The local prod-mirror DB is in the state required for the materialized branch added in 14-02 to return correct totals across the full historical window. Specifically:
- TicketDetail.drawId is populated for every row
- Every DRAWN draw with Prize rows has prizesProcessed=true
- DrawFinancial has one row per DRAWN+prizesProcessed draw
- Reconciliation CSV proves materialized totals match live SUM(TicketDetail.amount) for every row
- The REPORT_USE_MATERIALIZED env flag remains DISABLED — services still use the legacy path
</success_criteria>

<output>
After completion, create `.planning/phases/14-report-refactor-weekly-pnl/14-01-SUMMARY.md` recording:
- Two new script paths and their roles
- Before-count and after-count for each of the three data fixes (TicketDetail.drawId null, Draw.prizesProcessed false, DrawFinancial row count)
- Reconciliation CSV path and mismatch count (must be 0)
- P-D regression check result
- Explicit note: "REPORT_USE_MATERIALIZED flag remains disabled — flag flip is deferred to plan 14-02 last task"
</output>
