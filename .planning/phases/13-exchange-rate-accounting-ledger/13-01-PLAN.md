---
phase: 13
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/{timestamp}_phase13_accounting/migration.sql
autonomous: true
requirements:
  - FIN-RATE-01
  - FIN-RATE-02
  - FIN-RATE-03
  - FIN-LEDGER-01
  - FIN-LEDGER-06
  - FIN-LEDGER-07
user_setup: []

must_haves:
  truths:
    - "multer@^2.1.1 and file-type@^19 declared in backend/package.json dependencies (alphabetical)"
    - "package-lock.json reflects both new deps and is committed"
    - "Prisma schema declares ExchangeRate, Category, AccountingEntry, AccountingEntryAttachment models with Decimal(18,8) precision"
    - "AccountingEntry self-relation 'EntryReversal' (reversesId @unique + reversedById @unique) validates under prisma format"
    - "AccountingEntry.settlementId is an optional FK to ProviderWeeklySettlement (Phase 12 schema target — P-3 prerequisite check passes before migration runs)"
    - "Three new enums (AccountingEntryType, ExchangeRateType, AccountingCurrency) exist in schema"
    - "User model carries back-relations for createdById on rates, categories, entries, attachments"
    - "ProviderWeeklySettlement carries back-relation payments AccountingEntry[]"
    - "Category seed rows exist for INCOME (Premios cobrados, Otros ingresos), EXPENSE (Sueldos, Internet, Alquiler, Hosting, Servicios), PAYMENT (Comisiones proveedor, Premios pagados) with isActive=true"
    - "Local migration applies cleanly against tote_db on port 5433 and `prisma generate` succeeds"
    - "Runtime smoke test confirms prisma.exchangeRate / prisma.accountingEntry / prisma.category / prisma.accountingEntryAttachment are typeof 'object'"
  artifacts:
    - path: "backend/package.json"
      provides: "multer + file-type dependency declarations"
      contains: "multer"
    - path: "backend/prisma/schema.prisma"
      provides: "ExchangeRate, Category, AccountingEntry, AccountingEntryAttachment models + 3 enums + back-relations on User and ProviderWeeklySettlement"
      contains: "model AccountingEntry"
    - path: "backend/prisma/migrations/{timestamp}_phase13_accounting/migration.sql"
      provides: "Committed SQL migration creating 4 tables, 3 enums, indexes, FKs, and category seeds"
      contains: "CREATE TABLE \"AccountingEntry\""
  key_links:
    - from: "AccountingEntry.settlementId (nullable)"
      to: "ProviderWeeklySettlement.id"
      via: "Optional Prisma relation declared in Phase 13"
      pattern: "settlement\\s+ProviderWeeklySettlement\\?"
    - from: "AccountingEntryAttachment.entryId"
      to: "AccountingEntry.id"
      via: "Prisma relation with onDelete Cascade"
      pattern: "onDelete:\\s*Cascade"
    - from: "AccountingEntry.reverses / .reversedBy"
      to: "AccountingEntry.id (self)"
      via: "Named self-relation 'EntryReversal' with reversesId @unique"
      pattern: "@relation\\(\"EntryReversal\""
    - from: "AccountingEntry.exchangeRateId (nullable)"
      to: "ExchangeRate.id"
      via: "Optional Prisma relation — locked at create per F-7"
      pattern: "exchangeRate\\s+ExchangeRate\\?"
---

<objective>
Land the persistence foundation for Phase 13: install multer + file-type, add 4 Prisma models (ExchangeRate, Category, AccountingEntry, AccountingEntryAttachment), 3 enums, back-relations on User and ProviderWeeklySettlement, and apply the migration locally against the prod-mirror DB. This is the [BLOCKING] schema step that 13-02..04 build on.

Purpose: All downstream Phase 13 work (services, controllers, routes, UI) depends on these tables existing in the live local DB and on Prisma client accessors being generated. The Decimal(18,8) precision, the named self-relation for reversal, the optional FK to Phase 12's ProviderWeeklySettlement, and the seeded categories are load-bearing for plans 13-02 (services) through 13-04 (UI).

Output: schema.prisma additions, generated + committed migration SQL with category seed rows, multer + file-type in package.json/package-lock.json, working `prisma generate` artifact, smoke-test confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-CONTEXT.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-RESEARCH.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md
@.planning/phases/12-provider-commission-engine/12-CONTEXT.md
@.planning/phases/11-drawfinancial-foundation/11-01-SUMMARY.md
@./CLAUDE.md

<interfaces>
Existing schema anchors (from backend/prisma/schema.prisma):

- `model User` (around line 325) — already accumulates `auditLogs AuditLog[]` and other back-relations; this plan ADDS `exchangeRatesCreated`, `categoriesCreated`, `accountingEntries`, `attachmentsUploaded` to it.
- `model AuditLog` (line 404) — REUSE; do NOT add a new audit table. The plan does not modify this model; later plans write rows.
- `model ProviderWeeklySettlement` (Phase 12 model — landed before this phase in the batch) — this plan ADDS a single back-relation `payments AccountingEntry[]`. P-3 prerequisite verification asserts the table exists in the live DB BEFORE the migration is applied.
- `model Game` (around line 23) — pattern for `isActive Boolean @default(true)` soft-delete convention used by Category.
- Phase 11 `model DrawFinancial` (line 1149) — pattern for `Decimal @db.Decimal(18, 8)` precision (note Phase 13 uses (18,8), matching Phase 12, not Phase 11's (12,2)).
- Phase 12 `model ProviderCommissionConfig` — append-only/immutable convention mirrored by ExchangeRate (no UPDATE endpoint at the application layer per FIN-RATE-02).

Three enums to add (mirror `ApiSystemMode` style at schema.prisma):
- `AccountingEntryType` { INCOME, EXPENSE, PAYMENT }
- `ExchangeRateType` { BCV, PARALELO, OTRO }
- `AccountingCurrency` { BsF, USD }
</interfaces>

@backend/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install multer + file-type backend deps</name>
  <read_first>
    - backend/package.json (verify "type": "module" at line 6 — confirms file-type ESM compatibility)
    - .planning/phases/13-exchange-rate-accounting-ledger/13-RESEARCH.md "Standard Stack" section + Common Pitfalls P-2 (ESM-only)
    - .planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md section 14 "backend/package.json — MODIFIED"
    - Analog: .planning/phases/11-drawfinancial-foundation/11-01-SUMMARY.md — same alphabetical-insertion convention used when decimal.js was added
  </read_first>
  <action>
  Run `cd backend && npm install --save multer@^2.1.1 file-type@^19` (pin file-type to ^19 because v21 requires Node ≥22 and prod runs Node 20.20.2 per RESEARCH A1). Before installing, run `npm view multer version` and `npm view file-type@^19 main version engines.node` to confirm latest in range. After install, verify alphabetical placement of both new keys in `backend/package.json` "dependencies". Run `node --input-type=module -e 'import { fileTypeFromBuffer } from "file-type"; console.log(typeof fileTypeFromBuffer)'` to confirm the ESM import resolves (must print `function`). Commit `backend/package.json` AND `backend/package-lock.json` together (per project convention — Phase 11 lesson). Do NOT use multer.diskStorage anywhere; the controller layer in 13-03 will use memoryStorage so file-type byte inspection runs BEFORE fs.writeFile (RESEARCH "Anti-Patterns" + F-14).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -E '"(multer|file-type)":' package.json | wc -l | grep -q '^[[:space:]]*2$' &amp;&amp; node --input-type=module -e 'import { fileTypeFromBuffer } from "file-type"; if (typeof fileTypeFromBuffer !== "function") process.exit(1)'</automated>
  </verify>
  <done>
  - `backend/package.json` lists `"multer": "^2.1.1"` and `"file-type": "^19..."` in alphabetical order
  - `backend/package-lock.json` updated and staged
  - ESM smoke import returns `function`
  - No other dependency keys changed
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 4 Prisma models + 3 enums + back-relations to schema.prisma</name>
  <read_first>
    - backend/prisma/schema.prisma — model User (~line 325), model AuditLog (~line 404), model Game (~line 23), and Phase 12 model ProviderWeeklySettlement
    - .planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md section 1 (Prisma schema additions, lines 46-174) — exact field shapes, index list, named self-relation idiom
    - .planning/phases/13-exchange-rate-accounting-ledger/13-CONTEXT.md D-01 through D-07 (locked decisions)
    - .planning/phases/13-exchange-rate-accounting-ledger/13-RESEARCH.md "Code Examples — Prisma schema additions" (lines 554-649)
    - Analog: Phase 12 model ProviderCommissionConfig (append-only convention) and Phase 11 model DrawFinancialProvider (optional FK + Decimal(18,8) precision in Phase 12, NOT (12,2))
  </read_first>
  <action>
  Append the following to `backend/prisma/schema.prisma` AFTER Phase 12's ProviderWeeklySettlement block (do not interleave inside other model blocks):

  Three new enums per D-02/D-04/F-8: `AccountingEntryType { INCOME EXPENSE PAYMENT }`, `ExchangeRateType { BCV PARALELO OTRO }`, `AccountingCurrency { BsF USD }`.

  `ExchangeRate` model: `id String @id @default(uuid())`, `date DateTime @db.Date` (NOT @unique — D-01 allows multiple rateType rows per date), `rateBsPerUsd Decimal @db.Decimal(18, 8)` (F-4), `rateType ExchangeRateType` (F-8 from day one), `notes String?`, `createdById String`, `createdAt DateTime @default(now())`, relations `createdBy User @relation(fields: [createdById], references: [id])` and `accountingEntries AccountingEntry[]`, indexes `@@index([date, createdAt(sort: Desc)])` (D-01 lookup) and `@@index([rateType, date])`.

  `Category` model: `id String @id @default(uuid())`, `name String`, `appliesTo AccountingEntryType`, `isActive Boolean @default(true)` (D-02 soft-delete), `createdById String`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`, relations `createdBy User` and `entries AccountingEntry[]`, `@@unique([appliesTo, name])`, `@@index([appliesTo, isActive])`.

  `AccountingEntry` model: `id String @id @default(uuid())`, `type AccountingEntryType`, `entryDate DateTime @db.Date` (pure date, no time — assumption A2), `categoryId String`, `amountBsF Decimal @db.Decimal(18, 8)` (IMMUTABLE per FIN-LEDGER-09 — enforced at service layer), `originalAmount Decimal? @db.Decimal(18, 8)`, `originalCurrency AccountingCurrency`, `exchangeRateId String?` (LOCKED at create — F-7), `description String` (EDITABLE per FIN-LEDGER-09), `createdById String`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`. Reversal triple (D-06): `reversesId String? @unique`, `reversedById String? @unique`, `reversalReason String?`. PAYMENT FK (D-03): `settlementId String?`. Optional `sequentialNo Int @default(autoincrement()) @unique` per researcher Open Question O1 pre-decision. Relations: `category Category @relation(fields: [categoryId], references: [id])`, `exchangeRate ExchangeRate? @relation(fields: [exchangeRateId], references: [id])`, `createdBy User @relation(fields: [createdById], references: [id])`, `settlement ProviderWeeklySettlement? @relation(fields: [settlementId], references: [id])`, `attachments AccountingEntryAttachment[]`, plus the named self-relation pair `reverses AccountingEntry? @relation("EntryReversal", fields: [reversesId], references: [id])` and `reversedBy AccountingEntry? @relation("EntryReversal")`. Indexes: `@@index([entryDate, type])`, `@@index([categoryId, entryDate])`, `@@index([settlementId])`, `@@index([type, entryDate])`.

  `AccountingEntryAttachment` model: `id String @id @default(uuid())`, `entryId String`, `filename String` (UUID-based — set in 13-03), `originalName String`, `mimeType String` (server-validated — never trust client per F-14), `sizeBytes Int`, `uploadedById String`, `uploadedAt DateTime @default(now())`, relations `entry AccountingEntry @relation(fields: [entryId], references: [id], onDelete: Cascade)` (D-04 defensive — onDelete: Cascade) and `uploadedBy User @relation(fields: [uploadedById], references: [id])`, `@@index([entryId])`.

  Back-relations to ADD inside existing models:
  - User: `exchangeRatesCreated ExchangeRate[]`, `categoriesCreated Category[]`, `accountingEntries AccountingEntry[]`, `attachmentsUploaded AccountingEntryAttachment[]`.
  - ProviderWeeklySettlement: `payments AccountingEntry[]`.

  Run `npx prisma format` then `npx prisma validate` from `backend/`. If the named self-relation fails validation, the canonical Prisma idiom is the exact form above (one named relation `"EntryReversal"`, fields/references on the owning side, the back-reference is fields-less). Do NOT use the alternative `@relation("EntryReversal_reverse_fk")` shape suggested as one option in the patterns file — it is documented there as "VERIFY" and is non-canonical.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx prisma format &amp;&amp; npx prisma validate &amp;&amp; grep -v '^//' prisma/schema.prisma | grep -c 'model \(ExchangeRate\|Category\|AccountingEntry\|AccountingEntryAttachment\) {' | grep -q '^4$' &amp;&amp; grep -v '^//' prisma/schema.prisma | grep -c 'enum \(AccountingEntryType\|ExchangeRateType\|AccountingCurrency\) {' | grep -q '^3$' &amp;&amp; grep -q 'payments\s\+AccountingEntry\[\]' prisma/schema.prisma &amp;&amp; grep -q '@relation("EntryReversal"' prisma/schema.prisma</automated>
  </verify>
  <done>
  - `npx prisma validate` exits 0
  - 4 new models present, 3 new enums present
  - Named self-relation `"EntryReversal"` present on AccountingEntry
  - ProviderWeeklySettlement carries `payments AccountingEntry[]` back-relation
  - User carries the 4 new back-relations
  - No existing model fields modified (additive change only on User + ProviderWeeklySettlement)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [BLOCKING] Generate + apply Phase 13 migration locally with P-3 prerequisite check</name>
  <read_first>
    - .planning/phases/11-drawfinancial-foundation/11-01-SUMMARY.md — proven prisma migrate diff + docker exec psql approach for local prod-mirror migration
    - .planning/phases/13-exchange-rate-accounting-ledger/13-RESEARCH.md Common Pitfalls P-10 (P-3 prerequisite check — ProviderWeeklySettlement must exist BEFORE migration applies)
    - .planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md sections 1 + 14 + index/Runtime State Inventory
    - ./CLAUDE.md "Base de Datos — Local (Docker)" section (port 5433, user tote_user, db tote_db, password tote_password_2025)
    - backend/prisma/schema.prisma (Task 2 output)
  </read_first>
  <behavior>
    - Pre-check: query the LIVE local DB and assert `ProviderWeeklySettlement` table exists. If absent, abort with explicit message; do not run the migration.
    - Generate `migration.sql` via `prisma migrate diff --from-schema-datasource ... --to-schema-datamodel ... --script` — same approach Phase 11 used.
    - Migration SQL contains: 3 CREATE TYPE statements (enums), 4 CREATE TABLE statements, all `@@index` and FK constraints, and a final INSERT block seeding initial Category rows.
    - Category seeds inserted: appliesTo=EXPENSE → Sueldos, Internet, Alquiler, Hosting, Servicios; appliesTo=INCOME → Premios cobrados, Otros ingresos; appliesTo=PAYMENT → Comisiones proveedor, Premios pagados. All `isActive=true`, `createdById` is a placeholder user id resolved at migration time via subquery `(SELECT id FROM "User" WHERE role = 'ADMIN' ORDER BY "createdAt" ASC LIMIT 1)` so the seed never fails on a fresh DB with any admin.
    - Migration applied to live DB via `docker exec -i tote_postgres psql -U tote_user -d tote_db < migration.sql`.
    - `_prisma_migrations` row inserted manually with the migration name and rolled-up checksum.
    - `npx prisma generate` runs successfully — Phase 11/12 lesson: without it, model accessors are undefined.
    - Runtime smoke test: `node --input-type=module -e 'import { prisma } from "./src/lib/prisma.js"; console.log(typeof prisma.exchangeRate, typeof prisma.accountingEntry, typeof prisma.category, typeof prisma.accountingEntryAttachment)'` prints `object object object object`.
    - Direct SQL verification: `SELECT COUNT(*) FROM "Category" WHERE "appliesTo"='EXPENSE'` returns 5 (or greater if seeds already existed — script must be idempotent via `INSERT ... ON CONFLICT ("appliesTo","name") DO NOTHING`).
  </behavior>
  <action>
  Step A (P-3 prerequisite — runs FIRST, blocking): Execute `docker exec tote_postgres psql -U tote_user -d tote_db -tAc "SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'ProviderWeeklySettlement')"`. If output is not `t`, abort the task with message "Phase 12 schema not landed yet — ProviderWeeklySettlement missing. Run Phase 12-01 first." Do NOT continue.

  Step B: From `backend/`, create the migration directory `prisma/migrations/{timestamp}_phase13_accounting/` (timestamp in `YYYYMMDDHHMMSS` format) and generate the SQL using:
  `npx prisma migrate diff --from-url "$DATABASE_URL" --to-schema-datamodel prisma/schema.prisma --script > prisma/migrations/{timestamp}_phase13_accounting/migration.sql`

  Step C: Append idempotent category seed INSERTs to the bottom of `migration.sql`. The seeds use `ON CONFLICT ("appliesTo","name") DO NOTHING` so re-runs are safe. Each row resolves `createdById` via the subquery above. Final seed count: 5 EXPENSE + 2 INCOME + 2 PAYMENT = 9 rows.

  Step D: Apply via `docker exec -i tote_postgres psql -U tote_user -d tote_db < prisma/migrations/{timestamp}_phase13_accounting/migration.sql`.

  Step E: Insert `_prisma_migrations` row manually:
  `docker exec tote_postgres psql -U tote_user -d tote_db -c "INSERT INTO \"_prisma_migrations\" (id, checksum, finished_at, migration_name, started_at, applied_steps_count) VALUES (gen_random_uuid()::text, md5(...), NOW(), '{timestamp}_phase13_accounting', NOW(), 1)"`.

  Step F: From `backend/`, run `npx prisma generate`. Confirm the four accessors exist via the smoke command in <behavior>.

  Step G: Commit `backend/prisma/migrations/{timestamp}_phase13_accounting/migration.sql` + `backend/prisma/schema.prisma`. Do NOT commit `node_modules` or generated client.

  Reversal-of-failure path: if `prisma generate` fails, drop the 4 tables + 3 enums via `DROP TABLE ... CASCADE; DROP TYPE ...;`, delete the `_prisma_migrations` row, and re-run from Step B after fixing schema.prisma.
  </action>
  <verify>
    <automated>docker exec tote_postgres psql -U tote_user -d tote_db -tAc "SELECT (SELECT COUNT(*) FROM information_schema.tables WHERE table_name IN ('ExchangeRate','AccountingEntry','Category','AccountingEntryAttachment'))::int = 4 AND (SELECT COUNT(*) FROM \"Category\" WHERE \"appliesTo\"='EXPENSE')::int >= 5 AND (SELECT COUNT(*) FROM \"Category\" WHERE \"appliesTo\"='INCOME')::int >= 2 AND (SELECT COUNT(*) FROM \"Category\" WHERE \"appliesTo\"='PAYMENT')::int >= 2 AND (SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name='ProviderWeeklySettlement'))" | grep -q '^t$' &amp;&amp; cd backend &amp;&amp; node --input-type=module -e 'import { prisma } from "./src/lib/prisma.js"; if (typeof prisma.exchangeRate !== "object" || typeof prisma.accountingEntry !== "object" || typeof prisma.category !== "object" || typeof prisma.accountingEntryAttachment !== "object") process.exit(1); await prisma.$disconnect()'</automated>
  </verify>
  <done>
  - P-3 prerequisite check passed (ProviderWeeklySettlement exists)
  - 4 tables exist in tote_db: ExchangeRate, Category, AccountingEntry, AccountingEntryAttachment
  - 9 category seed rows present (idempotent — re-run leaves count unchanged)
  - `_prisma_migrations` carries the new migration row
  - `npx prisma generate` succeeds and smoke test prints `object object object object`
  - Migration SQL + schema.prisma committed
  - No ssh 94, no pm2 restart, no git push
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
- `docker exec tote_postgres psql -U tote_user -d tote_db -c "\dt" | grep -E "ExchangeRate|AccountingEntry|Category|AccountingEntryAttachment"` lists all 4 tables.
- `docker exec tote_postgres psql -U tote_user -d tote_db -c "\dT" | grep -E "AccountingEntryType|ExchangeRateType|AccountingCurrency"` lists all 3 enums.
- `cd backend && npx prisma validate` exits 0.
- Category seed query returns 9 rows.
- node smoke test confirms all 4 model accessors.
</verification>

<success_criteria>
Phase 13 schema foundation is complete: 4 models, 3 enums, back-relations, indexes, FKs, and seeded categories are live in the local prod-mirror DB. multer + file-type are installed. Plan 13-02 can import `prisma.exchangeRate`, `prisma.accountingEntry`, `prisma.category`, `prisma.accountingEntryAttachment` without further setup. P-3 (Phase 12 prerequisite) is verified at migration time, not assumed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-exchange-rate-accounting-ledger/13-01-SUMMARY.md` capturing the actual migration timestamp filename, seed count, prisma generate output, and any deviations from the planned schema (e.g., if `sequentialNo` was omitted or the self-relation needed a different idiom).
</output>
