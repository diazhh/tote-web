---
phase: 13
plan: 3
type: execute
wave: 3
depends_on: [2]
files_modified:
  - backend/src/middlewares/static-storage-guard.middleware.js
  - backend/src/middlewares/upload.middleware.js
  - backend/src/services/attachment.service.js
  - backend/src/controllers/attachment.controller.js
  - backend/src/routes/contabilidad.routes.js
  - backend/src/index.js
  - backend/src/__tests__/contabilidad.integration.test.js
autonomous: true
requirements:
  - FIN-LEDGER-04
  - FIN-LEDGER-05
user_setup: []

must_haves:
  truths:
    - "static-storage-guard.middleware.js mounted in backend/src/index.js BEFORE express.static('/storage') and returns 401 for any path under /storage/receipts/ (P-1 closure)"
    - "upload.middleware.js exports `uploadReceipt` (multer.memoryStorage, fileSize 5MB, files 1, NO fileFilter — byte validation happens in controller per F-14)"
    - "attachment.service.js exports validateAndStore, getAttachmentStream, deleteAttachment — validateAndStore calls fileTypeFromBuffer BEFORE fs.writeFile and rejects with statusCode=422 when actualMime not in ['application/pdf','image/jpeg','image/png']"
    - "Filename on disk is `${crypto.randomUUID()}.${detected.ext}` — never the operator's originalName"
    - "Disk path is backend/storage/receipts/YYYY/MM/{uuid}.{ext} where YYYY/MM is from entry.entryDate (NOT upload date)"
    - "attachment.controller.js exposes upload (auth-gated + multer single('file')), download (auth-gated stream), remove (auth-gated soft delete) — every mutation writes AuditLog with action UPLOAD or DELETE on entity AccountingEntryAttachment, including ipAddress + userAgent"
    - "contabilidad.routes.js mounts authenticate + authorize('ADMIN') router-level + .bind(controller) for all handlers + a router-level multer error handler that maps LIMIT_FILE_SIZE to 413 with friendly Spanish message (P-3 patterns)"
    - "Router mounted at /api/contabilidad in backend/src/index.js with the other /api/* mounts (lines 228-255 area)"
    - "Integration test exercises end-to-end: POST rate (BsF=200 USD per 1 USD), POST USD entry (amount=10 USD on rate date) → assert amountBsF=2000.00000000 and exchangeRateId locked, POST receipt upload (PDF magic bytes) → 201, POST receipt upload (.html renamed .pdf) → 422, GET /storage/receipts/<path> direct → 401 (P-1 guard), POST /asientos/:id/reverse → returns negated entry, AuditLog row count for that entry is 4 (CREATE entry + UPLOAD attachment + REVERSE + the rate CREATE on the ExchangeRate entity)"
  artifacts:
    - path: "backend/src/middlewares/static-storage-guard.middleware.js"
      provides: "3-line guard that returns 401 for /receipts/* paths under /storage mount (P-1)"
      contains: "staticStorageGuard"
    - path: "backend/src/middlewares/upload.middleware.js"
      provides: "Configured multer instance: memoryStorage + 5MB limit + files=1 + NO fileFilter"
      contains: "memoryStorage"
    - path: "backend/src/services/attachment.service.js"
      provides: "Byte-level MIME validation + UUID filename + entryDate-bucketed disk write + stream + delete"
      exports: ["validateAndStore", "getAttachmentStream", "deleteAttachment"]
    - path: "backend/src/controllers/attachment.controller.js"
      provides: "Upload (multer-piped) + auth-gated download stream + delete with AuditLog UPLOAD/DELETE"
      contains: "class AttachmentController"
    - path: "backend/src/routes/contabilidad.routes.js"
      provides: "Single Express router covering /tasas, /asientos (with /reverse), /asientos/:id/attachments/*, /categorias — all admin-gated + multer error handler"
      contains: "authenticate, authorize('ADMIN')"
    - path: "backend/src/index.js"
      provides: "Mount points: staticStorageGuard BEFORE express.static('/storage'); /api/contabilidad route mount alongside other /api/* mounts"
      contains: "staticStorageGuard"
    - path: "backend/src/__tests__/contabilidad.integration.test.js"
      provides: "End-to-end Jest integration test against the local prod-mirror DB"
      contains: "describe"
  key_links:
    - from: "express.static('/storage', ...) at index.js:136"
      to: "staticStorageGuard mounted on app.use('/storage', ...) at the line immediately BEFORE 136"
      via: "Middleware order — guard short-circuits with 401 for /receipts/* before static handler runs"
      pattern: "app\\.use\\(['\"]\\/storage['\"], staticStorageGuard\\)"
    - from: "contabilidad.routes.js multer middleware"
      to: "attachment.controller.js#upload"
      via: "router.post('/asientos/:id/attachments', uploadReceipt.single('file'), attachmentController.upload.bind(attachmentController))"
      pattern: "uploadReceipt\\.single\\(['\"]file['\"]\\)"
    - from: "attachment.service.js#validateAndStore"
      to: "file-type fileTypeFromBuffer + fs/promises.writeFile"
      via: "Byte inspection THEN disk write — never the reverse (F-14 anti-pattern)"
      pattern: "fileTypeFromBuffer\\(.+?\\.buffer"
    - from: "backend/src/index.js mount block"
      to: "/api/contabilidad → contabilidad.routes.js"
      via: "app.use('/api/contabilidad', contabilidadRoutes) alongside lines 228-255"
      pattern: "app\\.use\\(['\"]\\/api\\/contabilidad['\"]"
---

<objective>
Wire the receipt-upload + auth-gated download + static-storage guard pieces of Phase 13 and compose all controllers from plan 13-02 into a single admin-gated router mounted at `/api/contabilidad`. Close the P-1 BLOCKING gap by inserting `staticStorageGuard` BEFORE `express.static('/storage')` in `backend/src/index.js` so direct hits to `/storage/receipts/*` return 401. Add an integration test against the local prod-mirror DB that exercises the full happy path (rate → USD entry → receipt upload → reversal → AuditLog count) plus three trust-boundary rejections (MIME spoof, direct static URL, settlement-status guard).

Purpose: This plan closes the last backend gap before the UI. After it lands, the frontend in 13-04 only needs to consume the route surface; no further backend changes are required for Phase 13.

Output: 2 new middlewares (guard + multer), 1 new service (attachments — first file-upload feature in repo), 1 new controller (attachments), 1 new router file (composes 4 sub-resources), 1 modified `index.js` (2 added lines for guard + mount), 1 integration test file covering 6 assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-CONTEXT.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-RESEARCH.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-02-SUMMARY.md
@./CLAUDE.md

<interfaces>
Existing in-tree analogs reused verbatim:

- `backend/src/routes/provider.routes.js` (lines 1-40) — exact router template (`Router()` + `router.use(authenticate, authorize('ADMIN'))` + `.bind(controller)` per route).
- `backend/src/middlewares/auth.middleware.js` — exports `authenticate` and `authorize('ADMIN')`.
- `backend/src/controllers/imageController.js` — `res.sendFile` precedent (closest analog for the auth-gated download stream — uses fs.createReadStream-style pipe in 13's case).
- `backend/src/index.js`:
  - Line 24: `app.set('trust proxy', 1)` already set — `req.ip` works behind nginx (P-8 mitigated).
  - Line 136 area: `app.use('/storage', express.static(path.join(__dirname, '../storage'), {...}))` — guard MUST be mounted on `app.use('/storage', staticStorageGuard)` BEFORE this line.
  - Lines 228-255: existing `/api/*` route mounts (gameRoutes, drawRoutes, …). Add `app.use('/api/contabilidad', contabilidadRoutes)` in this block.

Jest test pattern (planner pre-decision O5 — match Phase 11): raw Jest, no supertest, use native fetch against http://localhost:3001 with the dev server running OR direct `import('./contabilidad.routes.js')` mounted on a throwaway express app. Phase 11 used the latter; copy that shape.

Multer 2.x API:
  import multer from 'multer';
  const upload = multer({ storage: multer.memoryStorage(), limits: { fileSize: 5*1024*1024, files: 1 } });
  // Route: app.post('/...', upload.single('file'), handler);
  // In handler: req.file = { buffer, originalname, mimetype (UNTRUSTED), size }.

file-type 19.x API (ESM-only):
  import { fileTypeFromBuffer } from 'file-type';
  const detected = await fileTypeFromBuffer(buffer);  // returns { ext, mime } or undefined.
</interfaces>

@backend/src/index.js
@backend/src/routes/provider.routes.js
@backend/src/middlewares/auth.middleware.js
@backend/src/controllers/imageController.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement static-storage-guard + upload middleware, mount guard in index.js BEFORE express.static</name>
  <read_first>
    - .planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md section 11 (upload.middleware.js, lines 759-786), section 12 (static-storage-guard.middleware.js, lines 790-812), section 13 (index.js modification, lines 816-846)
    - .planning/phases/13-exchange-rate-accounting-ledger/13-RESEARCH.md P-1 (CRITICAL: backend/storage is publicly served — must guard BEFORE static handler) and P-3 (multer LIMIT_FILE_SIZE friendly message)
    - backend/src/index.js around line 136 to confirm exact mount point (the line numbers may shift slightly after Phase 12 changes; identify the `app.use('/storage', express.static(...))` call site by string match, not line number)
  </read_first>
  <behavior>
    - `staticStorageGuard(req, res, next)` returns res.status(401).json({ error: 'Forbidden' }) when `req.path.startsWith('/receipts/')` or `req.path === '/receipts'`. Otherwise calls next(). When mounted via `app.use('/storage', staticStorageGuard)`, the express-stripped path matches `/receipts/...`.
    - `uploadReceipt` is a configured multer instance: `multer.memoryStorage()`, `limits: { fileSize: 5 * 1024 * 1024, files: 1 }`, NO `fileFilter` (the controller does byte-level validation — multer's fileFilter sees only the client-supplied mimetype, which is untrusted per F-14).
    - In `backend/src/index.js`, after the top imports, add `import { staticStorageGuard } from './middlewares/static-storage-guard.middleware.js';`. In the route mount block, add a single line `app.use('/storage', staticStorageGuard);` immediately BEFORE the existing `app.use('/storage', express.static(...))` call. Existing static handler is unchanged.
    - Manual smoke: starting `npm run dev` from backend/, `curl -i http://localhost:3001/storage/receipts/anything.pdf` → HTTP/1.1 401; `curl -i http://localhost:3001/storage/games/1/example.png` → 200 (or 404 if file absent) — non-receipts traffic is unaffected.
  </behavior>
  <action>
  Create `backend/src/middlewares/static-storage-guard.middleware.js` exporting a named function `staticStorageGuard` matching the body in patterns section 12. Add a short JSDoc explaining the P-1 closure rationale and the mount-order requirement.

  Create `backend/src/middlewares/upload.middleware.js` exporting a named const `uploadReceipt` configured per patterns section 11. Comment explicitly that fileFilter is intentionally OMITTED (cite F-14).

  Modify `backend/src/index.js`: add the import at the top with the other middleware imports; locate the existing `app.use('/storage', express.static(...))` call by string search; insert exactly one line `app.use('/storage', staticStorageGuard);` immediately above it. Do NOT change the existing static handler, its options, or any other route mount in this task.
  </action>
  <verify>
    <automated>grep -E "export\s+function\s+staticStorageGuard" backend/src/middlewares/static-storage-guard.middleware.js &amp;&amp; grep -E "memoryStorage|fileSize:\s*5\s*\*\s*1024\s*\*\s*1024" backend/src/middlewares/upload.middleware.js &amp;&amp; ! grep -E "fileFilter" backend/src/middlewares/upload.middleware.js &amp;&amp; grep -E "app\.use\(['\"]\/storage['\"],\s*staticStorageGuard\)" backend/src/index.js &amp;&amp; awk '/app\.use\(.\/storage.,/{print NR": "$0}' backend/src/index.js | head -3 | awk -F: 'NR==1{guard=$1} NR==2{static_=$1} END{exit !(guard &amp;&amp; static_ &amp;&amp; guard+0 &lt; static_+0)}'</automated>
  </verify>
  <done>
  - Both middleware files exist
  - upload.middleware.js has memoryStorage + 5MB limit + files=1 + NO fileFilter
  - index.js imports staticStorageGuard and mounts it BEFORE express.static('/storage') — verified by awk line-order check
  - Backend still boots (`node -c backend/src/index.js` or `npm run dev` smoke)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement attachment.service.js + attachment.controller.js with byte-level MIME validation and AuditLog writes</name>
  <read_first>
    - .planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md section 5 (attachment.service.js, lines 382-457), section 9 (attachment.controller.js, lines 615-697)
    - .planning/phases/13-exchange-rate-accounting-ledger/13-RESEARCH.md Anti-Patterns ("Trusting req.file.mimetype" + "Routing receipt downloads through express.static") + P-5 (entryDate TZ on YYYY/MM path)
    - .planning/phases/13-exchange-rate-accounting-ledger/13-CONTEXT.md D-04 (storage path uses entryDate, NOT upload date)
    - backend/src/controllers/imageController.js — sendFile/createReadStream precedent
    - Task 1 output: upload.middleware.js
  </read_first>
  <behavior>
    - attachment.service.js exports:
      - validateAndStore({ buffer, originalName, entryDate, uploadedById, entryId }): byte-validates with fileTypeFromBuffer; rejects with err.statusCode=422 when not in ['application/pdf','image/jpeg','image/png']; defensive size check rejects with err.statusCode=413 when buffer.length>5MB (multer is primary gate); computes path `backend/storage/receipts/YYYY/MM/{uuid}.{ext}` where YYYY/MM is derived from entryDate via `format(entryDate, 'yyyy/MM')` from date-fns (entryDate is stored as @db.Date — pure date, no TZ trap per P-5); generates UUID via `crypto.randomUUID()`; `fs.mkdir(dir, { recursive: true })` then `fs.writeFile(path, buffer)` then `prisma.accountingEntryAttachment.create({...})` with mimeType set to the VALIDATED detected.mime (NOT req.file.mimetype).
      - getAttachmentStream(attachmentId): loads attachment with `include: { entry: { select: { entryDate: true } } }`, derives the path the same way, returns `{ att, stream: createReadStream(full) }`.
      - deleteAttachment(attachmentId): unlinks from disk (best-effort `.catch(() => {})` — disk drift is recoverable), then deletes the row.
    - attachment.controller.js (class shape mirroring patterns section 9):
      - upload(req,res): if !req.file → 400. Load entry via prisma.accountingEntry.findUniqueOrThrow({ where: { id: req.params.id }, select: { entryDate: true } }). Call attachmentService.validateAndStore({ buffer, originalName: req.file.originalname, entryDate: entry.entryDate, uploadedById: req.user.id, entryId: req.params.id }). On err.statusCode (413/422) → return that status with error message. On success → write AuditLog `{ action: 'UPLOAD', entity: 'AccountingEntryAttachment', entityId: att.id, userId, ipAddress: req.ip, userAgent: req.get('user-agent'), changes: { entryId, mimeType, sizeBytes } }`. Respond 201 with the attachment row.
      - download(req,res): call attachmentService.getAttachmentStream(req.params.attId). Set Content-Type=att.mimeType, Content-Disposition=`attachment; filename="${encodeURIComponent(att.originalName)}"`. Pipe stream to res. No AuditLog on download (read-only op).
      - remove(req,res): call attachmentService.deleteAttachment(req.params.attId). Write AuditLog `{ action: 'DELETE', entity: 'AccountingEntryAttachment', entityId: req.params.attId, changes: { entryId: req.params.id } }`. Respond 200 with `{ success: true }`.
  </behavior>
  <action>
  Create `backend/src/services/attachment.service.js` per patterns section 5 (full byte-validation + path-derive-from-entryDate + UUID filename + fs.writeFile flow). Imports: singleton prisma; fileTypeFromBuffer from 'file-type' (ESM-confirmed in 13-01); randomUUID from 'crypto'; fs from 'fs/promises'; createReadStream from 'fs'; path; format from 'date-fns'. Module-scope constants: ALLOWED_MIMES = new Set([...]), STORAGE_ROOT = path.join(process.cwd(), 'storage', 'receipts'), MAX_BYTES = 5*1024*1024.

  Create `backend/src/controllers/attachment.controller.js` exporting `export default new AttachmentController()` with `upload`, `download`, `remove` methods. Mirror patterns section 9. Every mutation writes AuditLog with ipAddress + userAgent (P-4).

  Do NOT touch index.js or the router in this task — composition happens in Task 3.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; node --input-type=module -e "import * as s from './src/services/attachment.service.js'; import c from './src/controllers/attachment.controller.js'; for (const k of ['validateAndStore','getAttachmentStream','deleteAttachment']) if (typeof s[k] !== 'function') { console.error('svc missing',k); process.exit(1); } for (const m of ['upload','download','remove']) if (typeof c[m] !== 'function') { console.error('ctrl missing',m); process.exit(1); }" &amp;&amp; grep -E "fileTypeFromBuffer\(" backend/src/services/attachment.service.js | head -1 &amp;&amp; grep -E "randomUUID\(\)" backend/src/services/attachment.service.js | head -1 &amp;&amp; grep -E "ipAddress:\s*req\.ip" backend/src/controllers/attachment.controller.js | head -1 &amp;&amp; grep -E "userAgent:\s*req\.get" backend/src/controllers/attachment.controller.js | head -1 &amp;&amp; ! grep -E "req\.file\.mimetype" backend/src/services/attachment.service.js</automated>
  </verify>
  <done>
  - attachment.service.js has all 3 named exports, uses fileTypeFromBuffer + crypto.randomUUID, never references req.file.mimetype
  - attachment.controller.js has all 3 methods, AuditLog writes include ipAddress + userAgent
  - The byte-validation call happens BEFORE fs.writeFile (verifiable by reading the function body — sequence: validate → mkdir → writeFile)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose contabilidad.routes.js router and mount it in backend/src/index.js</name>
  <read_first>
    - .planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md section 10 (contabilidad.routes.js, lines 702-753) — full file template with all routes + the LIMIT_FILE_SIZE error handler
    - backend/src/routes/provider.routes.js — `.bind(controller)` convention + `router.use(authenticate, authorize('ADMIN'))` mount style
    - backend/src/index.js — existing `/api/*` mount block around lines 228-255 for placement reference
    - Task 1+2 outputs: upload middleware, attachment controller; Plan 13-02 outputs: 3 other controllers
  </read_first>
  <behavior>
    - `contabilidad.routes.js` exports a default Router that mounts authenticate + authorize('ADMIN') at the top and then registers all routes per the patterns section 10 template, in this order:
      - POST/GET /tasas → rate controller (no PUT/DELETE — FIN-RATE-02)
      - POST/GET/PATCH /asientos, GET /asientos/:id, POST /asientos/:id/reverse → entry controller
      - POST /asientos/:id/attachments (preceded by uploadReceipt.single('file')) → attachment.upload
      - GET /asientos/:id/attachments/:attId → attachment.download
      - DELETE /asientos/:id/attachments/:attId → attachment.remove
      - POST/GET /categorias, PATCH /categorias/:id, PATCH /categorias/:id/deactivate → category controller
    - A router-level error handler at the bottom catches `err.code === 'LIMIT_FILE_SIZE'` and returns 413 with `{ error: 'Archivo excede 5MB' }` (P-3 friendly message).
    - In `backend/src/index.js`, after the other `/api/*` mounts (locate by string search for the existing `app.use('/api/auth', authRoutes)` block), add `app.use('/api/contabilidad', contabilidadRoutes)`. Import at the top with the other route imports.
  </behavior>
  <action>
  Create `backend/src/routes/contabilidad.routes.js` per patterns section 10 verbatim — imports, `router.use(authenticate, authorize('ADMIN'))`, all routes with `.bind(controller)`, the LIMIT_FILE_SIZE error handler, default export.

  Modify `backend/src/index.js`: add `import contabilidadRoutes from './routes/contabilidad.routes.js';` near the other route imports; add `app.use('/api/contabilidad', contabilidadRoutes);` in the mount block (location: after the existing admin routes mount near lines 228-255). Keep alphabetical-ish placement consistent with neighboring mounts.

  Smoke: start the dev server with `npm run dev` (in background); `curl -i http://localhost:3001/api/contabilidad/tasas` without a token → 401 (authenticate gate). With a valid admin JWT → 200 with `{ success: true, data: [...] }`. Stop the dev server after smoke.

  Do NOT add any TODO routes or deferred endpoints. The router file is final for Phase 13.
  </action>
  <verify>
    <automated>grep -E "authenticate,\s*authorize\(['\"]ADMIN['\"]\)" backend/src/routes/contabilidad.routes.js &amp;&amp; grep -E "uploadReceipt\.single\(['\"]file['\"]\)" backend/src/routes/contabilidad.routes.js &amp;&amp; grep -E "LIMIT_FILE_SIZE" backend/src/routes/contabilidad.routes.js &amp;&amp; grep -E "/asientos/:id/reverse" backend/src/routes/contabilidad.routes.js &amp;&amp; grep -E "app\.use\(['\"]\/api\/contabilidad['\"]" backend/src/index.js &amp;&amp; ! grep -E "router\.(put|delete)\(['\"]\/tasas" backend/src/routes/contabilidad.routes.js</automated>
  </verify>
  <done>
  - contabilidad.routes.js exists with all routes per patterns section 10
  - No PUT/DELETE on /tasas (FIN-RATE-02)
  - Router-level multer error handler maps LIMIT_FILE_SIZE → 413
  - Mounted at /api/contabilidad in index.js
  - Backend boots; unauthenticated GET /api/contabilidad/tasas returns 401
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration test against local prod-mirror DB — end-to-end happy path + 3 trust-boundary rejections</name>
  <read_first>
    - .planning/phases/11-drawfinancial-foundation/ — Phase 11 integration test scaffold (raw Jest, no supertest — planner pre-decision O5)
    - .planning/phases/13-exchange-rate-accounting-ledger/13-RESEARCH.md "Tests + Verification" section (if present) + P-1 + P-6
    - Task 1/2/3 outputs: route surface + middlewares
    - ./CLAUDE.md "Base de Datos — Local (Docker)" for DATABASE_URL
  </read_first>
  <behavior>
    - Test file `backend/src/__tests__/contabilidad.integration.test.js` uses raw Jest with the existing project Jest config. Uses native fetch to hit `http://localhost:3001/api/contabilidad/*` with a real admin JWT obtained via the existing login endpoint (mirror Phase 11 test setup).
    - beforeAll: ensure dev server is running OR mount the express app inline per Phase 11 pattern. Create an admin user if absent (or reuse a seed user) and obtain a JWT.
    - afterEach: clean Phase-13 test rows (DELETE FROM AccountingEntry, ExchangeRate, AccountingEntryAttachment WHERE description LIKE 'TEST-13-%' OR notes LIKE 'TEST-13-%').
    - Test cases (6 assertions total):
      1. Happy: POST /tasas { date: '2026-05-15', rateBsPerUsd: '200.00000000', rateType: 'BCV', notes: 'TEST-13-rate' } → 201. POST /asientos { type: 'EXPENSE', entryDate: '2026-05-15', categoryId: <a seeded EXPENSE category>, currency: 'USD', amount: '10', description: 'TEST-13-expense' } → 201; assert response.data.amountBsF === '2000.00000000' AND response.data.exchangeRateId is the rate id (F-7 lock).
      2. F-6 backend block: POST /asientos with entryDate='2026-04-01' (no rate exists for that date) AND currency='USD' → expect 400 with explicit message containing 'No exchange rate'.
      3. F-14 MIME spoof: Create a tiny HTML buffer (e.g., `<html>x</html>`) renamed `evil.pdf`; POST as multipart/form-data to /asientos/:id/attachments → expect 422 with error containing 'Tipo de archivo no permitido'. Confirm NO file written under backend/storage/receipts/ for that uuid (the buffer was rejected before writeFile).
      4. F-14 happy upload: POST a valid PDF buffer (use a minimal `%PDF-1.4\n...%%EOF` body or read a fixture) → 201 with attachment row; assert file exists on disk at backend/storage/receipts/{YYYY}/{MM}/{uuid}.pdf where YYYY/MM is from the entry's entryDate.
      5. P-1 guard: GET http://localhost:3001/storage/receipts/2026/05/anything.pdf (no auth header) → expect 401.
      6. D-06 reversal + AuditLog count: POST /asientos/:id/reverse { reversalReason: 'TEST-13-reverse' } → 201 with `data.amountBsF === '-2000.00000000'`. Then query the DB directly: `SELECT COUNT(*) FROM "AuditLog" WHERE ("entity"='ExchangeRate' AND "entityId"=<rateId>) OR ("entity"='AccountingEntry' AND "entityId"=<entryId>) OR ("entity"='AccountingEntryAttachment' AND "entityId"=<attId>)` → expect 4 rows (CREATE rate, CREATE entry, UPLOAD attachment, REVERSE entry). Every row's ipAddress and userAgent columns are non-null (P-4 enforced).
  </behavior>
  <action>
  Create `backend/src/__tests__/contabilidad.integration.test.js` with the 6 test cases above. Test data uses the seeded EXPENSE category created in 13-01 Task 3 (look up by name 'Sueldos' or similar — the test should not assume a specific id). Use a small in-test admin JWT (sign with the same secret the auth middleware checks against, OR call the real login endpoint with seeded admin credentials).

  Cleanup queries run after each test. The test file MUST be runnable via `cd backend && npm test -- contabilidad.integration` against the live local DB on port 5433. Document any one-time setup (e.g., "ensure docker-compose up -d" + "ensure backend dev server is running on 3001 OR import the express app inline") in a top-of-file JSDoc.

  If the test setup imports the express app inline (the cleaner Phase 11 pattern), DO NOT mutate process.env or other singletons in a way that affects parallel test runs.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx jest src/__tests__/contabilidad.integration.test.js --runInBand 2>&amp;1 | tee /tmp/phase13-int.log | grep -E "Tests:.*passed" | head -1</automated>
  </verify>
  <done>
  - Test file exists with the 6 assertions described
  - All 6 assertions pass against the live local DB
  - AuditLog row count assertion passes with exactly 4 rows for the touched entities, all with non-null ipAddress + userAgent
  - The HTML-renamed-as-PDF upload returns 422 AND NO orphan file remains on disk
  - The direct /storage/receipts/* hit returns 401
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/contabilidad/* | Admin JWT required; body and FormData are untrusted (validation in service/controller layer). |
| client → /storage/* | Public CDN-style serve for legit assets; receipts MUST be excluded (P-1). |
| client → /api/contabilidad/asientos/:id/attachments (multipart) | Untrusted bytes + untrusted client-supplied mimetype; byte inspection happens server-side BEFORE persistence. |
| service → filesystem | Service constructs path from entryDate + crypto.randomUUID — never from client-supplied filename. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | /storage/receipts/* served by express.static (P-1) | mitigate | staticStorageGuard mounted BEFORE express.static returns 401 (Task 1); integration test confirms (Task 4 assertion 5) |
| T-13-02 | Tampering | Malicious file upload (HTML disguised as PDF) | mitigate | multer memoryStorage + file-type byte inspection in attachment.service.js#validateAndStore BEFORE fs.writeFile; reject with 422; integration test (Task 4 assertion 3) |
| T-13-03 | Denial of Service | Multi-GB upload exhausts memory | mitigate | multer fileSize limit 5MB + files:1 (Task 1); LIMIT_FILE_SIZE error handler returns 413 (Task 3) |
| T-13-04 | Repudiation | Admin denies making a financial change | mitigate | Every D-07 mutation writes AuditLog row with userId + ipAddress + userAgent (Plans 13-02, 13-03); enforced by Task 4 assertion 6 |
| T-13-05 | Tampering | Tampering with operator-supplied filename to traverse paths (e.g., `../../etc/passwd`) | mitigate | Filename on disk is `crypto.randomUUID() + '.' + detected.ext` — operator's originalName preserved only in DB column, never in path (D-04 + attachment.service.js Task 2) |
| T-13-06 | Elevation of Privilege | Non-admin accesses /api/contabilidad/* | mitigate | router.use(authenticate, authorize('ADMIN')) at the top of contabilidad.routes.js (Task 3) |
| T-13-07 | Tampering | Concurrent PAYMENT entries overpay a settlement (P-6 race) | accept | Single-admin operational context per CONTEXT.md; backend re-validates `status IN ('CONFIRMED','ADJUSTED')` on POST (plan 13-02 createEntry); audit log gives operator post-hoc visibility. Documented as accepted risk. |
| T-13-08 | Spoofing | req.ip spoofed via X-Forwarded-For header | mitigate | `app.set('trust proxy', 1)` already at index.js:24 — only ONE proxy hop trusted (nginx on VPS 94) — prevents arbitrary header spoofing from clients (P-8 mitigated) |
</threat_model>

<verification>
After all 4 tasks complete:
- `grep -E "app\.use\(['\"]\/storage['\"]," backend/src/index.js | head -2` shows staticStorageGuard mount immediately above express.static.
- `cd backend && npm run dev` boots cleanly; `curl -i http://localhost:3001/storage/receipts/anything.pdf` → 401; `curl -i -H "Authorization: Bearer <admin-jwt>" http://localhost:3001/api/contabilidad/categorias` → 200 with seeded categories.
- `npx jest src/__tests__/contabilidad.integration.test.js` passes all 6 assertions.
</verification>

<success_criteria>
The full /api/contabilidad route surface is live behind admin auth on the local prod-mirror. The P-1 static-storage gap is closed. Receipt uploads land at the entryDate-derived path with UUID filenames, byte-validated MIME, and AuditLog UPLOAD rows. Reversal works atomically. Direct hits to /storage/receipts/ return 401. The integration test proves all of this against the live DB without ssh, push, or pm2 commands.
</success_criteria>

<output>
After completion, create `.planning/phases/13-exchange-rate-accounting-ledger/13-03-SUMMARY.md` capturing the exact line numbers of the index.js edits, the test runtime, any test flakes seen, and the final shape of the integration test (inline-app vs running-server). Document any deviation from the planned patterns section 5/9/10/11/12 if discovered during implementation.
</output>
