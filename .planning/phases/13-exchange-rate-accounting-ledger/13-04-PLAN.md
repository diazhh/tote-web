---
phase: 13
plan: 4
type: execute
wave: 4
depends_on: [3]
files_modified:
  - frontend/app/admin/contabilidad/page.js
  - frontend/app/admin/contabilidad/asientos/page.js
  - frontend/app/admin/contabilidad/asientos/nueva/page.js
  - frontend/app/admin/contabilidad/asientos/[id]/page.js
  - frontend/app/admin/contabilidad/tasas/page.js
  - frontend/app/admin/contabilidad/categorias/page.js
  - frontend/app/admin/contabilidad/pagos/page.js
  - frontend/lib/api/contabilidad.js
  - frontend/components/admin/Sidebar.js
  - .planning/phases/13-exchange-rate-accounting-ledger/13-DEPLOY.md
autonomous: false
requirements:
  - FIN-RATE-01
  - FIN-RATE-04
  - FIN-RATE-05
  - FIN-LEDGER-01
  - FIN-LEDGER-02
  - FIN-LEDGER-04
  - FIN-LEDGER-05
  - FIN-LEDGER-06
  - FIN-LEDGER-07
  - FIN-LEDGER-08
  - FIN-LEDGER-09
user_setup: []

must_haves:
  truths:
    - "Admin sidebar has a new top-level link 'Contabilidad' routing to /admin/contabilidad"
    - "/admin/contabilidad is a tab switcher with default 'asientos' and three other tabs (Tasas, Categorías, Pagos) per D-05"
    - "Tasas tab lets admin POST a new ExchangeRate (date + rateBsPerUsd + rateType BCV/PARALELO/OTRO + optional notes) and shows the historical timeline filtered by rateType"
    - "Asientos sub-route 'nueva' has a form with currency switcher (BsF/USD); when USD is selected, the form fetches the rate for entryDate and DISABLES the submit button when no rate exists, showing 'No hay tasa de cambio para [date] — ingresa una tasa primero' (F-6 frontend block)"
    - "USD entries show a live BsF preview (amount × rateBsPerUsd) before submit so the operator sees the exchange-rate effect"
    - "PAYMENT type reveals an optional settlement picker fetching `/api/comisiones/settlements?status=CONFIRMED,ADJUSTED&unpaidOnly=true` (or equivalent); selection populates settlementId in the form payload (D-03)"
    - "Categorías tab is a CRUD-style table grouped by appliesTo with Activate/Deactivate toggle — no hard-delete button rendered (D-02 + FIN-LEDGER-06)"
    - "Pagos tab is a filtered view of PAYMENT entries with their linked settlement; a 'Marcar pagado' action navigates to /admin/contabilidad/asientos/nueva?type=PAYMENT&settlementId=... (planner pre-decision O3 — query-string-pre-populated form)"
    - "Entry detail page at /admin/contabilidad/asientos/[id] shows description, amounts (BsF + historical USD eq via amountBsF/exchangeRate.rateBsPerUsd — never reconverted per F-7), receipts list with download links, reversal button visible only when entry.reversedById IS NULL AND entry.reversesId IS NULL (D-06), and AuditLog history embedded from the controller's getOne response"
    - "Reversal button opens a confirmation modal requiring a reversalReason; submit calls POST /api/contabilidad/asientos/:id/reverse and refreshes the detail page; the original entry shows a 'Reversado' badge and the new reversal row shows a 'Reversal de #X' badge"
    - "Receipt upload is a single-file drag-drop (or click-to-pick) widget on the entry detail page; uploads stream via FormData POST to /api/contabilidad/asientos/:id/attachments; client never trusts/sets the mimetype header"
    - "Receipt download links route through GET /api/contabilidad/asientos/:id/attachments/:attId (auth-gated) — NEVER through /storage/receipts/* (P-1)"
    - "All forms use plain useState + native handleSubmit + JWT from localStorage 'accessToken' (matching the in-tree convention proven in /admin/proveedores and /admin/conciliacion) — NO react-hook-form (P-9)"
    - ".planning/phases/13-exchange-rate-accounting-ledger/13-DEPLOY.md exists documenting the LOCAL-ONLY rollout: schema migration steps already taken in 13-01, env vars (none new), no cron updates, no pm2 restarts, no ssh, no push"
  artifacts:
    - path: "frontend/app/admin/contabilidad/page.js"
      provides: "Tab switcher root for /admin/contabilidad (4 sub-tabs per D-05)"
      contains: "useState"
    - path: "frontend/app/admin/contabilidad/asientos/page.js"
      provides: "Entry list with filters (type, dateRange, category, settlement/provider, includeReversed) + link to nueva + per-row detail link"
      contains: "/api/contabilidad/asientos"
    - path: "frontend/app/admin/contabilidad/asientos/nueva/page.js"
      provides: "Create-entry form with currency switcher, F-6 frontend block, settlement picker (PAYMENT only), receipt-upload deferred until detail page"
      contains: "usdBlocked"
    - path: "frontend/app/admin/contabilidad/asientos/[id]/page.js"
      provides: "Entry detail page: amounts, receipts, AuditLog history, reversal modal"
      contains: "canReverse"
    - path: "frontend/app/admin/contabilidad/tasas/page.js"
      provides: "Rate timeline + new-rate form (FIN-RATE-01 + FIN-RATE-05)"
      contains: "rateType"
    - path: "frontend/app/admin/contabilidad/categorias/page.js"
      provides: "Category CRUD table grouped by appliesTo with activate/deactivate (no hard-delete button)"
      contains: "deactivate"
    - path: "frontend/app/admin/contabilidad/pagos/page.js"
      provides: "PAYMENT entries view linked to settlements + 'Marcar pagado' quick action"
      contains: "settlementId"
    - path: "frontend/lib/api/contabilidad.js"
      provides: "Centralized fetch wrappers for all /api/contabilidad/* endpoints — URLSearchParams pattern like conciliacion.js"
      exports: ["fetchRates", "createRate", "fetchEntries", "createEntry", "fetchEntry", "updateEntry", "reverseEntry", "fetchCategories", "createCategory", "deactivateCategory", "uploadAttachment", "downloadAttachmentUrl", "deleteAttachment"]
    - path: "frontend/components/admin/Sidebar.js"
      provides: "New 'Contabilidad' nav link (visible to admin role)"
      contains: "Contabilidad"
    - path: ".planning/phases/13-exchange-rate-accounting-ledger/13-DEPLOY.md"
      provides: "LOCAL-ONLY deploy notes (no ssh, no push, no pm2)"
      contains: "LOCAL"
  key_links:
    - from: "nueva entry form (USD currency)"
      to: "GET /api/contabilidad/tasas?from=<entryDate>&to=<entryDate>"
      via: "useEffect on currency/entryDate change; sets rateForDate; submit button disabled when null (F-6)"
      pattern: "rateForDate"
    - from: "Receipt upload widget on entry detail"
      to: "POST /api/contabilidad/asientos/:id/attachments (multipart FormData)"
      via: "fetch with body: FormData, no Content-Type header (browser sets multipart boundary)"
      pattern: "FormData"
    - from: "Receipt download links"
      to: "GET /api/contabilidad/asientos/:id/attachments/:attId"
      via: "Browser navigation OR fetch+blob with Authorization header"
      pattern: "\\/api\\/contabilidad\\/asientos\\/.+\\/attachments\\/"
    - from: "Sidebar nav"
      to: "/admin/contabilidad"
      via: "Next.js Link"
      pattern: "Contabilidad"
---

<objective>
Ship the admin UI for Phase 13: a `/admin/contabilidad` section with four sub-tabs (Asientos, Tasas, Categorías, Pagos) per D-05, plus a centralized API client wrapper. Match the in-tree React conventions (plain `useState`, native handleSubmit, localStorage JWT, no react-hook-form). Implement the F-6 frontend block (disabled submit + explicit Spanish error message when a USD entry has no rate for the entryDate). Render reversal/reversed badges, AuditLog history on entry detail, and receipt upload/download through auth-gated routes (NEVER through /storage/*).

End the plan with a blocking `checkpoint:human-verify` task: the operator clicks through the four tabs against the live local server, creates a test rate + entry + reversal + receipt upload, and confirms behaviour matches CONTEXT.md success criteria 1-5. Also produce `13-DEPLOY.md` documenting the LOCAL-ONLY rollout.

Purpose: This is the final Phase 13 plan. After it completes and the operator approves the checkpoint, Phase 13 is done; Phase 14 (Report Refactor + Weekly P&L) can begin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-CONTEXT.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-RESEARCH.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md
@.planning/phases/13-exchange-rate-accounting-ledger/13-03-SUMMARY.md
@./CLAUDE.md

<interfaces>
Existing frontend conventions to match verbatim:

- `frontend/app/admin/proveedores/page.js` — tab switcher convention (`const [activeTab, setActiveTab] = useState('configurations')`) at line 14; full CRUD modal pattern at ~lines 780-1100.
- `frontend/app/admin/conciliacion/page.js` — filter+fetch JWT pattern (lines 1-77); URLSearchParams composition.
- `frontend/lib/api/conciliacion.js` — wrapper pattern with explicit named exports + URLSearchParams.
- `frontend/components/admin/Sidebar.js` — admin nav structure; add a new `<Link href="/admin/contabilidad">Contabilidad</Link>` entry alongside the existing 'Proveedores' / 'Comisiones' links.

Form-state convention (mirror `cuentas-sistema/page.js:14`):
  const [formData, setFormData] = useState({...});
  const handleSubmit = async (e) => {
    e.preventDefault();
    const token = localStorage.getItem('accessToken');
    await fetch(`${API_URL}/...`, { method: 'POST', headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' }, body: JSON.stringify(formData) });
  };

For multipart uploads, OMIT the 'Content-Type' header — browser sets the multipart boundary:
  const fd = new FormData();
  fd.append('file', file);
  await fetch(url, { method: 'POST', headers: { Authorization: `Bearer ${token}` }, body: fd });

Phase 13 API contract (consumed by this plan, established by 13-02 + 13-03):
  POST   /api/contabilidad/tasas            { date, rateBsPerUsd, rateType, notes? } → 201 { success, data }
  GET    /api/contabilidad/tasas?rateType=BCV&from=...&to=... → 200 { success, data: [...] }
  POST   /api/contabilidad/asientos         { type, entryDate, categoryId, currency, amount, description, settlementId? } → 201 OR 400 NoRateForDate
  GET    /api/contabilidad/asientos?type=&categoryId=&settlementId=&providerId=&from=&to=&includeReversed=
  GET    /api/contabilidad/asientos/:id     → { success, data: {...entry, auditHistory: [...]} }
  PATCH  /api/contabilidad/asientos/:id     { description?, categoryId?, settlementId? } — strips IMMUTABLE
  POST   /api/contabilidad/asientos/:id/reverse { reversalReason } → 201 with new entry
  POST   /api/contabilidad/asientos/:id/attachments (multipart, single 'file' field) → 201
  GET    /api/contabilidad/asientos/:id/attachments/:attId → file stream with Content-Disposition
  DELETE /api/contabilidad/asientos/:id/attachments/:attId
  POST/GET/PATCH /api/contabilidad/categorias … (+ PATCH /:id/deactivate)
</interfaces>

@frontend/app/admin/proveedores/page.js
@frontend/app/admin/conciliacion/page.js
@frontend/lib/api/conciliacion.js
@frontend/components/admin/Sidebar.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: frontend/lib/api/contabilidad.js + Sidebar nav link</name>
  <read_first>
    - frontend/lib/api/conciliacion.js — exact URLSearchParams + named-export pattern
    - frontend/components/admin/Sidebar.js — existing admin link structure
    - .planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md section File Classification (lib/api/contabilidad.js role) + section 15 (page.js tab switcher template, lines 877-909)
  </read_first>
  <behavior>
    - `frontend/lib/api/contabilidad.js` exports a stable set of named async functions wrapping fetch + JWT + URLSearchParams. Every function reads `localStorage.getItem('accessToken')` at call time. Each returns the parsed `{ success, data }` envelope OR throws on non-2xx with the server-provided error message.
    - Required exports: `fetchRates({rateType, from, to})`, `createRate(payload)`, `fetchEntries(filters)`, `createEntry(payload)`, `fetchEntry(id)`, `updateEntry(id, patch)`, `reverseEntry(id, reversalReason)`, `fetchCategories({appliesTo, includeInactive})`, `createCategory(payload)`, `renameCategory(id, name)`, `deactivateCategory(id)`, `reactivateCategory(id)`, `uploadAttachment(entryId, fileObject)`, `downloadAttachmentUrl(entryId, attId)` (returns the URL string for use in <a href> + Authorization is added by the client OR by an in-app fetch+blob redirect — see DEPLOY.md note), `deleteAttachment(entryId, attId)`.
    - uploadAttachment uses FormData + OMITS the Content-Type header (browser sets multipart boundary).
    - downloadAttachmentUrl returns `${API_URL}/api/contabilidad/asientos/${entryId}/attachments/${attId}`. The detail page uses a `<button>` that fetches the URL with the Authorization header, receives the file as a blob, creates an object URL, and triggers an anchor click for download — because a direct `<a href>` can't carry the Authorization header.
    - Sidebar nav: add a new admin link entry "Contabilidad" → "/admin/contabilidad" alongside the existing "Comisiones" / "Proveedores" links. Visible to admin role only (mirror existing role-gating logic in Sidebar.js).
  </behavior>
  <action>
  Create `frontend/lib/api/contabilidad.js` mirroring `conciliacion.js` shape: imports/constants (API_URL from env), then 14 named async functions per <behavior>. Use `URLSearchParams` for any list endpoint with query filters. Throw with the server error message on non-2xx.

  Modify `frontend/components/admin/Sidebar.js` to add the Contabilidad link. Match the existing icon/section convention. Place it adjacent to Comisiones in the admin section.

  Do NOT add any page components in this task — pages come in Tasks 2-4.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; node --input-type=module -e "import * as api from './lib/api/contabilidad.js'; const need=['fetchRates','createRate','fetchEntries','createEntry','fetchEntry','updateEntry','reverseEntry','fetchCategories','createCategory','renameCategory','deactivateCategory','reactivateCategory','uploadAttachment','downloadAttachmentUrl','deleteAttachment']; for (const k of need) if (typeof api[k] !== 'function') { console.error('missing',k); process.exit(1); }" &amp;&amp; grep -E "Contabilidad" frontend/components/admin/Sidebar.js &amp;&amp; grep -E "/admin/contabilidad" frontend/components/admin/Sidebar.js</automated>
  </verify>
  <done>
  - lib/api/contabilidad.js has all 15 required named exports
  - Sidebar has Contabilidad link → /admin/contabilidad
  - No react-hook-form imports anywhere in the new file
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tasas + Categorías + Pagos sub-tabs (CRUD pages, no complex form logic)</name>
  <read_first>
    - .planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md section 15 (page.js tab switcher), section "Recommended Project Structure" for the 4 sub-routes
    - frontend/app/admin/proveedores/page.js — tab switcher + CRUD-modal pattern
    - frontend/app/admin/conciliacion/page.js — filter+table layout
    - Task 1 output: lib/api/contabilidad.js
    - .planning/phases/13-exchange-rate-accounting-ledger/13-CONTEXT.md D-02 (no hard-delete on categories) + D-05 (sub-tab list)
  </read_first>
  <behavior>
    - `frontend/app/admin/contabilidad/page.js`: 'use client' tab switcher with `useState('asientos')` default per D-05. Renders four tab buttons; clicking a tab navigates via `next/navigation` router.push to `/admin/contabilidad/{tab}`. Keep the tab switcher in a shared shell layout if appropriate.
    - `frontend/app/admin/contabilidad/tasas/page.js`: shows a "Nueva tasa" inline form (date input default today, rateBsPerUsd number input with decimal step, rateType select BCV/PARALELO/OTRO, notes textarea optional). On submit calls `createRate` from the api client. Renders a timeline table below with a rateType filter (None / BCV / PARALELO / OTRO). Each row shows date, rateType, rateBsPerUsd (8-decimal display), notes, createdAt, createdBy (just userId for v1 — name resolution is backlog). NO edit / delete buttons (FIN-RATE-02). Validation: rateBsPerUsd > 0; date is required.
    - `frontend/app/admin/contabilidad/categorias/page.js`: lists categories grouped by appliesTo (3 sections: INCOME / EXPENSE / PAYMENT). Each section has a "Nueva categoría" inline form (name input + appliesTo locked to the section's group). Each row shows name + isActive toggle button (Activar/Desactivar) — NO delete button. Submit calls `createCategory` or `deactivateCategory` / `reactivateCategory`. Includes `?includeInactive=true` toggle to see inactive rows. On P2002 duplicate, show inline error "Categoría ya existe para este tipo".
    - `frontend/app/admin/contabilidad/pagos/page.js`: filtered view of entries where type=PAYMENT, optionally settlementId or providerId. Lists each PAYMENT entry with its linked settlement summary (week, provider name, settlement.amount, paid-so-far). Top of page: "Marcar pagado para una liquidación pendiente" — a settlement picker that lists settlements with status IN ('CONFIRMED','ADJUSTED') AND paidAmount < amount (the backend already filters, but the picker calls the existing Phase 12 endpoint). Clicking a settlement navigates to `/admin/contabilidad/asientos/nueva?type=PAYMENT&settlementId=<id>` (planner pre-decision O3 — query-string-pre-populated form).
  </behavior>
  <action>
  Create the four files listed. Match the `'use client'` directive at the top, useState + useEffect data fetching, and the localStorage 'accessToken' JWT convention. The tab switcher in `contabilidad/page.js` can either render the active sub-page inline OR navigate to a separate route — pick the navigate-to-route variant for deep-linking and back-button correctness (D-05 + Next.js App Router convention).

  Style with existing Tailwind classes used in `/admin/conciliacion` and `/admin/proveedores` — do NOT introduce a new design system or theme.

  For the categorías page, do NOT include a "Delete" button anywhere in the JSX. The only mutations are: create, rename, toggle isActive.

  For the pagos page, the settlement picker can be a simple `<select>` for v1 (operator's settlement count is small). The picker query string params hit Phase 12's `/api/comisiones/settlements?status=CONFIRMED&unpaidOnly=true` or equivalent — confirm the exact Phase 12 endpoint shape from the Phase 12 SUMMARY before wiring.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; for f in app/admin/contabilidad/page.js app/admin/contabilidad/tasas/page.js app/admin/contabilidad/categorias/page.js app/admin/contabilidad/pagos/page.js; do test -f "$f" || { echo "missing $f"; exit 1; }; done &amp;&amp; ! grep -E "react-hook-form" app/admin/contabilidad/tasas/page.js app/admin/contabilidad/categorias/page.js app/admin/contabilidad/pagos/page.js app/admin/contabilidad/page.js &amp;&amp; ! grep -E "Eliminar|Delete|hard.delete|button[^>]*onClick.*[Dd]elete" app/admin/contabilidad/categorias/page.js &amp;&amp; ! grep -E "Eliminar.*tasa|Editar.*tasa" app/admin/contabilidad/tasas/page.js &amp;&amp; grep -E "type=PAYMENT&amp;settlementId" app/admin/contabilidad/pagos/page.js &amp;&amp; cd .. &amp;&amp; cd frontend &amp;&amp; npm run build 2&gt;&amp;1 | tail -5 | grep -E "Compiled successfully|error"</automated>
  </verify>
  <done>
  - 4 page files exist
  - No react-hook-form imports
  - Categorías page has no Delete button (grep gate filters out HTML comments)
  - Tasas page has no Editar/Eliminar buttons (FIN-RATE-02)
  - Pagos page links to /admin/contabilidad/asientos/nueva?type=PAYMENT&settlementId=... query string
  - `next build` (or `next lint`) passes for these pages
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Asientos list + nueva (create-entry form with F-6 frontend block + settlement picker) + entry detail with reversal modal + receipt upload</name>
  <read_first>
    - .planning/phases/13-exchange-rate-accounting-ledger/13-PATTERNS.md sections 16 (nueva form, lines 913-974), 17 (detail page + Reversar, lines 978-1000)
    - .planning/phases/13-exchange-rate-accounting-ledger/13-CONTEXT.md D-03, D-06, FIN-LEDGER-09
    - .planning/phases/13-exchange-rate-accounting-ledger/13-RESEARCH.md "Anti-Patterns" — never trust client-supplied mimetype (server validates; client just sends FormData)
    - Task 1 output: lib/api/contabilidad.js (uploadAttachment, reverseEntry, etc.)
  </read_first>
  <behavior>
    - `frontend/app/admin/contabilidad/asientos/page.js`: list view with filters (type select, dateRange from/to, categoryId select, settlementId/providerId optional, includeReversed checkbox). Renders table with columns: date, type, category, description, amountBsF (8-decimal), USD eq (computed via amountBsF / exchangeRate.rateBsPerUsd when exchangeRateId present — never reconverted), settlement label if PAYMENT, status badges (Reversado if reversedById, "Reversal de #X" if reversesId). Row click navigates to `/admin/contabilidad/asientos/[id]`. "Nuevo asiento" button → /admin/contabilidad/asientos/nueva.
    - `frontend/app/admin/contabilidad/asientos/nueva/page.js`: full create-entry form per patterns section 16.
      - Query string honored: ?type=PAYMENT&settlementId=<id> pre-populates the form (planner pre-decision O3 + D-03 quick-action from Pagos tab).
      - useState formData = { type, entryDate, categoryId, description, currency, amount, settlementId, reversalReason: null }.
      - useEffect on (currency, entryDate) change: when currency==='USD', fetch /api/contabilidad/tasas?from=<entryDate>&to=<entryDate>; if response data is empty array, setRateForDate(null) → render the explicit Spanish error "No hay tasa de cambio para {entryDate} — ingresa una tasa primero." AND disable the submit button (F-6 frontend block).
      - Show live BsF preview when USD: `${Decimal(amount || 0).times(rate.rateBsPerUsd).toFixed(2)} BsF` (just for display — backend computes the authoritative value).
      - Category dropdown filters by appliesTo === formData.type (D-02 — category list call uses fetchCategories({appliesTo: formData.type})). Changing type clears categoryId.
      - When formData.type === 'PAYMENT', reveal the settlement picker fetching from Phase 12's settlement endpoint with status filter; selection sets formData.settlementId. When type !== 'PAYMENT', settlementId is forced to null.
      - On submit: POST via createEntry; on 400 with NoRateForDate message, re-render the inline error and re-disable button; on 201, router.push to `/admin/contabilidad/asientos/${response.data.id}`.
    - `frontend/app/admin/contabilidad/asientos/[id]/page.js`: detail page per patterns section 17.
      - Fetches via fetchEntry(id) which returns the entry plus `auditHistory: [...]`.
      - Renders amounts: amountBsF (8-decimal), originalAmount + originalCurrency if non-null, exchangeRate.rateBsPerUsd + rateType if present, USD historical eq = amountBsF / exchangeRate.rateBsPerUsd (display 2-decimal, computed client-side from the JOINed values — F-7 explicit comment in the JSX).
      - Receipts section: lists attachments (filename original name, sizeBytes formatted, uploadedAt). Each row has a "Descargar" button that calls downloadAttachmentUrl → fetches blob with Authorization → triggers anchor download. Below the list: a single-file drag-drop OR `<input type="file" accept=".pdf,.jpg,.jpeg,.png">` + "Subir recibo" button. On submit calls uploadAttachment(entryId, file). After 201 success, re-fetch the entry to refresh the receipts list.
      - AuditLog history section: list `entry.auditHistory` rows with action / userId / ipAddress / userAgent / createdAt / changes (collapsed JSON inspector).
      - Reversar button: visible only when `!entry.reversedById && !entry.reversesId` (D-06). Click opens confirmation modal with `<textarea required>` for reversalReason. Submit calls reverseEntry(id, reversalReason). After 201, router.push to the new reversal entry detail OR refresh the current page showing the "Reversado" badge.
      - If entry.reversedById is set → show red "Reversado" badge with link to the reversal row's detail.
      - If entry.reversesId is set → show "Reversal de #<originalSequentialNo or id-prefix>" badge with link back.
      - PATCH editable fields inline: description (textarea), categoryId (re-fetched via fetchCategories filtered by entry.type), settlementId (when type=PAYMENT). Save button calls updateEntry(id, patch). IMMUTABLE fields (amountBsF, entryDate, exchangeRateId, type) are display-only — no inputs rendered (FIN-LEDGER-09 frontend enforcement).
  </behavior>
  <action>
  Create the 3 files listed. Use plain useState per P-9 convention.

  For the receipt upload widget, accept exactly one file at a time per CONTEXT.md D-04 (multi-file is deferred to backlog). Pre-validate client-side that file.size <= 5MB and file.type is in ['application/pdf','image/jpeg','image/png','image/jpg'] — but mark this in a comment as "advisory only; server byte-validates" (F-14 anti-pattern: client checks alone are insufficient).

  For the reversal modal, use the same fixed-inset overlay convention seen in `frontend/app/admin/proveedores/logs/page.js` (lines 29-90). The reversalReason textarea has `required` attribute and is rejected by the client if empty (the backend also rejects via 400).

  For inline updates, only PATCH the diff (use a `dirtyFields` set tracked alongside formData). Never send amountBsF, entryDate, exchangeRateId, type in the PATCH body even though the backend strips them — defense in depth and clearer UX.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; for f in app/admin/contabilidad/asientos/page.js app/admin/contabilidad/asientos/nueva/page.js "app/admin/contabilidad/asientos/[id]/page.js"; do test -f "$f" || { echo "missing $f"; exit 1; }; done &amp;&amp; grep -E "usdBlocked|rateForDate" app/admin/contabilidad/asientos/nueva/page.js &amp;&amp; grep -E "No hay tasa de cambio" app/admin/contabilidad/asientos/nueva/page.js &amp;&amp; grep -E "type=PAYMENT.*settlementId|settlementId.*type=PAYMENT|searchParams.*settlementId" app/admin/contabilidad/asientos/nueva/page.js &amp;&amp; grep -E "!entry\.reversedById\s*&amp;&amp;\s*!entry\.reversesId|canReverse" "app/admin/contabilidad/asientos/[id]/page.js" &amp;&amp; grep -E "reversalReason" "app/admin/contabilidad/asientos/[id]/page.js" &amp;&amp; grep -E "FormData\(\)" "app/admin/contabilidad/asientos/[id]/page.js" &amp;&amp; ! grep -E "react-hook-form" app/admin/contabilidad/asientos/page.js app/admin/contabilidad/asientos/nueva/page.js "app/admin/contabilidad/asientos/[id]/page.js" &amp;&amp; npm run build 2&gt;&amp;1 | tail -10 | grep -E "Compiled successfully|error"</automated>
  </verify>
  <done>
  - 3 page files exist
  - nueva/page.js has the F-6 frontend block (usdBlocked + Spanish error + disabled submit)
  - nueva/page.js honors ?type=PAYMENT&settlementId=... query string
  - [id]/page.js has the Reversar visibility predicate AND the reversalReason input
  - [id]/page.js uses FormData for upload (multipart boundary set by browser)
  - No react-hook-form imports
  - `npm run build` passes
  </done>
</task>

<task type="auto">
  <name>Task 4: Author 13-DEPLOY.md (LOCAL-ONLY rollout notes)</name>
  <read_first>
    - .planning/phases/11-drawfinancial-foundation/11-DEPLOY.md — template for LOCAL-ONLY deploy doc shape
    - .planning/phases/13-exchange-rate-accounting-ledger/13-CONTEXT.md (scope_boundaries + assumptions_for_planner)
    - This session's planning_context: "LOCAL ONLY against the prod-mirror DB. NO ssh 94, NO git push, NO pm2 restart. Phase 13 has NO new cron job."
  </read_first>
  <action>
  Create `.planning/phases/13-exchange-rate-accounting-ledger/13-DEPLOY.md` documenting:

  1. **Scope statement (top of file):** "Phase 13 is LOCAL-ONLY in this session. No ssh 94. No git push. No pm2 restart. No /etc/cron.d/tote-triggers update."
  2. **What landed locally (from 13-01..13-03):** 4 Prisma tables, 9 seeded categories, multer + file-type deps, /api/contabilidad routes, static-storage guard, integration test passing.
  3. **Pre-flight checklist for production rollout (deferred to a later session, not run now):**
     - `ssh 94 "cd /var/proyectos/tote-web && git pull"` — pulls main branch after this session's local commits are pushed in a future session.
     - `ssh 94 "cd /var/proyectos/tote-web/backend && npm install"` to install multer + file-type on the VPS.
     - `ssh 94 "cd /var/proyectos/tote-web/backend && DATABASE_URL=... npx prisma migrate deploy"` — applies the Phase 13 migration to prod DB. The migration is idempotent on seeds via ON CONFLICT DO NOTHING.
     - `ssh 94 "pm2 restart tote-backend && pm2 restart tote-frontend"` after migration succeeds. (Frontend may flake — see MEMORY note in CLAUDE.md re: VPS 144 build issues; verify BUILD_ID + Compiled successfully before restart.)
     - Smoke: `curl -i -H "Authorization: Bearer <prod-admin-jwt>" https://toteback.atilax.io/api/contabilidad/categorias` returns 200 with the 9 seeds. `curl -i https://toteback.atilax.io/storage/receipts/anything.pdf` returns 401.
  4. **Rollback plan:** if migration fails on prod, DROP TABLE ... CASCADE the 4 Phase 13 tables and the 3 enums; pg-boss queue, Phase 11/12 tables, and existing workflow are not touched.
  5. **Operator handoff notes:**
     - First production task after rollout: enter today's BCV rate via /admin/contabilidad/tasas before logging any USD expense (F-6 will block otherwise).
     - Seeded categories can be renamed but not deleted; deactivate via the UI toggle to hide unused ones.
     - Receipts older than ~1 month can be moved to cold storage by `mv` on the VPS — only the row in AccountingEntryAttachment matters for app correctness.
  6. **Known operational notes for the planner / future maintainer:**
     - P-6 settlement race: backend re-validates picker selection; the single-admin operator context makes this acceptable per the threat model.
     - The integration test (13-03 Task 4) is the regression baseline — re-run after any controller changes.

  Format the file in plain Markdown with H2 sections for each numbered item above. Do NOT execute any of the production steps — this is documentation only.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-exchange-rate-accounting-ledger/13-DEPLOY.md &amp;&amp; grep -E "LOCAL.ONLY|LOCAL ONLY" .planning/phases/13-exchange-rate-accounting-ledger/13-DEPLOY.md &amp;&amp; grep -E "ssh 94|prisma migrate deploy|pm2 restart" .planning/phases/13-exchange-rate-accounting-ledger/13-DEPLOY.md &amp;&amp; ! grep -v '^#\|^>\|prefix.*ssh' .planning/phases/13-exchange-rate-accounting-ledger/13-DEPLOY.md | grep -E "^\s*ssh 94 " | head -1</automated>
  </verify>
  <done>
  - 13-DEPLOY.md exists with all 6 sections
  - File explicitly states LOCAL-ONLY scope
  - File documents the production rollout steps as deferred (not executed in this session)
  - No live `ssh 94` / `git push` / `pm2` shell commands were actually run during this task — only documented as future steps
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Operator UI smoke + acceptance (CONTEXT.md success criteria 1-5)</name>
  <what-built>
    - Backend: 4 Prisma tables, /api/contabilidad routes, static-storage guard, multer + file-type integration, integration test passing.
    - Frontend: /admin/contabilidad with 4 sub-tabs (Asientos, Tasas, Categorías, Pagos), entry create form with F-6 frontend block, entry detail with reversal + receipt upload, sidebar nav link.
    - 13-DEPLOY.md documenting LOCAL-ONLY rollout.
  </what-built>
  <how-to-verify>
  Run both backend and frontend dev servers locally:
  ```bash
  cd backend && npm run dev    # http://localhost:3001
  cd frontend && npm run dev   # http://localhost:10000
  ```

  Log in as admin at http://localhost:10000/admin. Then verify each CONTEXT.md success criterion below in order:

  1. **FIN-RATE-01 / D-01 — Daily rate entry + immutability:** Navigate to /admin/contabilidad → Tasas tab. Click "Nueva tasa", enter date=today, rateBsPerUsd=100.00, rateType=BCV, notes='Smoke'. Submit → expect 201 and the row appears at the top of the timeline. Try to re-enter the SAME date with a different rate — expect a new row to be created (FIN-RATE-01 + D-01 explicitly allows multiple rows per date; the lookup picks the most-recent createdAt). Confirm there is no Edit/Delete affordance on any rate row (FIN-RATE-02).

  2. **F-6 / FIN-RATE-04 — USD entry blocked without rate:** Navigate to Asientos → Nuevo asiento. Set type=EXPENSE, entryDate=YESTERDAY, currency=USD, amount=50 — expect the form to show "No hay tasa de cambio para {date} — ingresa una tasa primero" and the submit button is DISABLED. Switch entryDate to TODAY (where we entered a rate in step 1) — the message disappears and submit becomes enabled. Change currency back to BsF — submit is enabled regardless of date (BsF entries don't need a rate).

  3. **FIN-LEDGER-01/02/03 — INCOME/EXPENSE/PAYMENT entry with USD conversion:** Create a USD entry: type=EXPENSE, entryDate=today, categoryId=any seeded EXPENSE (e.g., Sueldos), currency=USD, amount=10, description='Smoke USD'. Submit → expect 201 redirect to the detail page; confirm amountBsF reads as `1000.00000000` (10 × 100), the rate row is linked, and originalAmount=10 USD is displayed. Verify the displayed USD historical eq on the detail page comes from amountBsF / exchangeRate.rateBsPerUsd (not a re-conversion using a NEW rate).

  4. **FIN-LEDGER-04 / F-14 — Receipt upload with byte-level MIME rejection:** On the entry detail page, upload a valid PDF (any PDF file ≤ 5MB) → expect 201 and the attachment row appears. Then try to upload an HTML file renamed `.pdf` (create `evil.pdf` containing `<html>x</html>`) → expect HTTP 422 with explicit Spanish error. Confirm `ls backend/storage/receipts/YYYY/MM/` shows the valid PDF but NO file for the rejected HTML upload.

  5. **FIN-LEDGER-05 / P-1 — Direct /storage/receipts/* returns 401:** Note the uuid filename of the valid upload from step 4. Open a NEW browser tab (no admin cookie/JWT) and visit `http://localhost:3001/storage/receipts/{YYYY}/{MM}/{uuid}.pdf` → expect HTTP 401 (not 200, not 404). Also try via curl with no Authorization header — same result.

  Plus two extra smoke checks not in CONTEXT.md success criteria but locked by D-06 + D-02:

  6. **D-06 — Reversal mechanism:** On the entry detail page from step 3, click "Reversar", enter reason 'Smoke reverse', submit → expect 201 with the new reversal entry showing amountBsF=-1000.00000000. Original entry now shows "Reversado" badge and Reversar button is gone. Open the reversal entry — it shows "Reversal de #..." and its Reversar button is also gone.

  7. **D-02 — Category soft-delete:** Navigate to Categorías. Click Desactivar on any seeded EXPENSE category. Confirm the toggle flips to "Activar". Re-open the nueva asiento form with type=EXPENSE — the deactivated category does NOT appear in the dropdown (because the picker filters isActive=true). Toggle Activar back; category re-appears.

  Reply with one of:
  - "approved" — all 7 checks passed.
  - A bullet list of failures with the step number and observed behavior. Plan will be revised before Phase 13 closes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks (including the human checkpoint):
- All 4 sub-tab routes resolve in the browser.
- F-6 frontend block triggers as designed (manual + automated covered).
- Receipt upload writes to the entryDate-derived path; bytewise-bad uploads are 422'd.
- `/storage/receipts/*` direct hits are 401.
- `npm run build` succeeds for the frontend.
- 13-DEPLOY.md documents the deferred production rollout.
</verification>

<success_criteria>
Phase 13 closes when the operator types "approved" on the checkpoint. The admin UI is feature-complete for FIN-RATE-01/04/05 + FIN-LEDGER-01/02/04/05/06/07/08/09. The system enforces F-6 at both the frontend and backend, F-7 by locking exchangeRateId at create, F-14 by byte-level MIME validation, and P-1 by guarding /storage/receipts/* before the static handler. 13-DEPLOY.md is the handoff doc for the eventual production rollout (deferred — not in this session).
</success_criteria>

<output>
After completion (post-checkpoint), create `.planning/phases/13-exchange-rate-accounting-ledger/13-04-SUMMARY.md` capturing: which 7 smoke checks passed unmodified, any UI tweaks made between the initial build and operator approval, the final file count, and any open follow-ups for Phase 14 (e.g., AuditLog viewer UI, multi-file drag-drop, settlement auto-PAID status).
</output>
