---
phase: 12
plan: 4
type: execute
wave: 4
depends_on: [3]
files_modified:
  - frontend/app/admin/proveedores/[id]/comisiones/page.js
  - frontend/app/admin/comisiones/page.js
  - frontend/app/admin/comisiones/settlements/[id]/page.js
  - frontend/components/admin/comisiones/StatusBadge.js
  - frontend/components/admin/comisiones/SettlementsTab.js
  - frontend/components/admin/comisiones/LedgerTab.js
  - frontend/components/admin/comisiones/NewConfigModal.js
  - frontend/lib/api/commissions.js
  - backend/src/scripts/backfill-provider-commissions.mjs
  - backend/storage/backfill-reports/.gitkeep
  - .planning/phases/12-provider-commission-engine/12-DEPLOY.md
autonomous: false
requirements:
  - FIN-COMM-08
  - FIN-COMM-09
  - FIN-COMM-10
  - FIN-COMM-11
  - FIN-COMM-12
user_setup: []
deferred_items:
  - "Tier configurator UI with drag-to-reorder brackets (v1.4+ per REQUIREMENTS.md)"
  - "Threshold alerting >2× week-over-week (backlog)"
  - "Email/Telegram broadcast on settlement confirm (backlog)"
  - "Provider portal where each provider sees own ledger (backlog)"

must_haves:
  truths:
    - "frontend/app/admin/proveedores/[id]/comisiones/page.js exists — Comisiones tab inside the provider detail page; shows current effective config + historical append-only timeline + 'Nueva configuración' button opening a modal (D-05)"
    - "frontend/app/admin/comisiones/page.js exists as the top-level section with two tabs: Liquidaciones (default) and Ledger (D-05)"
    - "frontend/app/admin/comisiones/settlements/[id]/page.js exists with drill-down: settlement header, per-draw ledger lines, status badge, Confirmar/Ajustar buttons gated by current status, Excel + PDF download buttons"
    - "Confirmar button is HIDDEN when status !== 'DRAFT' (UI mirror of backend D-03 enforcement)"
    - "Ajustar button opens a modal collecting { amount, adjustmentReason } and POSTs to /api/commissions/settlements/:id/adjust"
    - "Excel + PDF buttons fetch with auth header and trigger browser download via URL.createObjectURL + <a download>"
    - "NewConfigModal supports all 4 formula types — shows salesRate input when SALES_PCT or SALES_AND_UTILITY_PCT, utilityRate input when UTILITY_PCT or SALES_AND_UTILITY_PCT, dynamic tiers array when TIERED"
    - "backfill-provider-commissions.mjs refuses to write without --confirm (D-02 dry-run gate)"
    - "backfill-provider-commissions.mjs aborts with non-zero exit if any candidate draw has drawnAt < COMMISSION_GO_LIVE (2026-04-17T00:00:00-04:00) — F-17"
    - "backfill-provider-commissions.mjs WHERE clause filters drawnAt >= COMMISSION_GO_LIVE — defense in depth"
    - "backfill-provider-commissions.mjs surfaces D-01 silent-skip count in its summary log"
    - "backfill-provider-commissions.mjs writes a reconciliation CSV at backend/storage/backfill-reports/provider-commission-recon-{stamp}.csv with columns drawId, apiSystemId, formulaType, salesBase, utilityBase, computedAmount, configEffectiveFrom"
    - "12-DEPLOY.md documents the cron line for /etc/cron.d/tote-triggers and the prisma migrate deploy + backfill sequence (LOCAL ONLY — no VPS commands executed this session)"
    - "Plan ends with a checkpoint:human-verify task gating the real backfill execution against the local prod-mirror DB (~660k TicketDetail rows)"
  artifacts:
    - path: "frontend/app/admin/proveedores/[id]/comisiones/page.js"
      provides: "Comisiones tab on the provider detail page — config history + Nueva configuración modal"
      contains: "ProveedorComisionesPage"
    - path: "frontend/app/admin/comisiones/page.js"
      provides: "Top-level /admin/comisiones section with Liquidaciones + Ledger tabs"
      contains: "ComisionesPage"
    - path: "frontend/app/admin/comisiones/settlements/[id]/page.js"
      provides: "Settlement detail with ledger lines, status badge, Confirmar / Ajustar / Excel / PDF buttons"
      contains: "SettlementDetailPage"
    - path: "frontend/components/admin/comisiones/StatusBadge.js"
      provides: "DRAFT / CONFIRMED / ADJUSTED status pill"
    - path: "frontend/lib/api/commissions.js"
      provides: "Typed fetch helpers — listConfigs, createConfig, getLedger, getSettlements, confirmSettlement, adjustSettlement, downloadSettlementExcel, downloadSettlementPdf"
    - path: "backend/src/scripts/backfill-provider-commissions.mjs"
      provides: "Chunked + dry-run-gated + F-17-enforced backfill from 2026-04-17 to deploy date, with reconciliation CSV"
      contains: "COMMISSION_GO_LIVE"
      min_lines: 120
    - path: "backend/storage/backfill-reports/.gitkeep"
      provides: "Reserves the report output directory in source control (already exists from Phase 11 but kept here for self-containment)"
    - path: ".planning/phases/12-provider-commission-engine/12-DEPLOY.md"
      provides: "Production deploy sequence (LOCAL-ONLY — VPS commands are documentation; no execution this session)"
      min_lines: 60
  key_links:
    - from: "frontend pages"
      to: "backend /api/commissions/*"
      via: "fetch with Authorization Bearer JWT from localStorage"
      pattern: "accessToken"
    - from: "settlement detail page Confirmar button"
      to: "PATCH /api/commissions/settlements/:id/confirm"
      via: "commissions.js client helper"
      pattern: "/confirm"
    - from: "settlement detail page Excel button"
      to: "GET /api/commissions/settlements/:id/excel"
      via: "fetch + blob + URL.createObjectURL + <a download>"
      pattern: "createObjectURL"
    - from: "backfill-provider-commissions.mjs"
      to: "commission.service.js (computeAndUpsertLedgerForDraw)"
      via: "Direct service-function import; bypasses pg-boss"
      pattern: "computeAndUpsertLedgerForDraw"
---

<objective>
Ship the admin-facing surface and the historical backfill. The frontend pieces consume the backend routes from Plan 12-03 and present the full D-05 information architecture: a per-provider Comisiones tab for append-only config CRUD, and a top-level /admin/comisiones with Liquidaciones + Ledger tabs and a settlement drill-down that exposes confirm/adjust/Excel/PDF. The backfill script populates ProviderCommissionLedger for all DRAWN draws from COMMISSION_GO_LIVE (2026-04-17) to today using the historically-effective config for each (provider, drawnAt) pair, with full F-17 enforcement, D-02 dry-run gate, and reconciliation CSV output. A checkpoint:human-verify task gates the real backfill execution against the local prod-mirror DB so the operator can inspect the dry-run output before committing. DEPLOY.md captures the production sequence for a future ssh-94 session — but no VPS commands run this session.

Purpose: Until this plan ships, admins cannot manage commission configs, view ledger data, or confirm settlements. The backfill is the second load-bearing piece — without it, the engine only computes commissions for new draws going forward; ~6 weeks of historical data (2026-04-17 → 2026-05-15) remain uncovered.

Output: 4 frontend pages + 4 frontend components + 1 client lib; 1 backfill script + 1 .gitkeep; 1 DEPLOY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-provider-commission-engine/12-CONTEXT.md
@.planning/phases/12-provider-commission-engine/12-RESEARCH.md
@.planning/phases/12-provider-commission-engine/12-PATTERNS.md
@.planning/phases/12-provider-commission-engine/12-01-SUMMARY.md
@.planning/phases/12-provider-commission-engine/12-02-SUMMARY.md
@.planning/phases/12-provider-commission-engine/12-03-SUMMARY.md
@.planning/phases/11-drawfinancial-foundation/11-04-SUMMARY.md
@./CLAUDE.md

<interfaces>
Frontend analogs (read FULL):
- frontend/app/admin/proveedores/page.js — 'use client', useState/useEffect, tab-state pattern at line 14 (const [activeTab, setActiveTab] = useState('configurations')), API_URL from process.env.NEXT_PUBLIC_API_URL, JWT header pattern: const token = localStorage.getItem('accessToken'); headers: { Authorization: `Bearer ${token}` }.
- frontend/app/admin/proveedores/logs/page.js — modal pattern (fixed inset + bg-opacity overlay + click-outside close), status-badge component at lines 8-27 — copy directly for DRAFT/CONFIRMED/ADJUSTED.
- frontend/app/admin/conciliacion/page.js — filters + paginated table + auth header pattern.

Backfill analog (read FULL):
- backend/src/scripts/backfill-draw-financials.mjs — entire script (Phase 11) — copy near-verbatim: argv flag parsing, DRY_RUN/CONFIRM gate, log helper, chunked loop with --chunk-size flag (default 100, min 50, max 500), reconciliation CSV header + write, main() + .finally(prisma.$disconnect).

Service contract from Plan 12-02:
- computeAndUpsertLedgerForDraw(drawId) returns { providersProcessed, skipped }.

Schema constraints from Plan 12-01:
- F-17 locked constant — COMMISSION_GO_LIVE = new Date('2026-04-17T00:00:00-04:00').

Backend routes from Plan 12-03 (verify by curl during UI dev):
- GET    /api/commissions/configs/:apiSystemId
- POST   /api/commissions/configs
- GET    /api/commissions/ledger?apiSystemId=&from=&to=
- GET    /api/commissions/settlements?isoYear=&isoWeek=&apiSystemId=&status=
- GET    /api/commissions/settlements/:id
- PATCH  /api/commissions/settlements/:id/confirm
- PATCH  /api/commissions/settlements/:id/adjust
- GET    /api/commissions/settlements/:id/excel
- GET    /api/commissions/settlements/:id/pdf

CLAUDE.md: local DB is docker tote_postgres on port 5433, prod-mirror with ~660k TicketDetail rows. NO ssh 94, NO pm2 restart, NO git push this session.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the frontend — Comisiones provider tab, top-level /admin/comisiones with Liquidaciones+Ledger tabs, settlement drill-down with Confirmar/Ajustar/Excel/PDF buttons, NewConfigModal, StatusBadge component, commissions.js client lib</name>
  <files>
    frontend/lib/api/commissions.js
    frontend/components/admin/comisiones/StatusBadge.js
    frontend/components/admin/comisiones/NewConfigModal.js
    frontend/components/admin/comisiones/SettlementsTab.js
    frontend/components/admin/comisiones/LedgerTab.js
    frontend/app/admin/proveedores/[id]/comisiones/page.js
    frontend/app/admin/comisiones/page.js
    frontend/app/admin/comisiones/settlements/[id]/page.js
  </files>
  <read_first>
    - frontend/app/admin/proveedores/page.js (FULL — 'use client', useState pattern, tab state at line 14, API_URL env, JWT header pattern at lines 50-53)
    - frontend/app/admin/proveedores/logs/page.js (FULL — status-badge component at lines 8-27, modal overlay pattern at lines 29-90)
    - frontend/app/admin/conciliacion/page.js (FULL — filter bar + table layout)
    - .planning/phases/12-provider-commission-engine/12-PATTERNS.md sections 13, 14, 15 (per-provider tab, /admin/comisiones top-level, settlement drill-down) — especially the blob-download pattern at section 15 (`URL.createObjectURL` + `<a download>` because no existing analog uses it)
    - .planning/phases/12-provider-commission-engine/12-CONTEXT.md D-03 (Confirmar must be hidden when status !== 'DRAFT'), D-05 (UI placement spec — 2 sub-tabs, Liquidaciones default), D-06 (settlement display format `${isoYear}-W${pad(isoWeek)}`), Deferred Ideas list (UI must NOT contain a tier-configurator drag-to-reorder feature; basic CRUD only)
    - .planning/phases/12-provider-commission-engine/12-RESEARCH.md "User Constraints" reaffirming D-05 and D-03
  </read_first>
  <action>
    Step A — Create frontend/lib/api/commissions.js with typed-ish fetch helpers (Next.js JS file, no TS). Each helper reads localStorage.getItem('accessToken'), sets Authorization header, fetches from `${process.env.NEXT_PUBLIC_API_URL}/api/commissions/...`, handles 401 by redirecting to login (or throwing — match the project's existing client-helper convention). Helpers: listConfigs(apiSystemId); createConfig(body); getLedger({apiSystemId, from, to}); getSettlements({isoYear, isoWeek, apiSystemId, status}); getSettlementDetail(id); confirmSettlement(id); adjustSettlement(id, body); downloadSettlementExcel(id) — returns Blob; downloadSettlementPdf(id) — returns Blob. The two download helpers do NOT JSON-decode the response; they call response.blob() and return the Blob.

    Step B — Create frontend/components/admin/comisiones/StatusBadge.js. Copy the structure from frontend/app/admin/proveedores/logs/page.js lines 8-27. Map: DRAFT → 'Borrador' (bg-yellow-100 text-yellow-800), CONFIRMED → 'Confirmada' (bg-green-100 text-green-800), ADJUSTED → 'Ajustada' (bg-orange-100 text-orange-800). Default fallback bg-gray-100 text-gray-800.

    Step C — Create frontend/components/admin/comisiones/NewConfigModal.js. Modal overlay (fixed inset, bg-opacity, click-outside close). Form fields: formulaType select (4 options); effectiveFrom date-time input; notes textarea (optional). Conditional fields: salesRate (numeric, %) shown when formulaType in {SALES_PCT, SALES_AND_UTILITY_PCT}; utilityRate shown when formulaType in {UTILITY_PCT, SALES_AND_UTILITY_PCT}; tiers section shown when formulaType === 'TIERED' with an "Agregar bracket" button that appends rows of {minSales, maxSales (nullable for top tier), rate}. Submit posts to createConfig API helper. On success, calls a parent onCreated callback (which refreshes the list) and closes. NO drag-to-reorder (deferred per REQUIREMENTS.md / deferred_items).

    Step D — Create frontend/components/admin/comisiones/SettlementsTab.js. Filter bar: isoYear/isoWeek input, apiSystem dropdown, status dropdown. Table with columns: Provider (apiSystem.name), Semana (`${isoYear}-W${pad(isoWeek)}`), Estado (StatusBadge), Monto, Líneas, Acciones (link to /admin/comisiones/settlements/{id}). On filter change, calls getSettlements and re-renders.

    Step E — Create frontend/components/admin/comisiones/LedgerTab.js. Filter bar: apiSystem dropdown, from/to date inputs. Table with columns: Sorteo (link to draw detail), Provider, Ventas, Premios, Comisión, Fecha. On filter change, calls getLedger and re-renders.

    Step F — Create frontend/app/admin/proveedores/[id]/comisiones/page.js. 'use client'. useParams to get apiSystemId. On mount and on refresh, fetch listConfigs(apiSystemId). Render: header with "Comisiones — {provider name}" (load provider name from a lightweight existing endpoint, OR just show the id); "Nueva configuración" button that opens NewConfigModal; list of configs in a table — newest effectiveFrom first — columns: Efectivo desde, Tipo de fórmula, Tasa ventas, Tasa utilidad, Brackets (count for TIERED), Notas, Creado por. NO edit/delete buttons (append-only — F-5).

    Step G — Create frontend/app/admin/comisiones/page.js. 'use client'. Two-tab switcher with activeTab state defaulting to 'settlements' (per D-05). Renders <SettlementsTab/> or <LedgerTab/> based on activeTab.

    Step H — Create frontend/app/admin/comisiones/settlements/[id]/page.js. 'use client'. useParams to get id. On mount, fetch getSettlementDetail(id). Render: header `${apiSystem.name} — ${isoYear}-W${pad(isoWeek)}`, StatusBadge, totals card (amount + ledgerRowCount + confirmedAt/By if present + adjustmentReason if present). Action buttons row:
    - Confirmar button — HIDDEN when status !== 'DRAFT' (D-03 UI mirror). On click, calls confirmSettlement(id) and refreshes.
    - Ajustar button — visible when status in {CONFIRMED, ADJUSTED}. Opens a modal collecting { amount, adjustmentReason } and POSTs via adjustSettlement.
    - Descargar Excel button — always visible. Calls downloadSettlementExcel(id), creates blob URL, triggers `<a download="liquidacion-{tag}.xlsx">` programmatically, revokes object URL.
    - Descargar PDF button — same pattern with downloadSettlementPdf.

    Below action buttons: table of per-draw ledger lines (from settlement.ledgerRows) with columns: Sorteo, Fecha, Ventas, Premios, Comisión.

    All frontend files use TailwindCSS v4 classes (no separate CSS file). Match the existing admin page visual style (font, spacing, table classes) from proveedores/page.js.

    DO NOT add a un-confirm button anywhere. DO NOT add a delete-config button anywhere. DO NOT add a tier-reorder drag handle.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - All 8 frontend files exist at the specified paths.
    - grep -F "Confirmar" frontend/app/admin/comisiones/settlements/\[id\]/page.js returns a match.
    - grep -F "status !== 'DRAFT'" frontend/app/admin/comisiones/settlements/\[id\]/page.js returns a match (D-03 UI gate).
    - grep -F "createObjectURL" frontend/app/admin/comisiones/settlements/\[id\]/page.js returns at least 1 match (blob download).
    - grep -F "adjustmentReason" frontend/app/admin/comisiones/settlements/\[id\]/page.js returns a match.
    - grep -E "(deleteConfig|updateConfig|/configs[^']*['\"][^,)]*?(?:PUT|DELETE))" frontend/ -r returns NO match (F-5 — no update/delete on configs from the UI).
    - grep -F "drag" frontend/components/admin/comisiones/NewConfigModal.js returns NO match (deferred — no drag-to-reorder).
    - grep -F "setActiveTab('settlements')" frontend/app/admin/comisiones/page.js returns a match (default tab per D-05).
    - frontend lint passes (npm run lint --quiet exits 0).
    - Manual smoke: visiting http://localhost:10000/admin/comisiones in dev mode renders the page without console errors (validated in Task 3 checkpoint).
  </acceptance_criteria>
  <done>
    All frontend files exist, F-5 / D-03 / D-05 enforced in UI, no deferred-idea features present, lint passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the backfill script backfill-provider-commissions.mjs with F-17 enforcement, D-02 dry-run gate, reconciliation CSV; draft 12-DEPLOY.md</name>
  <files>
    backend/src/scripts/backfill-provider-commissions.mjs
    backend/storage/backfill-reports/.gitkeep
    .planning/phases/12-provider-commission-engine/12-DEPLOY.md
  </files>
  <read_first>
    - backend/src/scripts/backfill-draw-financials.mjs (FULL — copy near-verbatim: argv parsing, DRY_RUN/CONFIRM gate, log helper, chunked loop with --chunk-size flag default 100 min 50 max 500, reconciliation CSV header + write, main() + .finally(prisma.$disconnect))
    - .planning/phases/11-drawfinancial-foundation/11-04-PLAN.md (entire — for the F-10 enum-guard structure to mirror in F-17 form)
    - .planning/phases/11-drawfinancial-foundation/11-DEPLOY.md (Phase 11 deploy doc — copy structure and sequencing for 12-DEPLOY.md)
    - .planning/phases/12-provider-commission-engine/12-PATTERNS.md section 9 (backfill script — F-17 enforcement + candidate-draws SQL + chunked per-draw call + reconciliation CSV columns + summary line for D-01 skipped count)
    - .planning/phases/12-provider-commission-engine/12-RESEARCH.md Pitfall 6 (F-17 — backfill bleeds into pre-2026-04-17), Pitfall 4 (F-12 — cron line in /etc/cron.d/tote-triggers)
    - .planning/phases/12-provider-commission-engine/12-CONTEXT.md D-07 (standalone CLI script, F-17 abort, D-01 skipped count surfaced)
    - backend/src/services/commission.service.js (Plan 12-02 — confirm computeAndUpsertLedgerForDraw signature returns { providersProcessed, skipped })
  </read_first>
  <action>
    Step A — Create backend/src/scripts/backfill-provider-commissions.mjs.

    Header docstring documents: F-17 (COMMISSION_GO_LIVE constant), D-02 (--dry-run / --confirm gate), D-01 silent-skip surfaced in summary, reconciliation CSV path.

    Imports: prisma from '../lib/prisma.js'; logger from '../lib/logger.js'; { computeAndUpsertLedgerForDraw } from '../services/commission.service.js'; fs/promises; path.

    Constants: COMMISSION_GO_LIVE = new Date('2026-04-17T00:00:00-04:00') — locked by REQUIREMENTS.md.

    Argv parsing: const DRY_RUN = argv.includes('--dry-run'); const CONFIRM = argv.includes('--confirm'); chunk-size flag default 100, clamped 50..500. If !DRY_RUN && !CONFIRM, write to stderr "Refusing to write without --confirm. Run with --dry-run first." and process.exit(2).

    main() body:
    1. Log start with mode + chunk size.
    2. F-17 candidate-draws query — prisma.$queryRaw SELECT d.id, d."drawnAt" FROM "Draw" d WHERE d.status = 'DRAWN' AND d."prizesProcessed" = true AND d."drawnAt" >= ${COMMISSION_GO_LIVE} AND NOT EXISTS (SELECT 1 FROM "ProviderCommissionLedger" cl WHERE cl."drawId" = d.id) AND EXISTS (SELECT 1 FROM "DrawFinancialProvider" dfp WHERE dfp."drawId" = d.id AND dfp."apiSystemId" IS NOT NULL) ORDER BY d."drawnAt" ASC. Log the count.
    3. F-17 defense in depth: separately assert prisma.$queryRaw SELECT COUNT(*) FROM "Draw" d WHERE d.status = 'DRAWN' AND d."prizesProcessed" = true AND d."drawnAt" < ${COMMISSION_GO_LIVE} AND EXISTS (SELECT 1 FROM "ProviderCommissionLedger" cl WHERE cl."drawId" = d.id) — must be 0 (no pre-GO-LIVE ledger rows). If non-zero, abort with exit code 3 and detailed log.
    4. Chunked loop. For each draw in slice: if DRY_RUN, increment processed counter and continue. Else try { const r = await computeAndUpsertLedgerForDraw(draw.id); processed++; totalSkipped += r.skipped; } catch (err) { errors++; logger.error(...) }.
    5. After all chunks, write reconciliation CSV at backend/storage/backfill-reports/provider-commission-recon-{ISO-stamp}.csv with header drawId,apiSystemId,formulaType,salesBase,utilityBase,computedAmount,configEffectiveFrom and one row per ledger row written (or all rows in scope if DRY_RUN). Path must be built from process.cwd() + fixed string + timestamp — no untrusted input.
    6. Summary log line includes ledgerWritten = processed - errors, skipped(no_config) = totalSkipped (D-01), errors. Exit 0 on success.

    main + finally — copy verbatim shape from backfill-draw-financials.mjs (catch + log + exitCode = 1; finally + prisma.$disconnect).

    Step B — Create backend/storage/backfill-reports/.gitkeep (zero-byte file). The directory already exists from Phase 11 — confirm via ls and only create the .gitkeep if missing. This is a no-op if the directory + file already exist.

    Step C — Draft .planning/phases/12-provider-commission-engine/12-DEPLOY.md. Document the production deploy sequence for a future ssh-94 session — NO commands run this session. Include:
    1. Pre-flight check: confirm Phase 11 backfill complete on production (SELECT COUNT(*) FROM "DrawFinancial").
    2. Git push + ssh 94 + git pull steps (placeholder commands; not run this session).
    3. cd backend && npx prisma migrate deploy (applies the Plan 12-01 migration on production).
    4. npx prisma generate (Phase 11 lesson — explicit).
    5. pm2 restart tote-backend.
    6. pm2 restart tote-frontend (after frontend build succeeds per CLAUDE.md feedback).
    7. F-12 mitigation — add the cron line to /etc/cron.d/tote-triggers:
       `0 10 * * 1 root /usr/bin/node /var/proyectos/tote-web/backend/src/scripts/trigger-pgboss-cron.mjs weekly-settlement-snapshot`
       (10:00 UTC = 06:00 VE, Venezuela UTC-4 no DST).
    8. Smoke test: verify the queue is in pgboss.queue on prod: `ssh 94 "PGPASSWORD='...' psql -U tote_user -h localhost -p 5433 -d tote_db -tAc \"SELECT COUNT(*) FROM pgboss.queue WHERE name = 'weekly-settlement-snapshot'\""` (must return 1).
    9. Backfill execution (after the next totalized draw has produced a normal ledger row in production):
       `ssh 94 "cd /var/proyectos/tote-web/backend && node src/scripts/backfill-provider-commissions.mjs --dry-run"` then inspect CSV, then re-run with `--confirm`.
    10. Validation: SELECT COUNT(*) on ProviderCommissionLedger; verify MIN(drawnAt) >= '2026-04-17' via JOIN with Draw.
    11. Production status sanity: production still uses status='PUBLISHED' for completed draws (per CLAUDE.md) — but the backfill query filters d.status='DRAWN' AND d.prizesProcessed=true. Document that the production backfill query may need adjustment to `d.status IN ('DRAWN', 'PUBLISHED')` per CLAUDE.md guidance for production-vs-local. Mark this as a pre-deploy review item.

    All of the above is documentation. Do NOT run any ssh command, git push, or pm2 restart this session.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; node src/scripts/backfill-provider-commissions.mjs 2>&amp;1 | grep -q "Refusing to write without --confirm" &amp;&amp; node src/scripts/backfill-provider-commissions.mjs --dry-run 2>&amp;1 | grep -q "Remaining draws to backfill"</automated>
  </verify>
  <acceptance_criteria>
    - grep -F "COMMISSION_GO_LIVE" backend/src/scripts/backfill-provider-commissions.mjs returns at least 2 matches (declaration + usage in WHERE clause).
    - grep -F "'2026-04-17T00:00:00-04:00'" backend/src/scripts/backfill-provider-commissions.mjs returns a match (locked constant).
    - grep -F "--confirm" backend/src/scripts/backfill-provider-commissions.mjs returns at least 1 match (D-02 gate).
    - grep -F "Refusing to write without --confirm" backend/src/scripts/backfill-provider-commissions.mjs returns a match.
    - Running `node src/scripts/backfill-provider-commissions.mjs` (no flags) exits with code 2 and prints the refusal message.
    - Running `node src/scripts/backfill-provider-commissions.mjs --dry-run` exits 0 and prints "Remaining draws to backfill: N" (N is whatever the local prod-mirror DB has — could be 0 if no DRAWN draws after 2026-04-17 exist; the count is whatever it is).
    - grep -F "no_config" backend/src/scripts/backfill-provider-commissions.mjs returns a match (D-01 surfaced in summary).
    - grep -F "provider-commission-recon-" backend/src/scripts/backfill-provider-commissions.mjs returns a match (CSV filename pattern).
    - Directory backend/storage/backfill-reports/ exists.
    - File .planning/phases/12-provider-commission-engine/12-DEPLOY.md exists with at least 60 lines and contains: "prisma migrate deploy", "prisma generate", "/etc/cron.d/tote-triggers", "0 10 * * 1", "weekly-settlement-snapshot", "--dry-run", "--confirm", "status IN ('DRAWN', 'PUBLISHED')" (production status caveat).
  </acceptance_criteria>
  <done>
    Backfill script exists with all D-02 / D-01 / F-17 guards, refuses to write without confirm, dry-run prints remaining count. DEPLOY.md documents the full prod sequence.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT] Human verification — run backfill dry-run against local prod-mirror DB, inspect CSV, then run --confirm; smoke-test the admin UI</name>
  <files>backend/storage/backfill-reports/provider-commission-recon-*.csv</files>
  <action>
    Operator-driven gating task — see <how-to-verify> below for the 15 ordered steps the human must execute. The agent does not perform any of these steps autonomously: the backfill --confirm run and the UI smoke test must be witnessed by the operator before Phase 12 is marked complete. After the human types the resume signal, write the Plan 12-04 SUMMARY.md per the <output> section of this PLAN.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — see how-to-verify for the human-operator steps"</automated>
  </verify>
  <done>Operator approves all 15 verification steps (backfill dry-run inspected, --confirm executed with errors=0, MIN(drawnAt) >= 2026-04-17, admin UI loads and Excel export contains SUM formulas, DEPLOY.md reviewed).</done>
  <what-built>
    All Phase 12 software is now in place locally:
    - Plan 12-01: schema + migration + dateUtils ISO-week helpers
    - Plan 12-02: commission service + 2 workers + queue wiring + tests
    - Plan 12-03: step-process-prizes wiring + admin routes + integration test
    - Plan 12-04 Task 1: frontend pages and components
    - Plan 12-04 Task 2: backfill script + DEPLOY.md draft

    The backend is running locally against the prod-mirror DB (~660k TicketDetail rows) and the frontend is running on http://localhost:10000.
  </what-built>
  <how-to-verify>
    Part A — Backfill dry-run:
    1. Confirm docker is up: `docker ps --filter "name=tote_postgres"` — container running.
    2. cd backend && node src/scripts/backfill-provider-commissions.mjs --dry-run
    3. Inspect the console output for: "Remaining draws to backfill: N" and the F-17 defense-in-depth check passing.
    4. Open the most recent reconciliation CSV at backend/storage/backfill-reports/provider-commission-recon-*.csv. Confirm columns: drawId, apiSystemId, formulaType, salesBase, utilityBase, computedAmount, configEffectiveFrom.
    5. Sanity-check: pick 2-3 rows at random. For each, manually verify computedAmount = salesBase * salesRate / 100 (or whatever the formula type indicates) using a calculator. The numbers should match to 8 decimal places.

    Part B — Real backfill (only after dry-run output looks correct):
    6. cd backend && node src/scripts/backfill-provider-commissions.mjs --confirm
    7. Watch the chunked progress logs. The Summary line at the end must show ledgerWritten + skipped(no_config) + errors. errors MUST be 0.
    8. Run SQL: docker exec tote_postgres psql -U tote_user -d tote_db -tAc "SELECT COUNT(*) FROM \"ProviderCommissionLedger\"" — should be > 0 if any DRAWN draws exist between 2026-04-17 and today with a matching ProviderCommissionConfig.
    9. Run SQL: docker exec tote_postgres psql -U tote_user -d tote_db -tAc "SELECT MIN(d.\"drawnAt\") FROM \"ProviderCommissionLedger\" cl JOIN \"Draw\" d ON d.id = cl.\"drawId\"" — must be >= 2026-04-17 (F-17 holds).

    Part C — Admin UI smoke test:
    10. cd frontend && npm run dev (if not already running) — wait for "Ready in Xs".
    11. Open http://localhost:10000/admin/comisiones in browser. Verify the Liquidaciones tab loads (may be empty if the weekly snapshot worker has not fired yet — that is OK).
    12. Click the Ledger tab. The ledger rows from the backfill should appear with filters working.
    13. Open http://localhost:10000/admin/proveedores — pick any provider — click into the Comisiones tab. Verify "Nueva configuración" button opens a modal with all 4 formula types selectable; verify saving a new config appends a new row (does NOT edit the previous).
    14. If any settlement exists, click into one. Confirm StatusBadge renders, Confirmar button is shown only for DRAFT, Descargar Excel triggers a .xlsx download with audit-grade SUM formulas (open the file and click the TOTAL cell — must show `=SUM(C2:C...)` etc., not a literal number).

    Part D — DEPLOY.md review:
    15. Open .planning/phases/12-provider-commission-engine/12-DEPLOY.md. Confirm the cron line, the prisma generate step, and the production-status caveat (status IN ('DRAWN', 'PUBLISHED')) are all documented.

    If any step fails: describe the issue in your reply. The agent will diagnose and fix before re-running this checkpoint.
  </how-to-verify>
  <resume-signal>
    Type "approved" once all 15 steps pass. Or describe the specific failure (e.g. "Step 7: backfill threw error on draw X") for the agent to fix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend (browser) → /api/commissions/* | JWT from localStorage in Authorization header; auth gate in Plan 12-03 router. |
| Backfill CLI → Postgres | Runs locally with developer credentials; no untrusted input. |
| Backfill CSV file → backend/storage/backfill-reports/ | Path built from process.cwd() + fixed string + timestamp — no input from user. |
| DEPLOY.md → Operator | Documentation only this session; no commands executed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-19 | Tampering | Operator runs backfill without --dry-run first | mitigate | D-02 gate: script refuses to write without --confirm AND requires --dry-run output to be reviewed first (documented in DEPLOY.md as a procedural step). |
| T-12-20 | Tampering | Backfill produces ledger rows for draws before 2026-04-17 (F-17 breach) | mitigate | Triple defense: WHERE clause filters d.drawnAt >= COMMISSION_GO_LIVE; defense-in-depth COUNT check at start aborts with exit 3 if any pre-GO-LIVE ledger row already exists; reconciliation CSV header includes "GO_LIVE=2026-04-17" for audit trail. |
| T-12-21 | Information disclosure | Reconciliation CSV contains sensitive provider rates | accept | File written to backend/storage/backfill-reports/ — same filesystem permissions as the rest of the storage directory; access requires backend filesystem access (admin only). |
| T-12-22 | Tampering | Frontend caller submits PUT/DELETE on /configs | mitigate | Backend has no such route (Plan 12-03 F-5 enforcement); frontend grep confirms no UI button triggers update/delete. |
| T-12-23 | Tampering | Frontend caller submits PATCH /confirm on already-CONFIRMED row | mitigate | Backend returns 400 (Plan 12-03 D-03 enforcement); UI hides the Confirmar button when status !== 'DRAFT' (D-05 UI mirror). |
| T-12-24 | Denial of service | Backfill runs unchunked on ~2600 draws | mitigate | Chunked loop with default 100, configurable via --chunk-size 50..500; sequential (not parallel) to avoid hammering DrawFinancialProvider read path. |
</threat_model>

<verification>
After Plan 12-04 completes:
1. All 8 frontend files exist and frontend lint passes.
2. backfill-provider-commissions.mjs exists; runs without flags = exit 2 with refusal; --dry-run = exit 0 with count; --confirm = writes ledger rows + reconciliation CSV.
3. backend/storage/backfill-reports/ has at least one provider-commission-recon-*.csv file after the human-verified --confirm run.
4. 12-DEPLOY.md exists with all required keywords.
5. Human checkpoint approved (admin UI smoke test + backfill CSV review pass).
6. No VPS commands executed; all production work is documentation.
</verification>

<success_criteria>
- Admin UI fully consumes /api/commissions/* endpoints with F-5 / D-03 / D-05 / D-06 enforcement in the UI.
- Backfill executes against local prod-mirror DB with F-17 holding (MIN drawnAt of ledger >= 2026-04-17).
- DEPLOY.md captures the production sequence for a future operator session.
- Human checkpoint approved before Phase 12 is marked complete.
</success_criteria>

<output>
After completion, create .planning/phases/12-provider-commission-engine/12-04-SUMMARY.md with: the list of all 8 frontend files and their roles, the backfill summary line output (ledgerWritten + skipped + errors counts from the --confirm run), the reconciliation CSV file location with a few sample rows, the human-checkpoint approval, and a note that Phase 12 is now ready for the future production deploy (which uses 12-DEPLOY.md — NOT executed this session).

Also update .planning/STATE.md "Current Position" to Phase 12 → COMPLETE, and append a Phase 12 decisions block to "Accumulated Context" capturing: backfill outcome (ledger row count), the (provider, week) pairs where no config existed (silently skipped), and any operator notes from the checkpoint review.
</output>
</content>
</invoke>