---
phase: 12
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - backend/src/services/commission.service.js
  - backend/src/services/__tests__/commission.service.test.js
  - backend/src/queue/workers/calculate-provider-commission.worker.js
  - backend/src/queue/workers/__tests__/calculate-provider-commission.worker.test.js
  - backend/src/queue/workers/weekly-settlement-snapshot.worker.js
  - backend/src/queue/workers/__tests__/weekly-settlement-snapshot.worker.test.js
  - backend/src/queue/constants.js
  - backend/src/queue/register.js
autonomous: true
requirements:
  - FIN-COMM-01
  - FIN-COMM-02
  - FIN-COMM-03
  - FIN-COMM-04
  - FIN-COMM-05
  - FIN-COMM-06
  - FIN-COMM-07
  - FIN-COMM-09
user_setup: []
deferred_items:
  - "Provider notification on settlement creation (backlog)"
  - "Threshold alerting for week-over-week growth (backlog)"

must_haves:
  truths:
    - "commission.service.js exports findEffectiveConfig, computeCommission, getCumulativeWeeklySales, computeAndUpsertLedgerForDraw, computeSettlementForWeek, getSettlementWithLedger, buildSettlementExcel, getSettlementPdfData, and the DrawFinancialNotReadyError class"
    - "service uses decimal.js with ROUND_HALF_UP for every monetary computation (F-4)"
    - "service NEVER calls prisma.providerCommissionConfig.update — append-only enforced (F-5)"
    - "computeCommission supports all four formula types (SALES_PCT, UTILITY_PCT, SALES_AND_UTILITY_PCT, TIERED) per FIN-COMM-01/02/03"
    - "TIERED bracket lookup uses cumulative weekly sales from getCumulativeWeeklySales (D-04)"
    - "computeAndUpsertLedgerForDraw reads from DrawFinancialProvider (NOT raw TicketDetail) per researcher anti-pattern guidance"
    - "computeAndUpsertLedgerForDraw silently skips providers with no effective config and logs a structured warning (D-01 / FIN-COMM-06)"
    - "Ledger rows include configSnapshot Json with formulaType + rates + tiers for reproducibility"
    - "calculate-provider-commission worker throws DrawFinancialNotReadyError if DrawFinancial.totalizedAt IS NULL (race-guard per Pitfall 7)"
    - "weekly-settlement-snapshot worker uses findFirst + state-conditional branch (NEVER prisma.upsert) per D-03 freeze"
    - "weekly-settlement-snapshot detects drift on CONFIRMED rows and marks them ADJUSTED (D-02 path 2)"
    - "register.js swaps the placeholder no-op for the real calculate-provider-commission handler"
    - "register.js calls boss.createQueue(WEEKLY_SETTLEMENT_SNAPSHOT) BEFORE boss.work for that queue (F-11)"
    - "constants.js declares QUEUES.WEEKLY_SETTLEMENT_SNAPSHOT = 'weekly-settlement-snapshot' and a QUEUE_CONFIGS entry with retryLimit/retryDelay/expireInMinutes"
    - "Both new queues exist in pgboss.queue at boot"
  artifacts:
    - path: "backend/src/services/commission.service.js"
      provides: "Effective-config lookup, formula evaluators (4 types), TIERED cumulative-sales SQL, ledger upsert (D-08 findFirst+update/create), settlement compute, Excel builder, PDF data builder, DrawFinancialNotReadyError class"
      contains: "DrawFinancialNotReadyError"
      min_lines: 250
    - path: "backend/src/queue/workers/calculate-provider-commission.worker.js"
      provides: "pg-boss handler with array-unwrap, race-condition guard, service delegation"
      contains: "DrawFinancialNotReadyError"
    - path: "backend/src/queue/workers/weekly-settlement-snapshot.worker.js"
      provides: "Cron-triggered handler — computes last completed ISO week, GROUP BY ledger, state-conditional findFirst+branch upsert"
      contains: "getISOWeekVE"
    - path: "backend/src/queue/constants.js"
      provides: "QUEUES.WEEKLY_SETTLEMENT_SNAPSHOT + QUEUE_CONFIGS entry"
      contains: "WEEKLY_SETTLEMENT_SNAPSHOT"
    - path: "backend/src/queue/register.js"
      provides: "Real calculate-provider-commission worker bound (replaces placeholder) + weekly-settlement-snapshot worker registered with createQueue-before-work"
      contains: "weeklySettlementSnapshotWorker"
    - path: "backend/src/services/__tests__/commission.service.test.js"
      provides: "Unit tests for formula evaluators (all 4 types incl. TIERED multi-bracket), D-01 silent-skip, D-04 multi-draw cumulative, append-only assertion"
      min_lines: 150
    - path: "backend/src/queue/workers/__tests__/calculate-provider-commission.worker.test.js"
      provides: "Worker test — race-guard throws when DrawFinancial.totalizedAt is null; succeeds when totalized"
      min_lines: 40
    - path: "backend/src/queue/workers/__tests__/weekly-settlement-snapshot.worker.test.js"
      provides: "Worker test — DRAFT update path, CONFIRMED freeze path, ADJUSTED drift detection path"
      min_lines: 60
  key_links:
    - from: "calculate-provider-commission.worker.js"
      to: "commission.service.js (computeAndUpsertLedgerForDraw)"
      via: "Named import"
      pattern: "computeAndUpsertLedgerForDraw"
    - from: "weekly-settlement-snapshot.worker.js"
      to: "dateUtils.js (getISOWeekVE, startOfISOWeekVE, endOfISOWeekVE)"
      via: "Named import"
      pattern: "getISOWeekVE"
    - from: "register.js Phase 11 block"
      to: "calculateProviderCommissionWorker (real impl)"
      via: "Replaces the placeholder no-op at lines 108-112"
      pattern: "calculateProviderCommissionWorker"
    - from: "register.js"
      to: "QUEUES.WEEKLY_SETTLEMENT_SNAPSHOT registration"
      via: "boss.createQueue then boss.work — F-11 ordering"
      pattern: "WEEKLY_SETTLEMENT_SNAPSHOT"
---

<objective>
Build the compute layer of the commission engine: a single commission.service.js with formula evaluators (4 types incl. TIERED cumulative-weekly bracket lookup), append-only effective-config lookup, race-guarded ledger upsert (mirroring Phase 11 D-08 findFirst+update/create), Excel builder reusing the audit-grade SUM-formula pattern from accounting-report.service.js, and a PDF data builder; the calculate-provider-commission pg-boss worker that replaces the Phase 11 placeholder; the weekly-settlement-snapshot worker that runs every Monday via cron Linux and uses state-conditional upsert to honor the D-03 freeze; full unit-test coverage including all four formula types, the D-04 multi-draw cumulative case, D-01 silent skip, the F-15 boundary case from Plan 12-01, the Pitfall 7 race-condition, and the D-02 drift-detection path.

Purpose: Plans 12-03 (pipeline wiring + admin routes) and 12-04 (admin UI + backfill) both depend on the named service exports and the running worker pair. Compute correctness MUST be locked here — once wired into the pipeline, regressions show up as silent monetary drift across thousands of ledger rows.

Output: service file with named exports, two worker files, queue constants + register modifications, and three test files. Both new queues exist in pgboss.queue after the next backend boot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-provider-commission-engine/12-CONTEXT.md
@.planning/phases/12-provider-commission-engine/12-RESEARCH.md
@.planning/phases/12-provider-commission-engine/12-PATTERNS.md
@.planning/phases/12-provider-commission-engine/12-01-SUMMARY.md
@.planning/phases/11-drawfinancial-foundation/11-02-SUMMARY.md
@./CLAUDE.md

<interfaces>
Service shape analog (read fully):
- backend/src/services/draw-financial.service.js — D-08 explicit findFirst + update/create pattern, named exports (export const arrows + export class for error), PrizesNotProcessedError analog for our DrawFinancialNotReadyError, logger structured second-arg convention.

Worker shape analog (read fully):
- backend/src/queue/workers/calculate-draw-financials.worker.js — pg-boss v10 array-unwrap `const job = Array.isArray(jobs) ? jobs[0] : jobs`, draw load, service delegation, error class re-throw → pg-boss retries.

Cron-triggered worker analog (read fully):
- backend/src/queue/workers/cleanup-logs.worker.js — empty-payload, computes work from new Date(), no race guard needed.

Snapshot patterns (read these specific lines):
- backend/src/services/accounting-report.service.js lines 191-290 — buildAccountingExcel: workbook header, column headers with FF1F2937 fill, SUM-formula totals row at lines 246-261, currency formatting at lines 272-277. Copy these structure-by-structure for buildSettlementExcel.

Constants + register anchors (read these exact lines):
- backend/src/queue/constants.js line 18: CALCULATE_PROVIDER_COMMISSION already exists. Lines 108-114: CALCULATE_DRAW_FINANCIALS QUEUE_CONFIGS entry (copy shape for WEEKLY_SETTLEMENT_SNAPSHOT). Lines 115-121: CALCULATE_PROVIDER_COMMISSION QUEUE_CONFIGS entry (keep, the real worker uses the same config or you may tweak retryLimit if needed).
- backend/src/queue/register.js lines 94-113: Phase 11 block. Lines 106-112 is the placeholder no-op to REPLACE. Lines 101-102 createQueue calls STAY (F-11 — queues already registered from Phase 11). The new snapshot worker registration goes after line 113 logger.info, before the retry-failed-publications block at line 115.

Date helpers from Plan 12-01:
- backend/src/lib/dateUtils.js exports getISOWeekVE, startOfISOWeekVE, endOfISOWeekVE.

Schema accessors from Plan 12-01 (locked):
- prisma.providerCommissionConfig (with relation tiers, ledgerRows)
- prisma.providerCommissionTier
- prisma.providerCommissionLedger
- prisma.providerWeeklySettlement
- prisma.drawFinancialProvider (Phase 11)
- prisma.drawFinancial (Phase 11)
- prisma.draw (existing)
- prisma.apiSystem (existing)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create commission.service.js (compute + upsert + Excel + PDF data) and its unit test suite</name>
  <files>
    backend/src/services/commission.service.js
    backend/src/services/__tests__/commission.service.test.js
  </files>
  <read_first>
    - backend/src/services/draw-financial.service.js (FULL — D-08 pattern, PrizesNotProcessedError shape, logger second-arg convention, exporting both arrow consts and a class)
    - backend/src/services/__tests__/draw-financial.service.test.js (FULL — Jest unstable_mockModule pattern, prisma mock shape, expectations style — Plan 12-02 mirrors this exact test structure)
    - backend/src/services/accounting-report.service.js lines 180-300 (buildAccountingExcel — copy header / column / SUM-formula totals / currency formatting patterns)
    - backend/src/lib/dateUtils.js (confirm Plan 12-01 added getISOWeekVE, startOfISOWeekVE, endOfISOWeekVE)
    - .planning/phases/12-provider-commission-engine/12-PATTERNS.md section 2 "commission.service.js" — full module sketch including imports, custom error, findEffectiveConfig, computeCommission switch on formulaType, getCumulativeWeeklySales raw SQL, computeAndUpsertLedgerForDraw with D-08 findFirst+update/create branch, configSnapshot Json construction, buildSettlementExcel with audit-grade SUM formulas
    - .planning/phases/12-provider-commission-engine/12-RESEARCH.md Pattern 1 (effective-config lookup), Pattern 2 (formula evaluation Decimal.js flow with ROUND_HALF_UP), Pattern 3 (TIERED cumulative SQL), Pitfall 1 (F-4 floating-point), Pitfall 2 (F-5 append-only), Pitfall 8 (DrawFinancial vs DrawFinancialProvider consistency)
    - .planning/phases/12-provider-commission-engine/12-CONTEXT.md D-01 (silent skip), D-04 (TIERED weekly reset), and CONTEXT statement "Denormalize configSnapshot Json onto each ledger row"
  </read_first>
  <behavior>
    - computeCommission with SALES_PCT @ 5% on sales 1000 prize 200 returns "50.00000000" (decimal.js ROUND_HALF_UP, .toFixed(8))
    - computeCommission with UTILITY_PCT @ 10% on sales 1000 prize 200 (utility 800) returns "80.00000000"
    - computeCommission with SALES_AND_UTILITY_PCT salesRate 2% utilityRate 5% on sales 1000 prize 200 returns "60.00000000" (20 + 40)
    - computeCommission with TIERED — given tiers [{minSales:0, maxSales:5000, rate:3}, {minSales:5000, maxSales:null, rate:5}] and providerRow.totalSales=1000 with cumulativeWeeklySales=4500 selects the first bracket (rate 3) → returns "30.00000000"
    - computeCommission with TIERED on the same tiers but cumulativeWeeklySales=7000 selects the second bracket (rate 5) → returns "50.00000000"
    - computeCommission with TIERED on cumulative=5000 (boundary — gte 5000 AND lt null/undefined) selects the second bracket (open-ended top tier) → returns "50.00000000"
    - findEffectiveConfig returns the row with the latest effectiveFrom ≤ drawnAt (NOT the latest overall) — test by inserting two config rows and querying with a drawnAt between them
    - computeAndUpsertLedgerForDraw, given a provider WITHOUT any matching config, logs a warning with { drawId, apiSystemId, reason: 'no_config_at_drawnAt' } and writes NO ledger row (D-01); returns { providersProcessed: 0, skipped: 1 }
    - computeAndUpsertLedgerForDraw is idempotent — calling it twice for the same drawId+apiSystemId UPDATEs the existing row instead of throwing a unique-constraint error
    - computeAndUpsertLedgerForDraw populates configSnapshot Json on every ledger row with { formulaType, salesRate, utilityRate, tiers }
    - getCumulativeWeeklySales for a multi-draw week — given 3 DrawFinancialProvider rows for the same apiSystemId with drawnAt Monday/Tuesday/Wednesday all within the same ISO week, returns the SUM of all 3 totalSales (D-04 TIERED multi-draw assertion)
    - Append-only assertion: prisma.providerCommissionConfig.update is NEVER called from any exported function (F-5) — assert by spying on a mocked Prisma client and checking the mock was not called after a full run of every exported service function
  </behavior>
  <action>
    Step A — Create backend/src/services/commission.service.js with the following named exports.

    Imports at top: prisma from '../lib/prisma.js'; logger from '../lib/logger.js'; Decimal from 'decimal.js'; ExcelJS from 'exceljs'; { getISOWeekVE, startOfISOWeekVE, endOfISOWeekVE } from '../lib/dateUtils.js'. Set Decimal.set({ rounding: Decimal.ROUND_HALF_UP }) at module level (F-4).

    Custom error class (copy PrizesNotProcessedError shape from draw-financial.service.js): export class DrawFinancialNotReadyError extends Error — name property "DrawFinancialNotReadyError", message includes drawId.

    Named export findEffectiveConfig(apiSystemId, drawnAt) — prisma.providerCommissionConfig.findFirst with where { apiSystemId, effectiveFrom: { lte: drawnAt } }, orderBy { effectiveFrom: 'desc' }, include { tiers: { orderBy: { minSales: 'asc' } } }. Uses the @@index([apiSystemId, effectiveFrom(sort: Desc)]) from Plan 12-01.

    Named export computeCommission(config, providerRow, cumulativeWeeklySales) — pure function, no DB. Wraps providerRow.totalSales and totalPrize in new Decimal(...toString()). Computes utility = sales - prize. Switches on config.formulaType — SALES_PCT: sales × salesRate / 100; UTILITY_PCT: utility × utilityRate / 100; SALES_AND_UTILITY_PCT: sum of both halves; TIERED: find the first tier where new Decimal(cumulativeWeeklySales).gte(t.minSales) and (t.maxSales === null OR new Decimal(cumulativeWeeklySales).lt(t.maxSales)), then sales × bracket.rate / 100. Returns .toFixed(8) string (lossless transport into Prisma Decimal column). Throws Error('Unknown formulaType') for any other value (defensive — Prisma enum should already prevent this).

    Named export getCumulativeWeeklySales(apiSystemId, drawnAt) — uses prisma.$queryRaw template literal: SELECT COALESCE(SUM(dfp."totalSales"), 0)::numeric(18,8) AS cumulative FROM "DrawFinancialProvider" dfp JOIN "Draw" d ON d.id = dfp."drawId" WHERE dfp."apiSystemId" = ${apiSystemId} AND d."drawnAt" >= ${startOfISOWeekVE(drawnAt)} AND d."drawnAt" <= ${drawnAt}. Returns rows[0].cumulative as string. The JOIN to Draw is required because DrawFinancialProvider does not store drawnAt (Phase 12 documentation note).

    Named export computeAndUpsertLedgerForDraw(drawId) — loads the draw (select drawnAt), errors if not found. Reads prisma.drawFinancialProvider.findMany where { drawId, apiSystemId: { not: null } } include apiSystem. For each provider row: findEffectiveConfig; if null, logger.warn('[commission] no_config_at_drawnAt', { drawId, apiSystemId, reason: 'no_config_at_drawnAt' }) and increment skipped count and continue (D-01 silent skip). If TIERED, call getCumulativeWeeklySales; else pass '0'. Call computeCommission. Build configSnapshot = { formulaType, salesRate: salesRate?.toString() ?? null, utilityRate: utilityRate?.toString() ?? null, tiers: tiers.map(t => ({ minSales, maxSales, rate })) }. Compute sales/utility base via decimal.js .toFixed(8). Use D-08 explicit pattern (findFirst → if existing then update else create) on prisma.providerCommissionLedger keyed by { drawId, apiSystemId } — write { amount, salesBase, utilityBase, configId, configSnapshot }. Return { providersProcessed, skipped }.

    Named export computeSettlementForWeek(apiSystemId, isoYear, isoWeek) — used by Plan 12-03 admin recompute endpoint and Plan 12-04 backfill. Compute start/end via startOfISOWeekVE/endOfISOWeekVE applied to a reference date inside that week (build by date-fns setISOWeek/setISOWeekYear, OR compute Monday of (isoYear, isoWeek) directly via date-fns). Run prisma.$queryRaw SELECT SUM(amount) AS total, COUNT(*) AS rows FROM "ProviderCommissionLedger" cl JOIN "Draw" d ON d.id = cl."drawId" WHERE cl."apiSystemId" = ${apiSystemId} AND d."drawnAt" >= ${start} AND d."drawnAt" <= ${end}. Return { total: string, ledgerRowCount: number }. Pure compute — does NOT write the settlement row (the snapshot worker owns writes).

    Named export getSettlementWithLedger(settlementId) — loads the settlement with apiSystem (name, slug). Then loads the ledger rows for the same (apiSystemId, isoYear/isoWeek) via a follow-up query that JOINs Draw on drawnAt range. Returns { settlement, ledgerRows: [{ ...ledger, draw: { drawDate, drawTime, drawnAt } }] }. Used by Excel/PDF builders.

    Named export buildSettlementExcel(settlementId) — calls getSettlementWithLedger. Creates new ExcelJS.Workbook, creator 'Tote — Liquidación Semanal de Comisiones'. Worksheet name = `${isoYear}-W${String(isoWeek).padStart(2, '0')}`. Title row mergeCells A1:E1, bold size 14. Empty row. Header row [Sorteo, Fecha, Ventas, Premios, Comisión] with bold white-on-FF1F2937 fill (copy from accounting-report.service.js lines 222-230). Data rows for each ledger row. Empty row. TOTAL row using SUM formula cells: { formula: 'SUM(C{dataStart}:C{dataEnd})' } etc., bold + FFE5E7EB fill (copy from accounting-report.service.js lines 248-261). Currency formatting '#,##0.00' on columns C/D/E for all data + total rows. Return Buffer.from(await wb.xlsx.writeBuffer()).

    Named export getSettlementPdfData(settlementId) — pure data shape used by the controller in Plan 12-03 (the controller owns PDFKit streaming). Returns { settlement, ledgerRows, totals: { sales, prizes, commission } } where totals are decimal.js sums of the ledger column values.

    NEVER export updateConfig or anything that calls prisma.providerCommissionConfig.update — F-5 append-only enforcement.

    Step B — Create the test file backend/src/services/__tests__/commission.service.test.js.

    Mirror the Jest unstable_mockModule pattern from draw-financial.service.test.js. Mock prisma at the module level. Cover the 11 behaviors from the <behavior> block above. For the append-only assertion (last bullet), use jest.fn() on prisma.providerCommissionConfig.update and after invoking every exported function across a representative happy-path scenario, assert it was never called: expect(mockUpdate).not.toHaveBeenCalled().

    DO NOT modify any other files.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; node --experimental-vm-modules node_modules/.bin/jest src/services/__tests__/commission.service.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE 'export (const|class|function) (findEffectiveConfig|computeCommission|getCumulativeWeeklySales|computeAndUpsertLedgerForDraw|computeSettlementForWeek|getSettlementWithLedger|buildSettlementExcel|getSettlementPdfData|DrawFinancialNotReadyError)' backend/src/services/commission.service.js is at least 9.
    - grep -F "Decimal.set" backend/src/services/commission.service.js returns a match (decimal.js ROUND_HALF_UP locked at module level — F-4).
    - grep -F "ROUND_HALF_UP" backend/src/services/commission.service.js returns a match.
    - grep -F "prisma.providerCommissionConfig.update" backend/src/services/commission.service.js returns NO match (F-5 enforcement).
    - grep -F "prisma.upsert" backend/src/services/commission.service.js returns NO match (D-08 explicit findFirst+update/create pattern).
    - grep -cE "case 'SALES_PCT'|case 'UTILITY_PCT'|case 'SALES_AND_UTILITY_PCT'|case 'TIERED'" backend/src/services/commission.service.js equals 4.
    - grep -F "no_config_at_drawnAt" backend/src/services/commission.service.js returns a match (D-01 silent-skip reason code).
    - grep -F "configSnapshot" backend/src/services/commission.service.js returns at least 2 matches (build + write).
    - grep -F "SUM(C" backend/src/services/commission.service.js returns a match (audit-grade Excel SUM formula).
    - Jest exits 0 with all 11 behaviors passing.
  </acceptance_criteria>
  <done>
    Service file exports the 9 named symbols, all formula types compute correctly with Decimal precision, D-01 silent-skip and D-04 multi-draw cumulative pass, append-only assertion passes, Excel builder produces SUM-formula totals.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create calculate-provider-commission worker (replaces placeholder) + weekly-settlement-snapshot worker; both with race-guarded / state-conditional logic; unit tests for both</name>
  <files>
    backend/src/queue/workers/calculate-provider-commission.worker.js
    backend/src/queue/workers/weekly-settlement-snapshot.worker.js
    backend/src/queue/workers/__tests__/calculate-provider-commission.worker.test.js
    backend/src/queue/workers/__tests__/weekly-settlement-snapshot.worker.test.js
  </files>
  <read_first>
    - backend/src/queue/workers/calculate-draw-financials.worker.js (FULL — pg-boss v10 array-unwrap pattern, two-phase router structure to ignore here but the boilerplate matches)
    - backend/src/queue/workers/cleanup-logs.worker.js (FULL — cron-triggered empty-payload shape)
    - backend/src/queue/workers/__tests__/calculate-draw-financials.worker.test.js (FULL — test scaffolding mirror)
    - .planning/phases/12-provider-commission-engine/12-PATTERNS.md sections 3 and 4 (commission worker + snapshot worker full sketches)
    - .planning/phases/12-provider-commission-engine/12-RESEARCH.md Pitfall 7 (race-condition mitigation via DrawFinancialNotReadyError + pg-boss retry-with-backoff), Pattern 7 (snapshot worker with state-conditional findFirst + branch, D-02 drift handling)
    - .planning/phases/12-provider-commission-engine/12-CONTEXT.md D-02 (ADJUSTED triggers — manual override OR re-totalize-into-CONFIRMED), D-03 (CONFIRMED terminal — no automated overwrite)
    - backend/src/services/commission.service.js (just created — confirm exports computeAndUpsertLedgerForDraw and DrawFinancialNotReadyError)
    - backend/src/lib/dateUtils.js (just extended — confirm getISOWeekVE, startOfISOWeekVE, endOfISOWeekVE)
  </read_first>
  <behavior>
    - calculate-provider-commission worker: given job.data.drawId where DrawFinancial.totalizedAt IS NULL → throws DrawFinancialNotReadyError (pg-boss will retry per QUEUE_CONFIGS retryLimit + retryBackoff)
    - calculate-provider-commission worker: given job.data.drawId where DrawFinancial.totalizedAt IS NOT NULL → calls computeAndUpsertLedgerForDraw(drawId) and returns { success: true, drawId, providersProcessed, skipped }
    - weekly-settlement-snapshot worker: given an apiSystemId with ledger rows in the prior ISO week and NO existing settlement row → creates a new ProviderWeeklySettlement with status DRAFT, amount = SUM(ledger.amount), ledgerRowCount = COUNT
    - weekly-settlement-snapshot worker: given an existing DRAFT settlement → UPDATEs amount and ledgerRowCount (re-snapshot allowed in DRAFT)
    - weekly-settlement-snapshot worker: given an existing CONFIRMED settlement whose recomputed amount differs from existing.amount → marks the row ADJUSTED with adjustmentReason 'auto: drift detected by snapshot' (D-02 path 2) and logs a warning; the recomputed amount is NOT written to .amount (D-03 freeze)
    - weekly-settlement-snapshot worker: given an existing CONFIRMED settlement whose recomputed amount equals existing.amount → makes NO state change (no drift)
    - weekly-settlement-snapshot worker: given an existing ADJUSTED settlement → makes NO state change regardless of drift (D-02 says ADJUSTED is terminal vs automatic recomputation)
    - Both workers use the pg-boss v10 array-unwrap idiom: const job = Array.isArray(jobs) ? jobs[0] : jobs
  </behavior>
  <action>
    Step A — Create backend/src/queue/workers/calculate-provider-commission.worker.js.

    Imports: prisma from '../../lib/prisma.js'; logger from '../../lib/logger.js'; { computeAndUpsertLedgerForDraw, DrawFinancialNotReadyError } from '../../services/commission.service.js'.

    Export async function calculateProviderCommissionWorker(jobs). Body: const job = Array.isArray(jobs) ? jobs[0] : jobs; const { drawId } = job.data. Race-condition guard (Pitfall 7): const df = await prisma.drawFinancial.findUnique({ where: { drawId }, select: { totalizedAt: true } }); if (!df || df.totalizedAt === null) throw new DrawFinancialNotReadyError(drawId). On success: logger.info(`[calculate-provider-commission] drawId=${drawId}`); const result = await computeAndUpsertLedgerForDraw(drawId); return { success: true, drawId, providersProcessed: result.providersProcessed, skipped: result.skipped }.

    Step B — Create backend/src/queue/workers/weekly-settlement-snapshot.worker.js.

    Imports: prisma; logger; Decimal from 'decimal.js'; { getISOWeekVE, startOfISOWeekVE, endOfISOWeekVE } from '../../lib/dateUtils.js'; { subDays } from 'date-fns'.

    Export async function weeklySettlementSnapshotWorker(jobs). Body: const job = Array.isArray(jobs) ? jobs[0] : jobs (payload is empty — cron fires it). Compute referenceDate = subDays(new Date(), 1) (cron fires Monday 06:00 VE → use "yesterday" so we are firmly inside the closed prior ISO week even if cron fires late). const { isoYear, isoWeek } = getISOWeekVE(referenceDate); const start = startOfISOWeekVE(referenceDate); const end = endOfISOWeekVE(referenceDate). logger.info(`[weekly-settlement-snapshot] isoYear=${isoYear} isoWeek=${isoWeek} range=${start.toISOString()}..${end.toISOString()}`).

    GROUP BY apiSystemId via prisma.$queryRaw: SELECT cl."apiSystemId", SUM(cl.amount)::numeric(18,8) AS "totalAmount", COUNT(*)::int AS "ledgerRowCount" FROM "ProviderCommissionLedger" cl JOIN "Draw" d ON d.id = cl."drawId" WHERE d."drawnAt" >= ${start} AND d."drawnAt" <= ${end} GROUP BY cl."apiSystemId".

    For each row, branch on state (D-03 freeze + D-02 drift):
    - findFirst settlement where { apiSystemId, isoYear, isoWeek }.
    - If not exists → prisma.providerWeeklySettlement.create with { apiSystemId, isoYear, isoWeek, amount: row.totalAmount, ledgerRowCount: row.ledgerRowCount, status: 'DRAFT', snapshotAt: new Date() }; counter++.
    - Else if existing.status === 'DRAFT' → prisma.providerWeeklySettlement.update id with { amount: row.totalAmount, ledgerRowCount: row.ledgerRowCount, snapshotAt: new Date() }; counter++.
    - Else (CONFIRMED or ADJUSTED) — compute drift = !(new Decimal(existing.amount.toString()).equals(row.totalAmount.toString())). If drift AND existing.status === 'CONFIRMED' → update with { status: 'ADJUSTED', adjustmentReason: 'auto: drift detected by snapshot' }; log warn with old/new amount; drifted++. Otherwise frozen++.

    Final logger.info with counts. Return { isoYear, isoWeek, created, updated, frozen, drifted }.

    NEVER call prisma.upsert in this worker (D-03 freeze requires state-conditional branching per researcher Anti-Patterns).

    Step C — Worker tests.

    backend/src/queue/workers/__tests__/calculate-provider-commission.worker.test.js — mirror calculate-draw-financials.worker.test.js scaffold. Mock prisma and the commission service. Cover the two race-guard behaviors.

    backend/src/queue/workers/__tests__/weekly-settlement-snapshot.worker.test.js — mock prisma and the date helpers. Cover the four state-conditional branches (create new DRAFT, update existing DRAFT, freeze CONFIRMED with no drift, mark CONFIRMED as ADJUSTED on drift, leave ADJUSTED alone).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; node --experimental-vm-modules node_modules/.bin/jest src/queue/workers/__tests__/calculate-provider-commission.worker.test.js src/queue/workers/__tests__/weekly-settlement-snapshot.worker.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -F 'export async function calculateProviderCommissionWorker' backend/src/queue/workers/calculate-provider-commission.worker.js returns a match.
    - grep -F 'DrawFinancialNotReadyError' backend/src/queue/workers/calculate-provider-commission.worker.js returns at least 2 matches (import + throw).
    - grep -F 'Array.isArray(jobs)' backend/src/queue/workers/calculate-provider-commission.worker.js returns a match.
    - grep -F 'export async function weeklySettlementSnapshotWorker' backend/src/queue/workers/weekly-settlement-snapshot.worker.js returns a match.
    - grep -F 'getISOWeekVE' backend/src/queue/workers/weekly-settlement-snapshot.worker.js returns a match.
    - grep -F 'subDays' backend/src/queue/workers/weekly-settlement-snapshot.worker.js returns a match.
    - grep -F "prisma.upsert" backend/src/queue/workers/weekly-settlement-snapshot.worker.js returns NO match.
    - grep -F "prisma.providerWeeklySettlement.upsert" backend/src/queue/workers/weekly-settlement-snapshot.worker.js returns NO match.
    - grep -F "'auto: drift detected by snapshot'" backend/src/queue/workers/weekly-settlement-snapshot.worker.js returns a match (D-02 path 2 reason string).
    - Both worker test suites pass under Jest.
  </acceptance_criteria>
  <done>
    Both worker files exist with correct imports, race-guard and state-conditional logic, no prisma.upsert anywhere. All worker tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire QUEUES.WEEKLY_SETTLEMENT_SNAPSHOT into constants.js; swap commission placeholder in register.js and register snapshot worker with F-11 createQueue-before-work</name>
  <files>
    backend/src/queue/constants.js
    backend/src/queue/register.js
  </files>
  <read_first>
    - backend/src/queue/constants.js (FULL — locate the QUEUES object end (line ~28), the QUEUE_CONFIGS object for CALCULATE_DRAW_FINANCIALS at lines 108-114 (config shape to mirror), the existing CALCULATE_PROVIDER_COMMISSION QUEUES entry at line 18 and its QUEUE_CONFIGS entry at lines 115-121)
    - backend/src/queue/register.js (FULL — locate the Phase 11 block at lines 94-113. Line 102 already has boss.createQueue(QUEUES.CALCULATE_PROVIDER_COMMISSION) — STAYS. Lines 106-112 placeholder no-op handler — REPLACES with real handler. After line 113 logger.info, BEFORE the retry-failed-publications block at line 115, register the new snapshot worker.)
    - .planning/phases/12-provider-commission-engine/12-PATTERNS.md sections 5 (register.js — exact swap) and 6 (constants.js — exact additions)
    - .planning/phases/12-provider-commission-engine/12-RESEARCH.md Pitfall 4 (F-12 — DEPLOY.md will hold the cron line; this plan only registers the worker)
  </read_first>
  <action>
    Step A — Modify backend/src/queue/constants.js.

    Inside the QUEUES object (declared at line 1), after line 18 CALCULATE_PROVIDER_COMMISSION entry, add a single new line: `WEEKLY_SETTLEMENT_SNAPSHOT: 'weekly-settlement-snapshot',` with a comment "// Phase 12 — weekly settlement snapshot (cron-triggered Mondays 06:00 VE)".

    Inside the QUEUE_CONFIGS object, after the CALCULATE_PROVIDER_COMMISSION entry at lines 115-121, add a new entry mirroring the CALCULATE_DRAW_FINANCIALS shape but with larger expireInMinutes (GROUP BY across ~4 providers × ~50 rows can take longer than per-draw work, but should still be << 1 minute). Use: retryLimit 2, retryDelay 30, retryBackoff true, expireInMinutes 10.

    Step B — Modify backend/src/queue/register.js.

    Surgical change 1 — Replace the placeholder handler at lines 106-112. The placeholder boss.work call currently registers an async (jobs) => { logger.info(...); return { placeholder: true }; } handler. Replace ONLY the handler body, keeping the createQueue call at line 102 intact (F-11). The new code dynamically imports calculateProviderCommissionWorker from './workers/calculate-provider-commission.worker.js' (mirror the import style at line 99 for calculateDrawFinancialsWorker) and binds boss.work with that handler.

    Surgical change 2 — Register the snapshot worker. After the logger.info at line 113 "calculate-draw-financials + commission-placeholder registrados", and BEFORE the retry-failed-publications block at line 115, add:
    1. const { weeklySettlementSnapshotWorker } = await import('./workers/weekly-settlement-snapshot.worker.js');
    2. await boss.createQueue(QUEUES.WEEKLY_SETTLEMENT_SNAPSHOT); — F-11 mandate: createQueue BEFORE work.
    3. await boss.work(QUEUES.WEEKLY_SETTLEMENT_SNAPSHOT, QUEUE_CONFIGS[QUEUES.WEEKLY_SETTLEMENT_SNAPSHOT], weeklySettlementSnapshotWorker);
    4. logger.info('[pg-boss] Worker weekly-settlement-snapshot registrado (trigger via cron Linux, Lunes 06:00 VE)');

    Update the comment block above the Phase 11 section (lines 94-98) to mention Phase 12 now owns the real handler and adds the snapshot queue — narrative only, no functional change.

    Step C — Smoke test: after boot, run a SQL check that both queues exist in pgboss.queue (this is the verify command).

    DO NOT modify any other files. DO NOT touch any other queue's createQueue ordering.
  </action>
  <verify>
    <automated>grep -F "WEEKLY_SETTLEMENT_SNAPSHOT: 'weekly-settlement-snapshot'" backend/src/queue/constants.js &amp;&amp; grep -F "calculateProviderCommissionWorker" backend/src/queue/register.js &amp;&amp; grep -F "weeklySettlementSnapshotWorker" backend/src/queue/register.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -F "WEEKLY_SETTLEMENT_SNAPSHOT: 'weekly-settlement-snapshot'" backend/src/queue/constants.js returns a match.
    - grep -F "[QUEUES.WEEKLY_SETTLEMENT_SNAPSHOT]:" backend/src/queue/constants.js returns a match (config entry).
    - grep -F "expireInMinutes: 10" backend/src/queue/constants.js returns at least 1 match in the WEEKLY_SETTLEMENT_SNAPSHOT config block.
    - grep -F "calculateProviderCommissionWorker" backend/src/queue/register.js returns at least 2 matches (import + work registration).
    - grep -F "weeklySettlementSnapshotWorker" backend/src/queue/register.js returns at least 2 matches (import + work registration).
    - grep -F "boss.createQueue(QUEUES.WEEKLY_SETTLEMENT_SNAPSHOT)" backend/src/queue/register.js returns a match.
    - grep -F "phase-12 placeholder" backend/src/queue/register.js returns NO match (placeholder body has been swapped out).
    - grep -F "return { placeholder: true }" backend/src/queue/register.js returns NO match.
    - After starting the backend in dev mode (or running the boss-init smoke test), the SQL `SELECT name FROM pgboss.queue WHERE name IN ('calculate-provider-commission','weekly-settlement-snapshot')` returns exactly 2 rows. Verify by: docker exec tote_postgres psql -U tote_user -d tote_db -tAc "SELECT COUNT(*) FROM pgboss.queue WHERE name IN ('calculate-provider-commission','weekly-settlement-snapshot')" (run after the backend has booted once with the new code).
  </acceptance_criteria>
  <done>
    Constants has the new queue + config, register.js swapped the placeholder for the real worker and registered the snapshot worker with createQueue-before-work, both queues present in pgboss.queue at runtime.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg-boss queue payload → worker | Worker receives drawId from pg-boss; sourced internally from step-process-prizes.worker.js (trusted boundary). |
| Worker → commission.service.js | Pure named-function imports; no untrusted input. |
| Service → Prisma → Postgres | Prisma parameterized queries — no string concatenation in $queryRaw. |
| Cron Linux (root) → trigger-pgboss-cron.mjs → pg-boss enqueue | Out of scope this plan; covered by Plan 12-03 (allowlist update). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | Floating-point drift on commission rates (F-4) | mitigate | decimal.js with Decimal.set ROUND_HALF_UP at module load; every monetary computation wraps in new Decimal(...toString()) and emits .toFixed(8); unit tests prove all 4 formula types compute losslessly. |
| T-12-06 | Tampering | UPDATE on ProviderCommissionConfig retroactively (F-5) | mitigate | Service file never imports an update method for this model; explicit acceptance criterion forbids prisma.providerCommissionConfig.update calls (grep gate). Unit test mocks prisma.providerCommissionConfig.update and asserts it is never invoked. |
| T-12-07 | Tampering | CONFIRMED settlement overwritten by snapshot worker (D-03) | mitigate | Snapshot worker uses explicit findFirst + branch on existing.status — never prisma.upsert. CONFIRMED rows never have .amount changed; drift triggers status transition to ADJUSTED instead. |
| T-12-08 | Repudiation | Drift between recomputed and stored settlement amount goes silent | mitigate | When status === CONFIRMED and drift detected, the row is marked ADJUSTED with adjustmentReason 'auto: drift detected by snapshot' AND a Winston warn log is emitted with structured old/new amounts. |
| T-12-09 | Denial of service | Commission worker runs before DrawFinancialProvider rows exist (Pitfall 7) | mitigate | Worker checks DrawFinancial.totalizedAt at entry; if null, throws DrawFinancialNotReadyError. pg-boss retries 3× with backoff (retryDelay 5s, retryBackoff true) — DrawFinancial PRIZES will commit within that window. |
| T-12-10 | Information disclosure | configSnapshot Json exposing rates/tier values to ledger consumers | accept | configSnapshot is intentionally denormalized for reproducibility (locked in CONTEXT.md). Same trust surface as the source ProviderCommissionConfig row. |
| T-12-11 | Elevation of privilege | Cron-triggered worker payload tampering | accept | trigger-pgboss-cron.mjs is root-owned on the VPS and runs from /etc/cron.d/tote-triggers; if root is compromised, the entire box is. Plan 12-03 adds the queue to the allowlist. |
</threat_model>

<verification>
After Plan 12-02 completes:
1. cd backend ; node --experimental-vm-modules node_modules/.bin/jest src/services/__tests__/commission.service.test.js exits 0.
2. cd backend ; node --experimental-vm-modules node_modules/.bin/jest src/queue/workers/__tests__/calculate-provider-commission.worker.test.js src/queue/workers/__tests__/weekly-settlement-snapshot.worker.test.js exits 0.
3. After backend boot, docker exec tote_postgres psql -U tote_user -d tote_db -tAc "SELECT COUNT(*) FROM pgboss.queue WHERE name IN ('calculate-provider-commission','weekly-settlement-snapshot')" returns 2.
4. grep gates on commission.service.js, both worker files, constants.js, and register.js all pass.
</verification>

<success_criteria>
- All 9 service functions exported and tested.
- All 4 formula types compute correctly with decimal.js ROUND_HALF_UP.
- D-01 silent-skip writes no row and logs warning.
- D-04 multi-draw cumulative summation correct.
- Append-only assertion (no prisma.providerCommissionConfig.update) verified.
- Race-guard worker behavior proven via unit test (throws / succeeds based on DrawFinancial.totalizedAt).
- Snapshot worker honors D-03 freeze and D-02 drift detection.
- Both new queues exist in pgboss.queue at runtime.
</success_criteria>

<output>
After completion, create .planning/phases/12-provider-commission-engine/12-02-SUMMARY.md with: named-export contract for commission.service.js (signatures + return shapes), the race-guard and state-conditional logic of the two workers, the constants.js / register.js changes (exact line locations), and a note that Plan 12-03 will (a) wire boss.send into step-process-prizes.worker.js as the third parallel send, (b) add 'weekly-settlement-snapshot' to the ALLOWED_QUEUES set in trigger-pgboss-cron.mjs, and (c) create the commission.routes.js + commission.controller.js for admin CRUD and exports.
</output>
</content>
</invoke>