---
phase: 12
plan: 3
type: execute
wave: 3
depends_on: [2]
files_modified:
  - backend/src/queue/workers/step-process-prizes.worker.js
  - backend/src/scripts/trigger-pgboss-cron.mjs
  - backend/src/controllers/commission.controller.js
  - backend/src/routes/commission.routes.js
  - backend/src/index.js
  - backend/src/controllers/__tests__/commission.controller.test.js
  - backend/src/__tests__/commission-pipeline.integration.test.js
autonomous: true
requirements:
  - FIN-COMM-05
  - FIN-COMM-06
  - FIN-COMM-07
  - FIN-COMM-08
  - FIN-COMM-09
  - FIN-COMM-10
  - FIN-COMM-11
user_setup: []
deferred_items:
  - "Provider self-service endpoint (backlog)"
  - "Un-confirm endpoint with multi-sign approval (backlog)"

must_haves:
  truths:
    - "step-process-prizes.worker.js calls boss.send(CALCULATE_PROVIDER_COMMISSION, {drawId}) as the THIRD parallel send in BOTH the already-processed branch (around line 23) AND the main branch (around line 50) — matching the existing two sends to STEP_CALCULATE_STATS and CALCULATE_DRAW_FINANCIALS"
    - "trigger-pgboss-cron.mjs ALLOWED_QUEUES set includes 'weekly-settlement-snapshot'"
    - "commission.controller.js exports a CommissionController class with listConfigs, createConfig, getLedger, getSettlements, getSettlementDetail, confirmSettlement, adjustSettlement, exportSettlementExcel, exportSettlementPdf"
    - "commission.controller.js has NO updateConfig method and NO endpoint accepting PUT or DELETE on configs (F-5 append-only)"
    - "createConfig validates per-formula required fields (SALES_PCT requires salesRate; UTILITY_PCT requires utilityRate; SALES_AND_UTILITY_PCT requires both; TIERED requires non-empty tiers array)"
    - "confirmSettlement rejects with 400 if current status is not DRAFT (D-03 immutability — backend enforces, not just UI)"
    - "confirmSettlement writes an AuditLog row with action 'SETTLEMENT_CONFIRMED' (reuse existing AuditLog model per A4)"
    - "adjustSettlement requires { amount, adjustmentReason } body and snapshots existing amount into originalAmount (D-02 path 1)"
    - "adjustSettlement writes an AuditLog row with action 'SETTLEMENT_ADJUSTED'"
    - "commission.routes.js gates ALL routes behind authenticate + authorize('ADMIN') at the router level"
    - "commission.routes.js mounts at /api/commissions in backend/src/index.js"
    - "Integration test against the local mirror DB proves a sample draw + provider + config + DrawFinancialProvider produces a ProviderCommissionLedger row with the correct computed amount through the boss.send chain"
  artifacts:
    - path: "backend/src/queue/workers/step-process-prizes.worker.js"
      provides: "Third parallel boss.send for CALCULATE_PROVIDER_COMMISSION in both branches"
      contains: "CALCULATE_PROVIDER_COMMISSION"
    - path: "backend/src/scripts/trigger-pgboss-cron.mjs"
      provides: "ALLOWED_QUEUES allowlist extended with 'weekly-settlement-snapshot'"
      contains: "weekly-settlement-snapshot"
    - path: "backend/src/controllers/commission.controller.js"
      provides: "Express request handlers — config CRUD (create + list only), ledger query, settlement CRUD + state transitions + Excel/PDF export"
      contains: "confirmSettlement"
      min_lines: 200
    - path: "backend/src/routes/commission.routes.js"
      provides: "Express router mounted at /api/commissions with authenticate + authorize('ADMIN') guard"
      contains: "authorize('ADMIN')"
    - path: "backend/src/index.js"
      provides: "Mounts commission.routes.js"
      contains: "commission.routes"
    - path: "backend/src/__tests__/commission-pipeline.integration.test.js"
      provides: "End-to-end test against live local DB — seeds a draw + DrawFinancialProvider + ProviderCommissionConfig, calls the chained worker, asserts ledger row appears with the expected amount"
      min_lines: 80
  key_links:
    - from: "step-process-prizes.worker.js"
      to: "calculate-provider-commission queue (via boss.send)"
      via: "Third parallel send alongside STEP_CALCULATE_STATS and CALCULATE_DRAW_FINANCIALS"
      pattern: "QUEUES.CALCULATE_PROVIDER_COMMISSION"
    - from: "trigger-pgboss-cron.mjs ALLOWED_QUEUES set"
      to: "weekly-settlement-snapshot queue"
      via: "Allowlist entry"
      pattern: "weekly-settlement-snapshot"
    - from: "commission.routes.js"
      to: "auth.middleware.js (authenticate, authorize('ADMIN'))"
      via: "router.use at top level"
      pattern: "authorize\\('ADMIN'\\)"
    - from: "backend/src/index.js"
      to: "commission.routes.js"
      via: "app.use('/api/commissions', commissionRoutes)"
      pattern: "/api/commissions"
---

<objective>
Wire the compute layer from Plan 12-02 into the live system. Three coordinated edits to existing files (step-process-prizes.worker.js gets the third parallel boss.send, trigger-pgboss-cron.mjs gets the snapshot queue in its allowlist, backend/src/index.js mounts the new routes); two new Express files (commission.controller.js + commission.routes.js) implementing config CRUD (create + list only — F-5), ledger queries, settlement state transitions with backend-enforced immutability and AuditLog writes, and Excel/PDF export streaming; one integration test against the live local mirror DB that seeds a complete scenario and asserts the chained pipeline produces the expected ledger row.

Purpose: This is the plan that makes commissions actually compute when a draw totalizes. The boss.send wiring in step-process-prizes.worker.js is the load-bearing single line — without it the worker registered in Plan 12-02 never receives a job. The route layer is the only path admins use to interact with the engine until Plan 12-04 ships the UI. The integration test guards against regressions in any of the chained pieces (Phase 11 → Phase 12 → ledger).

Output: 3 modified files + 2 new files for the runtime path; 1 new controller test + 1 integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-provider-commission-engine/12-CONTEXT.md
@.planning/phases/12-provider-commission-engine/12-RESEARCH.md
@.planning/phases/12-provider-commission-engine/12-PATTERNS.md
@.planning/phases/12-provider-commission-engine/12-01-SUMMARY.md
@.planning/phases/12-provider-commission-engine/12-02-SUMMARY.md
@.planning/phases/11-drawfinancial-foundation/11-03-SUMMARY.md
@./CLAUDE.md

<interfaces>
Pipeline anchor (read FULL):
- backend/src/queue/workers/step-process-prizes.worker.js — line 19-23 already-processed branch shows the existing STEP_CALCULATE_STATS boss.send; lines 25-29 show the CALCULATE_DRAW_FINANCIALS send (Phase 11). Lines 50-53 are the main-branch STATS send; lines 55-59 are the main-branch CALCULATE_DRAW_FINANCIALS send (Phase 11). The NEW Phase 12 third send goes IMMEDIATELY AFTER lines 28-29 in branch 1 AND IMMEDIATELY AFTER lines 56-59 in branch 2.

Cron allowlist anchor:
- backend/src/scripts/trigger-pgboss-cron.mjs lines 32-45 — ALLOWED_QUEUES set. Currently ends with 'cleanup-logs' at line 44. Add 'weekly-settlement-snapshot' as a new entry.

Controller analog (read FULL):
- backend/src/controllers/provider.controller.js — class-style controller with JWT auth assumed; export default new ProviderController(); async methods with try/catch + res.json({ success: true, data }) on happy path and res.status(500).json({ success: false, error }) on error.
- backend/src/controllers/admin-jobs.controller.js lines 100-142 — AuditLog write pattern; entity/entityId/userId/changes Json/ipAddress/userAgent fields. Copy this exact shape for SETTLEMENT_CONFIRMED and SETTLEMENT_ADJUSTED actions.
- backend/src/controllers/monitor.controller.js lines 130-220 — PDFKit streaming: bufferPages: true; Content-Disposition attachment; doc.pipe(res); drawTable helper at lines 141-157 — lift the helper inline into commission.controller.js. Currency formatter via Intl.NumberFormat('es-VE', { style: 'currency', currency: 'VES' }).

Route analog (read FULL):
- backend/src/routes/provider.routes.js — router.use(authenticate, authorize('ADMIN')) single line covers all endpoints. Controller methods bound via .bind(controllerInstance).

Index mount anchor:
- backend/src/index.js — locate the existing route mount block (provider.routes.js is mounted somewhere around the other admin routes). The new commission routes mount alongside.

Service exports from Plan 12-02:
- backend/src/services/commission.service.js exports buildSettlementExcel, getSettlementWithLedger, getSettlementPdfData, computeAndUpsertLedgerForDraw, computeSettlementForWeek.

Constants from Plan 12-01/02:
- QUEUES.CALCULATE_PROVIDER_COMMISSION exists in constants.js; QUEUE_CONFIGS[QUEUES.CALCULATE_PROVIDER_COMMISSION] exists.

Schema accessors from Plan 12-01:
- prisma.providerCommissionConfig, prisma.providerCommissionTier, prisma.providerCommissionLedger, prisma.providerWeeklySettlement, prisma.auditLog (existing).

Local test DB credentials (from CLAUDE.md): tote_postgres on port 5433, user tote_user, password tote_password_2025, database tote_db. ~660k TicketDetail rows present (prod-mirror).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the third parallel boss.send to step-process-prizes.worker.js (both branches) and extend trigger-pgboss-cron.mjs ALLOWED_QUEUES</name>
  <files>
    backend/src/queue/workers/step-process-prizes.worker.js
    backend/src/scripts/trigger-pgboss-cron.mjs
  </files>
  <read_first>
    - backend/src/queue/workers/step-process-prizes.worker.js (FULL — find both branches: the already-processed branch starting around line 17 with existing STEP_CALCULATE_STATS + CALCULATE_DRAW_FINANCIALS sends; the main branch starting around line 45 with the same two sends. Imports at lines 4-5 already cover getBoss, QUEUES, QUEUE_CONFIGS.)
    - backend/src/scripts/trigger-pgboss-cron.mjs (FULL — confirm ALLOWED_QUEUES Set declaration at lines 32-45 ending with 'cleanup-logs')
    - .planning/phases/12-provider-commission-engine/12-PATTERNS.md sections 7 (step-process-prizes surgical add) and 8 (trigger-pgboss-cron allowlist)
    - .planning/phases/12-provider-commission-engine/12-RESEARCH.md Pattern 5 (rationale for chaining off step-process-prizes — NOT off calculate-draw-financials completion; race condition mitigated in the commission worker itself per Plan 12-02 Pitfall 7)
  </read_first>
  <action>
    Step A — Modify backend/src/queue/workers/step-process-prizes.worker.js.

    Two surgical additions, ONE in each branch. Both additions are immediately AFTER the existing Phase 11 CALCULATE_DRAW_FINANCIALS send block in their respective branch.

    The new send block (added in both branches) calls boss.send(QUEUES.CALCULATE_PROVIDER_COMMISSION, { drawId }, { singletonKey: `comm-${drawId}`, ...QUEUE_CONFIGS[QUEUES.CALCULATE_PROVIDER_COMMISSION] }). Use the exact same options-object shape as the existing STEP_CALCULATE_STATS / CALCULATE_DRAW_FINANCIALS sends (singletonKey prefix changes to "comm-"; payload is just { drawId } — no phase needed because the commission worker handles only one phase).

    Add a leading comment line above both insertions: "// Phase 12: parallel-trigger provider commission. Worker has DrawFinancialNotReadyError race-guard (Pitfall 7) — pg-boss retries 3× with backoff if PRIZES has not committed."

    NO try/catch wrapping needed (mirrors Phase 11 convention — failures bubble; pg-boss retries the parent worker).

    NO new imports needed (getBoss, QUEUES, QUEUE_CONFIGS already imported at lines 4-5).

    Step B — Modify backend/src/scripts/trigger-pgboss-cron.mjs.

    In the ALLOWED_QUEUES Set declaration (lines 32-45), add 'weekly-settlement-snapshot' as a new line entry inside the Set after 'cleanup-logs' at line 44. Add a comment line above it: "// Phase 12 — weekly settlement snapshot, fired Monday 06:00 VE via /etc/cron.d/tote-triggers".

    DO NOT touch any other entry in this file. DO NOT remove the FATAL guard at line 47-49 — that guard is what makes the allowlist meaningful.
  </action>
  <verify>
    <automated>grep -F "QUEUES.CALCULATE_PROVIDER_COMMISSION" backend/src/queue/workers/step-process-prizes.worker.js | wc -l | grep -qE '^[[:space:]]*2$' &amp;&amp; grep -F "'weekly-settlement-snapshot'" backend/src/scripts/trigger-pgboss-cron.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "QUEUES.CALCULATE_PROVIDER_COMMISSION" backend/src/queue/workers/step-process-prizes.worker.js returns at least 2 (one send in each branch).
    - grep -c "singletonKey: \`comm-" backend/src/queue/workers/step-process-prizes.worker.js returns at least 2.
    - grep -F "QUEUES.STEP_CALCULATE_STATS" backend/src/queue/workers/step-process-prizes.worker.js still returns at least 2 matches (the original sends are preserved).
    - grep -F "QUEUES.CALCULATE_DRAW_FINANCIALS" backend/src/queue/workers/step-process-prizes.worker.js still returns at least 2 matches (Phase 11 sends are preserved).
    - grep -F "'weekly-settlement-snapshot'" backend/src/scripts/trigger-pgboss-cron.mjs returns a match.
    - The ALLOWED_QUEUES set in trigger-pgboss-cron.mjs still contains every original entry: grep -cE "'(close-and-ingest-sweep|preselect-sweep|execute-draw-sweep|sync-api-tickets|sync-api-planning|sync-scrape-tickets|generate-daily-draws|retry-failed-publications|monitor-dlq|cleanup-logs|weekly-settlement-snapshot)'" backend/src/scripts/trigger-pgboss-cron.mjs returns 11.
  </acceptance_criteria>
  <done>
    Both branches of step-process-prizes.worker.js have the third parallel boss.send, trigger-pgboss-cron.mjs allowlist includes the snapshot queue, no other entries removed or modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create commission.controller.js + commission.routes.js, mount at /api/commissions in backend/src/index.js, with backend-enforced state transitions and AuditLog writes; controller unit tests</name>
  <files>
    backend/src/controllers/commission.controller.js
    backend/src/routes/commission.routes.js
    backend/src/index.js
    backend/src/controllers/__tests__/commission.controller.test.js
  </files>
  <read_first>
    - backend/src/controllers/provider.controller.js (FULL — class shape, default export instance, async method signatures, try/catch+res.json + res.status(500).json error envelope, req.user / req.ip / req.headers usage)
    - backend/src/controllers/admin-jobs.controller.js lines 100-142 (AuditLog write — entity/entityId/userId/changes Json/ipAddress/userAgent — copy this exact shape for SETTLEMENT_CONFIRMED and SETTLEMENT_ADJUSTED)
    - backend/src/controllers/monitor.controller.js lines 130-220 (PDFKit setup, drawTable helper at lines 141-157, currency formatter, doc.pipe(res), Content-Disposition attachment header)
    - backend/src/routes/provider.routes.js (FULL — top-level router.use(authenticate, authorize('ADMIN')); .bind(controller) on each route)
    - backend/src/middlewares/auth.middleware.js (FULL — confirms authenticate + authorize('ADMIN') signatures)
    - backend/src/index.js (FULL — locate the existing route-mount block for /api/providers or similar admin routes; the new /api/commissions mount goes alongside)
    - backend/src/services/commission.service.js (Plan 12-02 — confirm exports buildSettlementExcel, getSettlementWithLedger, getSettlementPdfData, computeSettlementForWeek, computeAndUpsertLedgerForDraw, findEffectiveConfig)
    - .planning/phases/12-provider-commission-engine/12-PATTERNS.md sections 10 and 11 (full controller + route sketches), and section 9 "Don't Hand-Roll: AuditLog" / Pattern 6 (settlement state machine)
    - .planning/phases/12-provider-commission-engine/12-RESEARCH.md Pattern 6 (settlement state machine — backend enforcement, not just UI)
    - .planning/phases/12-provider-commission-engine/12-CONTEXT.md D-02 (originalAmount snapshot on adjust), D-03 (CONFIRMED terminal — backend rejects status writes), A4 (reuse AuditLog model)
  </read_first>
  <behavior>
    - POST /api/commissions/configs with formulaType SALES_PCT and no salesRate → 400 validation error "salesRate required for SALES_PCT"
    - POST /api/commissions/configs with formulaType TIERED and empty tiers array → 400 validation error "tiers array required for TIERED"
    - POST /api/commissions/configs with valid SALES_PCT { apiSystemId, formulaType:'SALES_PCT', salesRate:5.5, effectiveFrom:'2026-05-15T00:00:00Z' } → 200 with new config row; subsequent GET /api/commissions/configs/{apiSystemId} returns it in the list ordered by effectiveFrom desc
    - There is NO PUT or DELETE route for /api/commissions/configs (F-5 — append-only)
    - PATCH /api/commissions/settlements/:id/confirm on a DRAFT row → 200, settlement.status becomes CONFIRMED, confirmedAt + confirmedById populated, AuditLog row created with action 'SETTLEMENT_CONFIRMED', entity 'ProviderWeeklySettlement', entityId = the settlement id, changes Json contains previousStatus + newStatus + amount
    - PATCH /api/commissions/settlements/:id/confirm on a CONFIRMED row → 400 "Settlement is not in DRAFT status" (D-03 backend immutability)
    - PATCH /api/commissions/settlements/:id/confirm on an ADJUSTED row → 400 (same — only DRAFT may transition to CONFIRMED)
    - PATCH /api/commissions/settlements/:id/adjust with body { amount: '999.50000000', adjustmentReason: 'manual override per audit' } on a CONFIRMED row → 200, settlement.amount becomes 999.50000000, originalAmount stores the previous amount (D-02 path 1), status becomes ADJUSTED, adjustmentReason stored; AuditLog row created with action 'SETTLEMENT_ADJUSTED'
    - PATCH /api/commissions/settlements/:id/adjust without adjustmentReason in body → 400 "adjustmentReason required"
    - All routes return 401 without an auth token; all routes return 403 if the user role is not ADMIN
  </behavior>
  <action>
    Step A — Create backend/src/controllers/commission.controller.js as a class with named methods + default export new CommissionController().

    Imports: prisma from '../lib/prisma.js'; logger from '../lib/logger.js'; import * as commissionService from '../services/commission.service.js'.

    Methods:

    listConfigs(req, res): from req.params.apiSystemId, prisma.providerCommissionConfig.findMany where apiSystemId, include tiers ordered by minSales asc, orderBy effectiveFrom desc. Returns { success: true, data }.

    createConfig(req, res): body { apiSystemId, formulaType, salesRate?, utilityRate?, effectiveFrom, notes?, tiers? }. Validate: formulaType in { SALES_PCT, UTILITY_PCT, SALES_AND_UTILITY_PCT, TIERED }. For SALES_PCT require salesRate (non-null, between 0 and 100). For UTILITY_PCT require utilityRate same range. For SALES_AND_UTILITY_PCT require BOTH. For TIERED require Array.isArray(tiers) && tiers.length > 0; validate each tier { minSales (Decimal-coercible >= 0), maxSales (null OR Decimal-coercible > minSales), rate (between 0 and 100) }. Reject with 400 on any failure. On success, prisma.providerCommissionConfig.create with data including createdById: req.user?.id and nested tiers create-many when TIERED. Returns 201 + the new row with tiers included.

    getLedger(req, res): query params apiSystemId (optional), from (optional), to (optional). prisma.providerCommissionLedger.findMany where { apiSystemId?: ..., createdAt?: { gte: from, lte: to } } include apiSystem (select name, slug) + draw (select drawDate, drawTime, drawnAt), orderBy createdAt desc, take 200. Returns { success: true, data }.

    getSettlements(req, res): query params isoYear (optional), isoWeek (optional), apiSystemId (optional), status (optional). prisma.providerWeeklySettlement.findMany with where filter built from provided params, include apiSystem, orderBy [{ isoYear: 'desc' }, { isoWeek: 'desc' }], take 200.

    getSettlementDetail(req, res): from req.params.id, delegate to commissionService.getSettlementWithLedger(id). Return 404 if not found.

    confirmSettlement(req, res): from req.params.id, fetch the settlement; if not found return 404; if settlement.status !== 'DRAFT' return 400 with body { success: false, error: 'Settlement is not in DRAFT status' }. Else prisma.providerWeeklySettlement.update id with { status: 'CONFIRMED', confirmedAt: new Date(), confirmedById: req.user?.id }. Then prisma.auditLog.create with { action: 'SETTLEMENT_CONFIRMED', entity: 'ProviderWeeklySettlement', entityId: id, userId: req.user?.id ?? null, ipAddress: req.ip, userAgent: req.headers['user-agent'], changes: { previousStatus: 'DRAFT', newStatus: 'CONFIRMED', amount: updated.amount.toString() } }. The audit write MUST block (no .catch swallow) — financial trust per D-03.

    adjustSettlement(req, res): from req.params.id, body { amount, adjustmentReason }. Validate amount is Decimal-coercible and adjustmentReason is a non-empty string (>=10 chars suggested but only require non-empty). Fetch settlement; if not found 404; if status not in { CONFIRMED, ADJUSTED } return 400 "Adjustment requires CONFIRMED or ADJUSTED status". Update with { amount, originalAmount: existing.amount, adjustmentReason, status: 'ADJUSTED' }. Write AuditLog with action 'SETTLEMENT_ADJUSTED' and changes Json including previousAmount + newAmount + reason.

    exportSettlementExcel(req, res): const buffer = await commissionService.buildSettlementExcel(req.params.id). Set Content-Type application/vnd.openxmlformats-officedocument.spreadsheetml.sheet, Content-Disposition attachment with filename `liquidacion-${tag}.xlsx`. res.send(buffer).

    exportSettlementPdf(req, res): const data = await commissionService.getSettlementPdfData(req.params.id). const PDFDocument = (await import('pdfkit')).default. Instantiate with size: 'LETTER', margins, bufferPages: true. Set Content-Type application/pdf, Content-Disposition attachment with filename `liquidacion-${tag}-${slug}.pdf`. doc.pipe(res). Render title "LIQUIDACIÓN DE COMISIÓN", subtitle "${apiSystem.name} — ${isoYear}-W${pad(isoWeek)}", horizontal rule, totals block, table of ledger rows using the drawTable helper lifted inline from monitor.controller.js lines 141-157. Currency formatter Intl.NumberFormat('es-VE', { style: 'currency', currency: 'VES' }). doc.end().

    Export default new CommissionController().

    Step B — Create backend/src/routes/commission.routes.js.

    Imports: express; commissionController from '../controllers/commission.controller.js'; { authenticate, authorize } from '../middlewares/auth.middleware.js'.

    const router = express.Router(). router.use(authenticate, authorize('ADMIN')). Define routes:
    - GET /configs/:apiSystemId → listConfigs.bind
    - POST /configs → createConfig.bind
    - GET /ledger → getLedger.bind
    - GET /settlements → getSettlements.bind
    - GET /settlements/:id → getSettlementDetail.bind
    - PATCH /settlements/:id/confirm → confirmSettlement.bind
    - PATCH /settlements/:id/adjust → adjustSettlement.bind
    - GET /settlements/:id/excel → exportSettlementExcel.bind
    - GET /settlements/:id/pdf → exportSettlementPdf.bind

    NO PUT or DELETE routes for /configs (F-5 enforcement at the routing layer too).

    export default router.

    Step C — Modify backend/src/index.js. Locate the existing block where other admin routes are mounted (e.g. app.use('/api/providers', providerRoutes)). Mount commissionRoutes at /api/commissions alongside. Add the import at the top of the file with the other route imports.

    Step D — Create backend/src/controllers/__tests__/commission.controller.test.js. Mock prisma + auth middleware. Test the 11 behaviors above using supertest or direct method invocation (mirror the test approach used for provider.controller.test.js or admin-jobs.controller.test.js — read whichever exists to confirm the project's controller-test style).

    DO NOT modify any other files. DO NOT introduce a PUT or DELETE /configs route under any condition.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -F "/api/commissions" src/index.js &amp;&amp; grep -F "authorize('ADMIN')" src/routes/commission.routes.js &amp;&amp; node --experimental-vm-modules node_modules/.bin/jest src/controllers/__tests__/commission.controller.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -F "app.use('/api/commissions'" backend/src/index.js returns a match.
    - grep -F "import commissionRoutes" backend/src/index.js returns a match (or equivalent import line).
    - grep -F "authorize('ADMIN')" backend/src/routes/commission.routes.js returns a match.
    - grep -cE "router\.(put|delete)\(['\"]\/configs" backend/src/routes/commission.routes.js returns 0 (F-5 — no PUT/DELETE on configs).
    - grep -cE "router\.(get|post|patch)\(" backend/src/routes/commission.routes.js returns at least 9 (one per method listed above).
    - grep -F "updateConfig" backend/src/controllers/commission.controller.js returns NO match (F-5).
    - grep -F "'SETTLEMENT_CONFIRMED'" backend/src/controllers/commission.controller.js returns at least 1 match.
    - grep -F "'SETTLEMENT_ADJUSTED'" backend/src/controllers/commission.controller.js returns at least 1 match.
    - grep -F "originalAmount: existing.amount" backend/src/controllers/commission.controller.js returns a match (D-02 path 1 snapshot).
    - grep -F "Settlement is not in DRAFT" backend/src/controllers/commission.controller.js returns a match (D-03 backend enforcement).
    - Jest run of commission.controller.test.js passes.
    - Booting the backend in dev mode and running `curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3001/api/commissions/settlements` returns 401 (auth gate is live).
  </acceptance_criteria>
  <done>
    Routes mounted, all 9 endpoints exist, F-5 / D-02 / D-03 all enforced at the controller layer, AuditLog written on both transitions, auth gate live, controller tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration test against the live local prod-mirror DB — seed → boss.send → assert ledger row</name>
  <files>
    backend/src/__tests__/commission-pipeline.integration.test.js
  </files>
  <read_first>
    - .planning/phases/11-drawfinancial-foundation/11-03-SUMMARY.md (Phase 11 integration test patterns against the live local DB — uses a separate Jest config or jest.setEnv to point at the docker DB; mirror that approach)
    - backend/src/__tests__/ (list the directory — if a Phase 11 integration test like draw-financial-pipeline.integration.test.js exists, read it FULL to copy the harness)
    - backend/src/services/commission.service.js (Plan 12-02 — confirm computeAndUpsertLedgerForDraw signature)
    - backend/src/queue/workers/calculate-provider-commission.worker.js (Plan 12-02 — confirm the handler signature)
    - .planning/phases/12-provider-commission-engine/12-RESEARCH.md Pitfall 8 (DrawFinancial vs DrawFinancialProvider sum consistency — the integration test should reuse the Phase 11 assertion that SUM(DrawFinancialProvider.totalSales) === DrawFinancial.totalSales)
    - ./CLAUDE.md (docker tote_postgres on port 5433; ~660k TicketDetail rows present — seed a NEW draw with a unique UUID rather than relying on any existing draw to keep the test deterministic and re-runnable)
  </read_first>
  <action>
    Create backend/src/__tests__/commission-pipeline.integration.test.js. Mirror the Phase 11 integration test harness — direct prisma calls against the live local DB, no mocks, with a beforeEach that wipes the test fixtures by ID prefix and an afterAll that disconnects.

    Test scenario:
    1. beforeAll: confirm the local DB is reachable; verify Plan 12-01 tables exist via prisma.providerCommissionLedger.count() returning a number (proves runtime accessor + table).
    2. Test 1 — "happy path SALES_PCT with chained boss.send":
       - Seed a Game (use the LOTOANIMALITO game id from CLAUDE.md d953f80c-4335-4bc9-9f78-9b56193286fe — it already exists; just reference its id).
       - Create a unique Draw with status DRAWN, prizesProcessed true, drawnAt = a fixed past timestamp (e.g. 2026-05-01T18:00:00-04:00) so it is well past COMMISSION_GO_LIVE and inside a deterministic ISO week.
       - Create an ApiSystem fixture for this test (slug 'integration-test-provider-{uuid}') OR reuse an existing one if SRQ exists. Locate via prisma.apiSystem.findFirst — if no testing fixture exists, prisma.apiSystem.create with mode 'PUSH'.
       - Create a ProviderCommissionConfig: { apiSystemId, formulaType: 'SALES_PCT', salesRate: '5.5000', effectiveFrom: a date before draw.drawnAt }.
       - Create a DrawFinancial row { drawId, totalSales: '1000.00', totalPrize: '200.00', utility: '800.00', ticketCount: 10, closedAt: ..., totalizedAt: a non-null date — this satisfies the worker race-guard }.
       - Create a DrawFinancialProvider row { drawId, apiSystemId, totalSales: '1000.00', totalPrize: '200.00', ticketCount: 10 }.
       - Invoke computeAndUpsertLedgerForDraw(drawId) directly (skipping pg-boss for determinism — pg-boss already tested in Plan 12-02 worker test; the integration test's job is to prove the service+schema path works against real data).
       - Assert: prisma.providerCommissionLedger.findFirst where { drawId, apiSystemId } returns a row with amount === '55.00000000' (5.5% of 1000 = 55), salesBase === '1000.00000000', utilityBase === '800.00000000', configSnapshot.formulaType === 'SALES_PCT', configSnapshot.salesRate === '5.5000'.
    3. Test 2 — "D-01 silent skip":
       - Same draw setup as Test 1 BUT do NOT create a ProviderCommissionConfig.
       - Invoke computeAndUpsertLedgerForDraw(drawId).
       - Assert: prisma.providerCommissionLedger.count where { drawId, apiSystemId } returns 0 (no row written).
       - Assert: the function return value has skipped >= 1.
    4. Test 3 — "idempotent re-run":
       - Re-invoke computeAndUpsertLedgerForDraw(drawId) from Test 1 a second time.
       - Assert: prisma.providerCommissionLedger.count where { drawId, apiSystemId } still returns exactly 1 (upsert, not duplicate).
    5. Test 4 — "DrawFinancialNotReadyError race-guard":
       - Set DrawFinancial.totalizedAt = null for the test draw.
       - Invoke calculateProviderCommissionWorker({ data: { drawId } }) directly (passing a single mock job object — the worker handles both array and singleton inputs).
       - Assert: the call throws an error whose .name === 'DrawFinancialNotReadyError'.
    6. afterAll: cleanup — delete the seeded ledger rows, the seeded DrawFinancialProvider, DrawFinancial, config, draw, and (if newly created) the apiSystem. Use prisma.$transaction for atomic cleanup. Always disconnect.

    The integration test runs against the docker DB (port 5433) — confirm DATABASE_URL in the test environment points at localhost:5433 (mirror the Phase 11 integration test harness for env loading).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; node --experimental-vm-modules node_modules/.bin/jest src/__tests__/commission-pipeline.integration.test.js --forceExit</automated>
  </verify>
  <acceptance_criteria>
    - The integration test file exists and runs against the docker DB on port 5433.
    - All 4 tests pass.
    - Test 1 asserts ledger.amount === '55.00000000' exactly (no float drift).
    - Test 2 asserts no ledger row exists when config is missing (D-01).
    - Test 3 asserts re-run produces exactly 1 row, not 2 (idempotent upsert).
    - Test 4 asserts the worker throws DrawFinancialNotReadyError when totalizedAt is null.
    - The afterAll cleanup leaves the DB in its prior state (run prisma.providerCommissionLedger.count() before and after the test — must be equal — or use a unique-prefixed apiSystem slug that is cleaned).
    - No test pollution: re-running the integration test twice in a row both passes (idempotent harness).
  </acceptance_criteria>
  <done>
    Integration test proves the schema + service + worker chain works against the live local DB; idempotent harness; cleanup leaves DB intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser) → /api/commissions/* | HTTPS + JWT (authenticate) + role check (authorize('ADMIN')) — same as every other admin route. |
| Controller → AuditLog | Audit write is internal but inputs (req.ip, req.headers, req.user.id) come from the request — validated by auth middleware before reaching the controller. |
| Cron Linux (root) → trigger-pgboss-cron.mjs → pg-boss | Allowlist check is the boundary; only queues in ALLOWED_QUEUES can be enqueued. |
| pg-boss → step-process-prizes worker → boss.send(...COMMISSION) | Internal — drawId originates from the existing pipeline which is already trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Spoofing | Caller asserts admin role without token | mitigate | router.use(authenticate, authorize('ADMIN')) at top of commission.routes.js — same pattern as provider.routes.js. |
| T-12-13 | Tampering | Client submits PUT or DELETE to /configs hoping to bypass append-only | mitigate | No PUT/DELETE route defined; Express returns 404 for undeclared methods. F-5 acceptance criterion gates the route file. |
| T-12-14 | Tampering | Client submits PATCH /settlements/:id/confirm on already-CONFIRMED row | mitigate | Controller checks current status before update and returns 400 with "Settlement is not in DRAFT status". Backend enforces, not just UI (D-03). |
| T-12-15 | Repudiation | State transition without audit | mitigate | confirmSettlement and adjustSettlement both write AuditLog rows synchronously (no .catch swallow) before returning success. Block on the write — financial trust. |
| T-12-16 | Tampering | adjustSettlement accepts arbitrary amount without reason | mitigate | Controller validates adjustmentReason is a non-empty string before accepting the request; returns 400 otherwise. |
| T-12-17 | Elevation of privilege | Cron Linux runs trigger-pgboss-cron.mjs as root → could enqueue arbitrary queue | mitigate | ALLOWED_QUEUES set in trigger-pgboss-cron.mjs limits which queues an attacker-with-cron-edit-rights can enqueue; the FATAL guard at line 47-49 aborts with non-zero exit code on any unknown queue. |
| T-12-18 | Information disclosure | Excel/PDF export streams to non-admin caller | mitigate | The /excel and /pdf endpoints live under the router.use(authorize('ADMIN')) guard. |
</threat_model>

<verification>
After Plan 12-03 completes:
1. grep gates on step-process-prizes.worker.js (≥2 commission sends), trigger-pgboss-cron.mjs (snapshot in allowlist), commission.controller.js (no updateConfig, SETTLEMENT_CONFIRMED + ADJUSTED audit actions), commission.routes.js (no PUT/DELETE on /configs).
2. backend/src/index.js mounts /api/commissions.
3. Jest commission.controller.test.js passes all behaviors.
4. Jest commission-pipeline.integration.test.js passes all 4 tests against the live local DB.
5. curl -s http://localhost:3001/api/commissions/settlements returns 401 without auth header.
</verification>

<success_criteria>
- Pipeline trigger live: closing a draw + processing prizes will now enqueue the commission worker (verified by Test 4 in Plan 12-04's UAT).
- Cron-trigger allowlist includes the snapshot queue.
- All controller routes function with backend-enforced state machine and AuditLog writes.
- Integration test passes against live local DB with deterministic seed/cleanup.
- F-5 (no UPDATE on configs), D-02 (originalAmount on adjust), D-03 (CONFIRMED terminal) all enforced at the controller layer.
</success_criteria>

<output>
After completion, create .planning/phases/12-provider-commission-engine/12-03-SUMMARY.md with: the exact line locations of the two boss.send insertions in step-process-prizes.worker.js, the trigger-pgboss-cron.mjs allowlist diff, the full route table for /api/commissions/* (method + path + handler + auth), the integration test scenario summaries, and a note that Plan 12-04 will (a) build the admin UI consuming these routes, (b) implement the historical backfill script, (c) draft DEPLOY.md including the /etc/cron.d/tote-triggers line (LOCAL-ONLY this session — no VPS commands executed).
</output>
</content>
</invoke>