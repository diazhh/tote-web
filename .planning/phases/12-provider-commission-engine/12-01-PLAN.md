---
phase: 12
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/{timestamp}_add_provider_commission_models/migration.sql
  - backend/src/lib/dateUtils.js
  - backend/src/lib/__tests__/dateUtils.test.js
autonomous: true
requirements:
  - FIN-COMM-01
  - FIN-COMM-02
  - FIN-COMM-03
  - FIN-COMM-04
user_setup: []
deferred_items:
  - "Settlement → AccountingEntry FK (Phase 13 will add it)"
  - "Provider-portal viewing of own ledger (backlog)"

must_haves:
  truths:
    - "Prisma schema declares ProviderCommissionConfig, ProviderCommissionTier, ProviderCommissionLedger, ProviderWeeklySettlement models"
    - "All commission amount/base columns use Decimal @db.Decimal(18, 8) (NOT 12,2 — differs from Phase 11 per F-4)"
    - "Rate columns use Decimal @db.Decimal(15, 4)"
    - "CommissionFormulaType enum has values SALES_PCT, UTILITY_PCT, SALES_AND_UTILITY_PCT, TIERED (per FIN-COMM-01)"
    - "SettlementStatus enum has values DRAFT, CONFIRMED, ADJUSTED (per FIN-COMM-09)"
    - "ProviderCommissionConfig has @@index([apiSystemId, effectiveFrom(sort: Desc)])"
    - "ProviderCommissionLedger.apiSystemId is NOT NULL (differs from DrawFinancialProvider — per D-01 / DIFFER FROM)"
    - "ProviderCommissionLedger has @@unique([drawId, apiSystemId]) — one row per provider-draw"
    - "ProviderCommissionLedger has configSnapshot Json field for reproducibility"
    - "ProviderCommissionLedger has NO settlementId FK (per A8 — JOIN by drawnAt range)"
    - "ProviderWeeklySettlement has @@unique([apiSystemId, isoYear, isoWeek]) — per D-06"
    - "ProviderWeeklySettlement has originalAmount Decimal? and adjustmentReason String? — per D-02"
    - "Prisma migration applies cleanly to local DB on port 5433 — all 4 tables created"
    - "npx prisma generate runs successfully — runtime accessors for new models exist (Phase 11 lesson)"
    - "dateUtils.js exports getISOWeekVE, startOfISOWeekVE, endOfISOWeekVE using date-fns getISOWeekYear (NOT getFullYear) — F-15"
    - "F-15 unit tests pass for 2026-12-29 → (2026,53), 2027-01-01 → (2026,53), 2027-01-04 → (2027,1)"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "4 new commission models + 2 enums + back-relations on ApiSystem and Draw"
      contains: "model ProviderCommissionConfig"
    - path: "backend/prisma/migrations/{timestamp}_add_provider_commission_models/migration.sql"
      provides: "Committed SQL migration creating all 4 tables, both enums, all indexes and FKs"
      contains: "CREATE TABLE \"ProviderCommissionConfig\""
    - path: "backend/src/lib/dateUtils.js"
      provides: "getISOWeekVE, startOfISOWeekVE, endOfISOWeekVE helpers using date-fns ISO functions"
      contains: "getISOWeekYear"
    - path: "backend/src/lib/__tests__/dateUtils.test.js"
      provides: "F-15 ISO week boundary tests (3 critical dates)"
      min_lines: 30
  key_links:
    - from: "ProviderCommissionLedger.apiSystemId"
      to: "ApiSystem.id"
      via: "Required Prisma relation (NOT NULL)"
      pattern: "apiSystem\\s+ApiSystem\\s+@relation"
    - from: "ProviderCommissionConfig.tiers"
      to: "ProviderCommissionTier[]"
      via: "1:N relation, onDelete Cascade"
      pattern: "tiers\\s+ProviderCommissionTier"
    - from: "ApiSystem (existing model)"
      to: "ProviderCommissionConfig[], ProviderCommissionLedger[], ProviderWeeklySettlement[]"
      via: "Back-relations added to ApiSystem at ~line 448"
      pattern: "commissionConfigs\\s+ProviderCommissionConfig"
    - from: "Draw (existing model)"
      to: "ProviderCommissionLedger[]"
      via: "Back-relation added on Draw model"
      pattern: "commissionLedgerRows\\s+ProviderCommissionLedger"
---

<objective>
Persistence + VE ISO-week foundation. Four new Prisma models (ProviderCommissionConfig, ProviderCommissionTier, ProviderCommissionLedger, ProviderWeeklySettlement), two enums (CommissionFormulaType, SettlementStatus), back-relations on ApiSystem and Draw, migration applied locally, Prisma client regenerated, dateUtils.js extended with three VE ISO-week helpers and their F-15 boundary unit tests.

Purpose: Plans 12-02/03/04 all import from this schema and these date helpers. The append-only versioning model (effectiveFrom), (18,8) precision, configSnapshot Json, and (isoYear, isoWeek) unique key are load-bearing for downstream computation. F-15 ISO-week math is the most common silent-failure source for weekly settlements; locking helpers + tests here means workers can trust the input.

Output: schema additions, generated + committed migration SQL, Prisma client regenerated, dateUtils.js extended, Jest test file with 6 cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-provider-commission-engine/12-CONTEXT.md
@.planning/phases/12-provider-commission-engine/12-RESEARCH.md
@.planning/phases/12-provider-commission-engine/12-PATTERNS.md
@.planning/phases/11-drawfinancial-foundation/11-01-SUMMARY.md
@./CLAUDE.md

<interfaces>
Schema anchors (read backend/prisma/schema.prisma):
- model Draw at line 112 — add commissionLedgerRows ProviderCommissionLedger[] alongside Phase 11 financial DrawFinancial? and financialProviders DrawFinancialProvider[].
- model ApiSystem at lines 428-453 — add three back-relations alongside drawFinancials DrawFinancialProvider[] at line 448.
- model DrawFinancial / DrawFinancialProvider — analog. Phase 12 DIFFERS: (18,8) not (12,2); ledger.apiSystemId NOT NULL.
- enum ApiSystemMode at line 455 — enum style anchor.

dateUtils.js anchors (read backend/src/lib/dateUtils.js):
- date-fns import block at top.
- VENEZUELA_TIMEZONE constant — reuse, do not redefine.
- getVenezuelaDateString export — mirror its style.

date-fns@^4.1.0 + date-fns-tz@^3.2.0 already installed. No npm install needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add commission models + enums + back-relations to schema.prisma; extend dateUtils.js with VE ISO-week helpers</name>
  <files>
    backend/prisma/schema.prisma
    backend/src/lib/dateUtils.js
    backend/src/lib/__tests__/dateUtils.test.js
  </files>
  <read_first>
    - backend/prisma/schema.prisma (read in full to locate model Draw at line 112, model DrawFinancial + DrawFinancialProvider from Phase 11, model ApiSystem at lines 428-453, enum ApiSystemMode at line 455)
    - backend/src/lib/dateUtils.js (read in full — small file; confirm existing VENEZUELA_TIMEZONE and date-fns imports)
    - .planning/phases/12-provider-commission-engine/12-PATTERNS.md section 1 (full field-by-field model spec including configSnapshot Json, @@unique constraints, @@index for effective-config lookup) and section 12 (dateUtils helpers)
    - .planning/phases/12-provider-commission-engine/12-RESEARCH.md Pattern 4 (F-15 edge cases, Venezuela UTC-4 no-DST justification for +4h shift)
    - .planning/phases/12-provider-commission-engine/12-CONTEXT.md decisions D-02 (originalAmount + adjustmentReason), D-04 (TIERED weekly reset), D-06 (isoYear + isoWeek INT, unique constraint), and the CONTEXT statement on "Denormalize configSnapshot Json onto each ledger row" and "NO settlementId FK on ledger (per A8)"
    - .planning/phases/11-drawfinancial-foundation/11-01-SUMMARY.md (Phase 11 schema patterns; migration name convention; prisma generate lesson)
  </read_first>
  <behavior>
    - Test 1: getISOWeekVE(new Date('2026-12-29T12:00:00-04:00')) returns { isoYear: 2026, isoWeek: 53 }
    - Test 2: getISOWeekVE(new Date('2027-01-01T12:00:00-04:00')) returns { isoYear: 2026, isoWeek: 53 } (NOT 2027 — F-15 trap)
    - Test 3: getISOWeekVE(new Date('2027-01-04T12:00:00-04:00')) returns { isoYear: 2027, isoWeek: 1 }
    - Test 4: startOfISOWeekVE returns a Date whose UTC ISO string is Monday 04:00:00.000Z of the corresponding ISO week
    - Test 5: endOfISOWeekVE(d) is strictly greater than startOfISOWeekVE(d) and less than startOfISOWeekVE(d) + 7 days
    - Test 6: getISOWeekVE(d).isoWeek is between 1 and 53 inclusive for any d (never 0)
  </behavior>
  <action>
    Step A — schema.prisma additions.

    Two enums near enum ApiSystemMode (line 455):
    - CommissionFormulaType: SALES_PCT, UTILITY_PCT, SALES_AND_UTILITY_PCT, TIERED (FIN-COMM-01).
    - SettlementStatus: DRAFT, CONFIRMED, ADJUSTED (FIN-COMM-09 / D-02 / D-03).

    Four models grouped after Phase 11's DrawFinancialProvider model.

    ProviderCommissionConfig — append-only versioned config. Fields: id String @id @default(uuid()); apiSystemId String; formulaType CommissionFormulaType; salesRate Decimal? @db.Decimal(15, 4) (required only for SALES_PCT / SALES_AND_UTILITY_PCT; service validates); utilityRate Decimal? @db.Decimal(15, 4) (required only for UTILITY_PCT / SALES_AND_UTILITY_PCT); effectiveFrom DateTime (FIN-COMM-04 lookup key); notes String?; createdById String? (nullable for system-generated); createdAt DateTime @default(now()). Relations: apiSystem ApiSystem @relation(fields: [apiSystemId], references: [id]); tiers ProviderCommissionTier[]; ledgerRows ProviderCommissionLedger[]. Indexes: @@index([apiSystemId, effectiveFrom(sort: Desc)]) (critical for Pattern 1); @@index([apiSystemId]).

    ProviderCommissionTier — TIERED bracket child. Fields: id String @id @default(uuid()); configId String; minSales Decimal @db.Decimal(18, 8) (inclusive lower); maxSales Decimal? @db.Decimal(18, 8) (null = open-ended top tier per A3); rate Decimal @db.Decimal(15, 4). Relation: config ProviderCommissionConfig @relation(fields: [configId], references: [id], onDelete: Cascade). Constraints: @@unique([configId, minSales]); @@index([configId]).

    ProviderCommissionLedger — per (provider × draw). Fields: id String @id @default(uuid()); drawId String; apiSystemId String NOT NULL (DIFFER FROM Phase 11 — D-01: no TAQUILLA_ONLINE bucket); amount Decimal @db.Decimal(18, 8); salesBase Decimal @db.Decimal(18, 8); utilityBase Decimal @db.Decimal(18, 8); configId String; configSnapshot Json (denormalized {formulaType, salesRate, utilityRate, tiers} per CONTEXT.md); createdAt DateTime @default(now()). Relations: draw Draw @relation(fields: [drawId], references: [id], onDelete: Cascade); apiSystem ApiSystem @relation(fields: [apiSystemId], references: [id]); config ProviderCommissionConfig @relation(fields: [configId], references: [id]). Constraints: @@unique([drawId, apiSystemId]); @@index([drawId]); @@index([apiSystemId, createdAt]) (supports FIN-COMM-08). NO settlementId FK per A8.

    ProviderWeeklySettlement — per (provider × ISO week). Fields: id String @id @default(uuid()); apiSystemId String; isoYear Int; isoWeek Int (D-06 — never store rendered string); amount Decimal @db.Decimal(18, 8); originalAmount Decimal? @db.Decimal(18, 8) (D-02 — snapshot before manual adjustment); adjustmentReason String? (D-02); ledgerRowCount Int @default(0); status SettlementStatus @default(DRAFT); snapshotAt DateTime @default(now()); confirmedAt DateTime?; confirmedById String?. Relation: apiSystem ApiSystem @relation(fields: [apiSystemId], references: [id]). Constraints: @@unique([apiSystemId, isoYear, isoWeek]) per D-06/FIN-COMM-07; @@index([isoYear, isoWeek]); @@index([apiSystemId, status]).

    Back-relations:
    - On model ApiSystem (line 428-453), immediately after drawFinancials DrawFinancialProvider[] at line 448, add: commissionConfigs ProviderCommissionConfig[]; commissionLedgerRows ProviderCommissionLedger[]; weeklySettlements ProviderWeeklySettlement[].
    - On model Draw (line 112 block), add commissionLedgerRows ProviderCommissionLedger[] alongside Phase 11's financial DrawFinancial? and financialProviders DrawFinancialProvider[].

    Run cd backend ; npx prisma format after edits.

    Step B — dateUtils.js extension.

    Extend imports at top: add getISOWeek, getISOWeekYear, startOfISOWeek, endOfISOWeek from date-fns; toZonedTime from date-fns-tz. Reuse existing VENEZUELA_TIMEZONE.

    Three exports at bottom (mirror getVenezuelaDateString export style):
    - getISOWeekVE(date): returns { isoYear, isoWeek }. isoYear comes from getISOWeekYear(toZonedTime(date, VENEZUELA_TIMEZONE)) (NOT getFullYear — F-15 fix). isoWeek from getISOWeek on the same zoned value.
    - startOfISOWeekVE(date): UTC Date corresponding to VE Monday 00:00 of the ISO week containing date. Implementation: toZonedTime → startOfISOWeek → add 4*60*60*1000 ms to shift VE wall-clock back to UTC (Venezuela UTC-4 year-round, no DST since 2007).
    - endOfISOWeekVE(date): same shape with endOfISOWeek (Sunday 23:59:59.999 VE wall-clock, +4h shift).

    Step C — Jest test file at backend/src/lib/__tests__/dateUtils.test.js. Create __tests__ dir if missing. Import the three helpers and cover the 6 behaviors above. Mirror the test-file shape from backend/src/services/__tests__/draw-financial.service.test.js — read it to confirm the Jest ESM config. Standard run: node --experimental-vm-modules node_modules/.bin/jest.

    DO NOT modify any other files. DO NOT install any npm packages.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx prisma format &amp;&amp; npx prisma validate &amp;&amp; node --experimental-vm-modules node_modules/.bin/jest src/lib/__tests__/dateUtils.test.js</automated>
  </verify>
  <acceptance_criteria>
    - npx prisma validate exits 0.
    - grep -cE '^model (ProviderCommissionConfig|ProviderCommissionTier|ProviderCommissionLedger|ProviderWeeklySettlement) \{' backend/prisma/schema.prisma equals 4.
    - grep -cE '^enum (CommissionFormulaType|SettlementStatus) \{' backend/prisma/schema.prisma equals 2.
    - grep -v '^#' backend/prisma/schema.prisma | grep -c '@db.Decimal(18, 8)' is at least 7 (ledger.amount + salesBase + utilityBase + settlement.amount + settlement.originalAmount + tier.minSales + tier.maxSales).
    - grep -v '^#' backend/prisma/schema.prisma | grep -c '@db.Decimal(15, 4)' is at least 3 (salesRate + utilityRate + tier.rate).
    - grep -F '@@index([apiSystemId, effectiveFrom(sort: Desc)])' backend/prisma/schema.prisma returns a match.
    - grep -F '@@unique([drawId, apiSystemId])' backend/prisma/schema.prisma returns at least 2 matches (Phase 11 DrawFinancialProvider + new ledger).
    - grep -F '@@unique([apiSystemId, isoYear, isoWeek])' backend/prisma/schema.prisma returns a match.
    - grep -F 'configSnapshot Json' backend/prisma/schema.prisma returns a match.
    - grep -F 'commissionConfigs ProviderCommissionConfig[]' backend/prisma/schema.prisma returns a match.
    - grep -F 'commissionLedgerRows ProviderCommissionLedger[]' backend/prisma/schema.prisma returns at least 2 matches (one on ApiSystem, one on Draw).
    - grep -F 'weeklySettlements ProviderWeeklySettlement[]' backend/prisma/schema.prisma returns a match.
    - grep -cE 'export function (getISOWeekVE|startOfISOWeekVE|endOfISOWeekVE)' backend/src/lib/dateUtils.js equals 3.
    - grep -F 'getISOWeekYear' backend/src/lib/dateUtils.js returns a match (F-15 fix in place).
    - grep -v '^#' backend/src/lib/dateUtils.js | grep -F 'getFullYear' returns no match (F-15 trap avoided).
    - Jest exits 0 with all 6 dateUtils behaviors passing.
  </acceptance_criteria>
  <done>
    Schema validates, 4 models + 2 enums declared with relations, back-relations added, dateUtils has 3 new exports using getISOWeekYear, all F-15 boundary tests pass.
  </done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Generate and apply Prisma migration; run prisma generate; verify tables and runtime accessors</name>
  <files>
    backend/prisma/migrations/{timestamp}_add_provider_commission_models/migration.sql
  </files>
  <read_first>
    - backend/prisma/schema.prisma (must be saved with Task 1 changes — re-confirm by grep on the new model names)
    - ./CLAUDE.md (Docker container tote_postgres, port 5433, password tote_password_2025; npm run db:migrate; prod-mirror DB has ~660k TicketDetail rows but this migration is purely additive CREATE TABLE)
    - .planning/phases/11-drawfinancial-foundation/11-01-SUMMARY.md (Phase 11 surfaced that npx prisma generate is REQUIRED explicitly after migrate or new model accessors are undefined at runtime)
  </read_first>
  <action>
    BLOCKING: this migration must succeed before Plan 12-02 can import the new models.

    Step 1: Confirm Docker DB. Run docker ps --filter "name=tote_postgres" --format '{{.Names}}: {{.Status}}'. If not running, docker-compose up -d from repo root.

    Step 2: From backend/, run npm run db:migrate -- --name add_provider_commission_models. Generates backend/prisma/migrations/{timestamp}_add_provider_commission_models/migration.sql, applies to local DB at port 5433, regenerates Prisma client. Do NOT use db:push — committed migration file is required for future migrate deploy in production.

    Step 3: Inspect generated migration.sql. Must contain CREATE TABLE for ProviderCommissionConfig, ProviderCommissionTier, ProviderCommissionLedger, ProviderWeeklySettlement, plus CREATE TYPE for CommissionFormulaType and SettlementStatus, all FK constraints, all @@unique and @@index entries.

    Step 4: Run cd backend ; npx prisma generate explicitly. The command must exit 0 even though db:migrate already runs it. This is the Phase 11 lesson — without explicit generate, model accessors are undefined at runtime in some setups.

    Step 5: Verify tables exist. docker exec tote_postgres psql -U tote_user -d tote_db -c '\dt "Provider*"' must list all 4 new tables.

    Step 6: Verify runtime accessors exist (catches Phase 11 "undefined accessor" bug):
    cd backend ; node --input-type=module -e "import {prisma} from './src/lib/prisma.js'; console.log('config:',typeof prisma.providerCommissionConfig.findMany,'tier:',typeof prisma.providerCommissionTier.findMany,'ledger:',typeof prisma.providerCommissionLedger.findMany,'settlement:',typeof prisma.providerWeeklySettlement.findMany);process.exit(0);"
    All four must print "function".

    Step 7: Stage and commit ONLY the new migrations directory (and migration_lock.toml if newly created), the schema.prisma changes from Task 1, and the dateUtils.js + test from Task 1. Do NOT commit any other files.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ls prisma/migrations/*add_provider_commission_models/migration.sql &amp;&amp; npx prisma generate &amp;&amp; docker exec tote_postgres psql -U tote_user -d tote_db -tAc 'SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = '\''public'\'' AND table_name IN ('\''ProviderCommissionConfig'\'','\''ProviderCommissionTier'\'','\''ProviderCommissionLedger'\'','\''ProviderWeeklySettlement'\'')' | grep -q '^4$'</automated>
  </verify>
  <acceptance_criteria>
    - ls backend/prisma/migrations/*add_provider_commission_models/migration.sql shows the file exists.
    - grep -c 'CREATE TABLE "ProviderCommissionConfig"' on the migration.sql returns 1.
    - grep -c 'CREATE TABLE "ProviderCommissionTier"' on the migration.sql returns 1.
    - grep -c 'CREATE TABLE "ProviderCommissionLedger"' on the migration.sql returns 1.
    - grep -c 'CREATE TABLE "ProviderWeeklySettlement"' on the migration.sql returns 1.
    - grep -c 'CREATE TYPE "public"."CommissionFormulaType"' on the migration.sql returns at least 1 (Prisma may emit "public" prefix or not).
    - grep -c 'CREATE TYPE "public"."SettlementStatus"' on the migration.sql returns at least 1.
    - docker exec tote_postgres psql -U tote_user -d tote_db -tAc 'SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = '\''public'\'' AND table_name IN ('\''ProviderCommissionConfig'\'','\''ProviderCommissionTier'\'','\''ProviderCommissionLedger'\'','\''ProviderWeeklySettlement'\'')' returns 4.
    - cd backend ; node --input-type=module -e "import {prisma} from './src/lib/prisma.js'; if(typeof prisma.providerCommissionConfig.findMany!=='function')process.exit(1);if(typeof prisma.providerCommissionTier.findMany!=='function')process.exit(2);if(typeof prisma.providerCommissionLedger.findMany!=='function')process.exit(3);if(typeof prisma.providerWeeklySettlement.findMany!=='function')process.exit(4);process.exit(0);" exits 0.
    - docker exec tote_postgres psql -U tote_user -d tote_db -tAc 'SELECT COUNT(*) FROM "ProviderCommissionLedger"' returns 0 (table empty; backfill is in Plan 12-04).
  </acceptance_criteria>
  <done>
    Migration committed under backend/prisma/migrations/, all 4 tables exist in local tote_db, Prisma client regenerated AND all four runtime accessors are functions (Phase 11 bug avoided), no other files modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local developer machine → Docker Postgres | Migration SQL generated from trusted schema file; no untrusted input. |
| Schema file → Prisma client codegen | All field types and relations statically declared. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | ProviderCommissionConfig row UPDATE bypassing append-only (F-5) | mitigate | Service layer + controller will enforce append-only in Plan 12-03 (no PUT/DELETE endpoint for configs); this plan only declares the model. Plan 12-02 includes a service unit test asserting prisma.providerCommissionConfig.update is never called. |
| T-12-02 | Information disclosure | Decimal precision drift on (18,8) amounts | mitigate | Prisma @db.Decimal(18, 8) on all amount/base columns; decimal.js ROUND_HALF_UP enforced in service layer (Plan 12-02). |
| T-12-03 | Tampering | ConfigSnapshot Json field tampering at rest | accept | Postgres FS access requires admin role; same threat surface as every other JSON field in the schema. |
| T-12-04 | Denial of service | Schema migration locks ApiSystem or Draw table during back-relation FK creation | accept | Phase 11 already added similar back-relations on ApiSystem/Draw without observable downtime; new tables are additive (CREATE TABLE only — FKs point FROM new tables TO existing ones, so existing tables are not ALTERed beyond Prisma's implicit constraint addition). |
</threat_model>

<verification>
After Plan 12-01 completes:
1. cd backend ; npx prisma validate exits 0.
2. cd backend ; npx prisma generate exits 0.
3. All 4 commission tables exist in local tote_db on port 5433.
4. Migration SQL committed under backend/prisma/migrations/.
5. dateUtils.js exports getISOWeekVE / startOfISOWeekVE / endOfISOWeekVE.
6. Jest run of src/lib/__tests__/dateUtils.test.js passes all 6 tests including the three F-15 boundary cases.
7. All 4 runtime model accessors on prisma client are functions.
</verification>

<success_criteria>
- Schema changes pass prisma validate and prisma format.
- Migration applied to local DB, committed under backend/prisma/migrations/.
- All 6 Jest dateUtils tests pass.
- All four runtime accessors print 'function'.
- No other files modified.
</success_criteria>

<output>
After completion, create .planning/phases/12-provider-commission-engine/12-01-SUMMARY.md summarizing: the 4 model definitions (exact column types), the 2 new enums, the back-relations added to ApiSystem and Draw, the migration filename, the dateUtils.js helper signatures, and a note that Plan 12-02 imports computeAndUpsertLedgerForDraw + DrawFinancialNotReadyError from a new commission.service.js that will be created next.
</output>
</content>
</invoke>