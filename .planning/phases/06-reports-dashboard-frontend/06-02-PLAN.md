---
phase: 06-reports-dashboard-frontend
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified: []
autonomous: false
requirements:
  - FILT-01
  - FILT-02
  - FILT-03
  - FILT-04
  - SUMM-01
  - SUMM-02
  - SUMM-03
  - DETL-01
  - DETL-02
  - DETL-03

must_haves:
  truths:
    - "The rebuilt /admin/reportes page is live on production at 144.126.150.120:10000"
    - "Production frontend build completes without errors"
    - "Page loads in the browser on the VPS without a white screen"
  artifacts:
    - path: "/var/proyectos/tote-web/frontend/app/admin/reportes/page.js"
      provides: "Deployed rebuilt page (production copy)"
    - path: "/var/proyectos/tote-web/frontend/lib/api/monitor.js"
      provides: "Deployed updated monitor API client"
  key_links:
    - from: "git push (local)"
      to: "ssh 144 git pull"
      via: "GitHub remote"
      pattern: "git pull"
    - from: "ssh 144 npm run build"
      to: "pm2 restart tote-frontend"
      via: "production pm2 process 15"
      pattern: "pm2 restart tote-frontend"
---

<objective>
Deploy the rebuilt reports dashboard to production.

Purpose: Phase 6 plan 01 rewrites the page locally. This plan ships it to the VPS so it is live at 144.126.150.120.

Output: Production frontend rebuilt and restarted via pm2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-reports-dashboard-frontend/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push to remote and pull on VPS</name>
  <files></files>
  <action>
Run in sequence:

1. Stage and commit the two changed files:
```bash
cd /Users/diazhh/Documents/GitHub/tote-web
git add frontend/app/admin/reportes/page.js frontend/lib/api/monitor.js
git commit -m "feat(06): rebuild reports dashboard frontend — FILT/SUMM/DETL requirements"
```

2. Push to remote:
```bash
git push origin diazhh
```

3. On the VPS, pull the branch and build the frontend:
```bash
ssh 144 "cd /var/proyectos/tote-web && git pull origin diazhh"
```

4. Build the Next.js frontend on the VPS:
```bash
ssh 144 "cd /var/proyectos/tote-web/frontend && npm run build"
```

Wait for the build to complete without errors.

5. Restart the frontend pm2 process:
```bash
ssh 144 "pm2 restart tote-frontend"
```

6. Confirm the process is online:
```bash
ssh 144 "pm2 list | grep tote-frontend"
```
  </action>
  <verify>
    <automated>ssh 144 "pm2 list | grep tote-frontend | grep -c online"</automated>
  </verify>
  <done>
- git push succeeds
- `npm run build` on VPS exits 0 with no errors
- `pm2 list` shows tote-frontend status = online
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployed reports page on production</name>
  <what-built>
    Reports dashboard deployed to production. Built and restarted via pm2 on VPS 144.126.150.120.
  </what-built>
  <how-to-verify>
    1. Open https://tote.144.126.150.120:10000/admin/reportes (or the actual production URL)
    2. Confirm the page loads without a white screen or JS errors in DevTools
    3. Set today's date range and verify summary cards show real totals
    4. Select a game filter — verify the breakdown tables update
    5. Optionally test pagination if there are many draws
  </how-to-verify>
  <action>Human verification — no automated action required. Review the production URL following the steps in how-to-verify.</action>
  <verify>
    <automated>echo 'checkpoint — human verification required'</automated>
  </verify>
  <done>Human confirms /admin/reportes loads on production and filters work as expected.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `ssh 144 "pm2 list | grep tote-frontend"` shows status online
- Page responds at production URL without errors
</verification>

<success_criteria>
1. git push succeeds and VPS git pull reflects the new commits
2. `npm run build` on VPS exits 0
3. pm2 tote-frontend process is online after restart
4. Human verifier confirms /admin/reportes loads and filters work in production
</success_criteria>

<output>
After completion, create `.planning/phases/06-reports-dashboard-frontend/06-02-SUMMARY.md` following the summary template.
</output>
