---
phase: 11
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - backend/src/services/draw-financial.service.js
  - backend/src/queue/workers/calculate-draw-financials.worker.js
  - backend/src/queue/constants.js
  - backend/src/queue/register.js
  - backend/src/services/__tests__/draw-financial.service.test.js
autonomous: true
requirements:
  - FIN-AGG-01
  - FIN-AGG-02
  - FIN-AGG-03
  - FIN-AGG-04
  - FIN-AGG-06
  - FIN-AGG-07
user_setup: []

must_haves:
  truths:
    - "draw-financial.service.js aggregates via TicketDetail.drawId, not Ticket.drawId (FIN-AGG-03)"
    - "Aggregation excludes Ticket.status = 'CANCELLED' (D-17)"
    - "Worker routes by job.data.phase: 'SALES' upserts sales fields, 'PRIZES' upserts prize fields + totalizedAt (D-13)"
    - "Worker throws PrizesNotProcessedError for phase PRIZES when Draw.prizesProcessed = false (D-14, FIN-AGG-07)"
    - "Re-running the worker upserts (no duplicate-key error) — both DrawFinancial and DrawFinancialProvider (FIN-AGG-06)"
    - "DrawFinancialProvider NULL-apiSystemId case uses explicit findFirst + update/create, NOT prisma.upsert (D-08)"
    - "register.js calls boss.createQueue BEFORE boss.work for both new queues (F-11)"
    - "Phase 12 placeholder worker logs and completes, NEVER throws (D-15)"
    - "Unit test proves SUM(TicketDetail.amount) for a draw matches DrawFinancial.totalSales"
  artifacts:
    - path: "backend/src/services/draw-financial.service.js"
      provides: "computeAndUpsertSales, computeAndUpsertPrizes, PrizesNotProcessedError"
      min_lines: 100
    - path: "backend/src/queue/workers/calculate-draw-financials.worker.js"
      provides: "calculateDrawFinancialsWorker(jobs) — routes by phase"
      min_lines: 40
    - path: "backend/src/queue/constants.js"
      provides: "QUEUES.CALCULATE_DRAW_FINANCIALS, QUEUES.CALCULATE_PROVIDER_COMMISSION, matching QUEUE_CONFIGS entries"
    - path: "backend/src/queue/register.js"
      provides: "Registration of real worker + Phase 12 placeholder, both with createQueue first"
    - path: "backend/src/services/__tests__/draw-financial.service.test.js"
      provides: "Tests verifying TicketDetail.drawId aggregation matches manual SQL sum, NULL apiSystemId upsert idempotency, PrizesNotProcessedError thrown"
      min_lines: 80
  key_links:
    - from: "calculate-draw-financials.worker.js"
      to: "draw-financial.service.js"
      via: "imports computeAndUpsertSales, computeAndUpsertPrizes, PrizesNotProcessedError"
      pattern: "from '../../services/draw-financial.service.js'"
    - from: "register.js"
      to: "calculate-draw-financials.worker.js"
      via: "dynamic import + boss.work registration after boss.createQueue"
      pattern: "boss\\.createQueue\\(QUEUES\\.CALCULATE_DRAW_FINANCIALS\\)"
    - from: "register.js (Phase 12 placeholder)"
      to: "QUEUES.CALCULATE_PROVIDER_COMMISSION"
      via: "boss.createQueue + boss.work with inline log-only handler"
      pattern: "QUEUES\\.CALCULATE_PROVIDER_COMMISSION"
---

<objective>
Create the production worker pipeline that materializes DrawFinancial and DrawFinancialProvider rows — service layer with pure aggregation functions, two-phase worker, queue config additions, register.js wiring for both the real worker and the Phase 12 commission placeholder, and unit tests proving the aggregation key is TicketDetail.drawId (not the v1.2-buggy Ticket.drawId).

Purpose: This plan delivers FIN-AGG-01/02/03/04/06/07. The worker must be proven correct in isolation BEFORE the pipeline triggers (Plan 11-03) fire it on real draws and BEFORE the backfill (Plan 11-04) writes ~2600 historical rows. Unit tests are the gate.

Output: One service file, one worker file, modified constants.js + register.js, and a unit test file with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-drawfinancial-foundation/11-CONTEXT.md
@.planning/phases/11-drawfinancial-foundation/11-PATTERNS.md
@.planning/phases/11-drawfinancial-foundation/11-01-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@./CLAUDE.md

<interfaces>
From backend/src/queue/constants.js (existing structure):
```javascript
export const QUEUES = {
  // ... existing entries including:
  STEP_CALCULATE_STATS: 'step-calculate-stats',
};
export const QUEUE_CONFIGS = {
  [QUEUES.STEP_CALCULATE_STATS]: {
    retryLimit: 3,
    retryDelay: 5,
    retryBackoff: true,
    expireInMinutes: 2,
  },
  // ... others
};
```

From backend/src/queue/workers/step-calculate-stats.worker.js (analog shape):
```javascript
import { prisma } from '../../lib/prisma.js';
import logger from '../../lib/logger.js';
import drawStatsService from '../../services/draw-stats.service.js';

export async function stepCalculateStatsWorker(jobs) {
  const job = Array.isArray(jobs) ? jobs[0] : jobs;
  const { drawId } = job.data;
  const draw = await prisma.draw.findUnique({ where: { id: drawId }, select: { statsCalculated: true } });
  if (!draw) throw new Error(`Draw ${drawId} no encontrado`);
  // ... call service, update draw, return
}
```

From backend/src/queue/register.js (F-11 gold-standard pattern, ~line 75-92):
```javascript
await boss.createQueue(QUEUES.STEP_CALCULATE_STATS);
// ... other createQueue calls ...
await boss.work(QUEUES.STEP_CALCULATE_STATS, { ...QUEUE_CONFIGS[QUEUES.STEP_CALCULATE_STATS], ...parallel }, stepCalculateStatsWorker);
```

From backend/prisma/schema.prisma (after Plan 11-01 migration):
- `model DrawFinancial { id; drawId @unique; totalSales Decimal(12,2); totalPrize Decimal(12,2); utility Decimal(12,2); ticketCount Int; closedAt DateTime?; totalizedAt DateTime?; ... }`
- `model DrawFinancialProvider { id; drawId; apiSystemId String?; totalSales; totalPrize; ticketCount; @@unique([drawId, apiSystemId]); ... }`
- `model TicketDetail { drawId String; amount Decimal; prize Decimal?; ticketId String; ticket Ticket @relation(...); ... }` — the canonical aggregation source
- `model Ticket { id; status TicketStatus; apiSystemId String?; ... }`

Prisma idempotent upsert for DrawFinancial (single-column unique):
```javascript
await prisma.drawFinancial.upsert({
  where: { drawId },
  update: { totalSales, ticketCount, closedAt },
  create: { drawId, totalSales, ticketCount, closedAt },
});
```

Idempotent upsert for DrawFinancialProvider with nullable apiSystemId (CRITICAL — D-08):
```javascript
const existing = await prisma.drawFinancialProvider.findFirst({
  where: { drawId, apiSystemId: row.apiSystemId ?? null },
});
if (existing) {
  await prisma.drawFinancialProvider.update({ where: { id: existing.id }, data: {...} });
} else {
  await prisma.drawFinancialProvider.create({ data: { drawId, apiSystemId: row.apiSystemId ?? null, ... } });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create draw-financial.service.js with computeAndUpsertSales, computeAndUpsertPrizes, PrizesNotProcessedError + tests</name>
  <files>
    backend/src/services/draw-financial.service.js
    backend/src/services/__tests__/draw-financial.service.test.js
  </files>
  <read_first>
    - backend/src/services/draw-stats.service.js (shape analog: imports, default export style)
    - backend/src/services/accounting-report.service.js (lines 100-157 specifically — the BUG pattern to INVERT; must NOT replicate the Ticket.drawId aggregation per F-3)
    - backend/src/lib/prisma.js (singleton import pattern)
    - backend/src/lib/logger.js (Winston import pattern)
    - backend/prisma/schema.prisma (the new DrawFinancial + DrawFinancialProvider definitions from Plan 11-01)
    - .planning/phases/11-drawfinancial-foundation/11-PATTERNS.md (section "3. backend/src/services/draw-financial.service.js" — the exact SQL/Prisma aggregation pattern and the explicit findFirst+update/create for NULL apiSystemId)
    - .planning/research/PITFALLS.md (F-3 in full — aggregate via TicketDetail.drawId; F-1 prizesProcessed guard)
    - .planning/phases/11-drawfinancial-foundation/11-CONTEXT.md (decisions D-08, D-13, D-14, D-17)
  </read_first>
  <behavior>
    - Test 1: `computeAndUpsertSales(drawId, closedAt)` for a draw with three tickets (two non-cancelled, one cancelled) writes DrawFinancial.totalSales equal to SUM(TicketDetail.amount) of the two non-cancelled tickets only.
    - Test 2: Re-running `computeAndUpsertSales` on the same drawId updates (does not duplicate) the row — `prisma.drawFinancial.count({where:{drawId}})` returns 1 both before and after the re-run.
    - Test 3: `computeAndUpsertPrizes(drawId, drawnAt)` throws `PrizesNotProcessedError` when Draw.prizesProcessed = false; the DrawFinancial row is NOT mutated to have totalizedAt set.
    - Test 4: `computeAndUpsertPrizes(drawId, drawnAt)` after `prizesProcessed = true` writes totalPrize, utility = totalSales - totalPrize, and totalizedAt = the passed-in argument (not now()).
    - Test 5: Tickets with apiSystemId = NULL aggregate into a single DrawFinancialProvider row where apiSystemId IS NULL; re-running creates no duplicates (verifies the D-08 findFirst+update/create pattern).
    - Test 6: Multi-draw ticket case (one Ticket with two TicketDetails pointing to two different drawIds): SUM of DrawFinancial.totalSales across both draws equals SUM of all TicketDetail.amount, proving the F-3 fix at the source of truth.
  </behavior>
  <action>
    Create `backend/src/services/draw-financial.service.js`. Imports: `import { prisma } from '../lib/prisma.js'` and `import logger from '../lib/logger.js'`. No imports from accounting-report.service.js — invert its aggregation per F-3.

    Export class `PrizesNotProcessedError extends Error` with constructor taking drawId and setting `this.name = 'PrizesNotProcessedError'` and message `Draw ${drawId} prizes not processed — cannot compute totalPrize/utility`. Use a named class export (`export class PrizesNotProcessedError`).

    Export function `computeAndUpsertSales(drawId, closedAt)`:
    - Run a Prisma aggregation: `prisma.ticketDetail.aggregate({ where: { drawId, ticket: { status: { not: 'CANCELLED' } } }, _sum: { amount: true } })` — call the sum `totalSalesSum` (Prisma Decimal | null; coerce null to 0).
    - Run a `prisma.ticketDetail.findMany({ where: { drawId, ticket: { status: { not: 'CANCELLED' } } }, distinct: ['ticketId'], select: { ticketId: true } })` and use `.length` for `ticketCount`.
    - Run a `$queryRaw` for per-provider breakdown grouped by `t."apiSystemId"` summing `td.amount` with the same CANCELLED filter — see 11-PATTERNS.md section 3 for the exact SQL. Cast `SUM(td.amount)::numeric(12,2)` so Prisma returns the value as a string or Decimal (not number with float drift).
    - Upsert DrawFinancial via `prisma.drawFinancial.upsert({ where: { drawId }, update: { totalSales: totalSalesSum, ticketCount, closedAt }, create: { drawId, totalSales: totalSalesSum, ticketCount, closedAt } })`.
    - For each row in the per-provider breakdown: use the D-08 findFirst + update/create explicit pattern (NOT prisma.upsert) — `apiSystemId: row.apiSystemId ?? null` in both branches. Update only totalSales and ticketCount (totalPrize stays at default 0 until PRIZES phase). Important: also handle the case where a provider had sales in a prior SALES run but is absent in this re-run (zero out or delete? — for Phase 11, we leave stale provider rows untouched; re-aggregation runs on the same draw will only touch providers currently present in the live ticket set. Late cancellations within the SALES→PRIZES window are absorbed naturally per D-18).
    - Log: `logger.info('[draw-financial] phase=SALES drawId=${drawId} totalSales=${totalSalesSum} ticketCount=${ticketCount}')`.
    - Return `{ drawId, phase: 'SALES', totalSales: totalSalesSum, ticketCount }`.
    - Use a named function export: `export const computeAndUpsertSales = async (drawId, closedAt) => { ... }` (or `export async function computeAndUpsertSales(...)`).

    Export function `computeAndUpsertPrizes(drawId, totalizedAt)`:
    - Read `prisma.draw.findUnique({ where: { id: drawId }, select: { prizesProcessed: true } })`. If `prizesProcessed === false`, throw `new PrizesNotProcessedError(drawId)` immediately (F-1, FIN-AGG-07).
    - Aggregate prizes via TicketDetail in the same shape as sales — `_sum: { prize: true }` filtered by `ticket.status != 'CANCELLED'`. Coerce null to 0.
    - Re-aggregate sales the same way (so utility = totalSales - totalPrize is computed from a single consistent snapshot — both numbers come from the same TicketDetail rows at the same point in time).
    - Compute `utility = totalSales - totalPrize` using Prisma Decimal arithmetic OR import `Decimal from 'decimal.js'` and use `new Decimal(totalSales).minus(totalPrize).toFixed(2)`. Either is acceptable as long as the result is stored without float drift.
    - Per-provider breakdown raw query summing both `td.amount` and `td.prize`.
    - Upsert DrawFinancial: update path writes `totalPrize`, `utility`, `totalizedAt: totalizedAt`. Do NOT pass `closedAt` here (preserves the value written by phase SALES); create path writes all four plus default ticketCount and a `closedAt: null` fallback (this only happens if PRIZES fires without SALES ever having run — should be impossible in normal pipeline order, but the upsert must remain idempotent).
    - For DrawFinancialProvider rows, use the same findFirst+update/create pattern, updating totalPrize for each provider. Leave totalSales/ticketCount as-is (already populated by SALES) — UPDATE clause should not zero them.
    - Log info line.
    - Return `{ drawId, phase: 'PRIZES', totalSales, totalPrize, utility, totalizedAt }`.
    - Use a named function export: `export const computeAndUpsertPrizes = async (drawId, totalizedAt) => { ... }` (or `export async function computeAndUpsertPrizes(...)`).

    Module exports: MUST use named exports — `export const computeAndUpsertSales`, `export const computeAndUpsertPrizes`, and `export class PrizesNotProcessedError` (or equivalent `export async function` / `export class` forms). Do NOT use `export default { ... }` (the cross-plan import contract in Plan 11-02 Task 2 and Plan 11-04 Task 1 assumes named imports). Document the chosen form (arrow const vs function declaration) in the SUMMARY for downstream readers.

    Create `backend/src/services/__tests__/draw-financial.service.test.js`. Use the existing project Jest setup (the repo already has `npm test` wired). Read `backend/src/services/__tests__/` for any existing test patterns and helper imports (e.g., `prismaMock` or live DB test pattern — pick whichever is dominant in that directory). Tests must cover the six behaviors above. Use a transaction-rollback or beforeEach cleanup pattern so tests don't leak rows.

    If the existing test setup uses a live Postgres test schema (likely, given Prisma): each test creates a fresh `Game`, `Draw`, and a small set of `Ticket` + `TicketDetail` rows, calls the service, asserts on DB state. Cleanup in `afterEach` deletes the test rows by Game prefix.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm test -- --testPathPattern=draw-financial.service.test 2&gt;&amp;1 | tail -40 &amp;&amp; grep -c "TicketDetail" src/services/draw-financial.service.js</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm test -- --testPathPattern=draw-financial.service` exits 0 with all 6 tests passing.
    - `grep -c "ticketDetail" backend/src/services/draw-financial.service.js` returns at least 4 (proves aggregation goes through TicketDetail, not Ticket).
    - `grep -c "draw.tickets" backend/src/services/draw-financial.service.js` returns 0 (proves the anti-pattern from accounting-report.service.js is NOT replicated).
    - `grep "status: { not: 'CANCELLED' }" backend/src/services/draw-financial.service.js` returns at least 2 matches.
    - `grep "PrizesNotProcessedError" backend/src/services/draw-financial.service.js` returns at least 2 matches (class declaration + throw).
    - `grep -c "prisma.drawFinancialProvider.upsert" backend/src/services/draw-financial.service.js` returns 0 (D-08: explicit findFirst+update/create, no Prisma upsert for the nullable-FK table).
    - `grep "findFirst" backend/src/services/draw-financial.service.js` returns at least 1 match.
    - Named export contract enforced (Plan 11-04 Task 1 depends on this):
      - `grep -E "^export (const|async function) computeAndUpsertSales" backend/src/services/draw-financial.service.js` returns at least 1 match.
      - `grep -E "^export (const|async function) computeAndUpsertPrizes" backend/src/services/draw-financial.service.js` returns at least 1 match.
      - `grep -E "^export class PrizesNotProcessedError" backend/src/services/draw-financial.service.js` returns at least 1 match.
      - `grep -c "^export default" backend/src/services/draw-financial.service.js` returns 0 (no default export — named imports only across all consumers).
    - Test file imports the service and exercises both phases against a real DB (uses Prisma client).
  </acceptance_criteria>
  <done>
    Service file delivers idempotent aggregation via TicketDetail.drawId with NULL-aware upserts and explicit PrizesNotProcessedError, exported as named exports for downstream consumers. All 6 unit tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create calculate-draw-financials.worker.js with two-phase routing + add QUEUES + QUEUE_CONFIGS entries</name>
  <files>
    backend/src/queue/workers/calculate-draw-financials.worker.js
    backend/src/queue/constants.js
  </files>
  <read_first>
    - backend/src/queue/workers/step-calculate-stats.worker.js (full file — the structural analog)
    - backend/src/queue/constants.js (the existing QUEUES + QUEUE_CONFIGS blocks; specifically the STEP_CALCULATE_STATS entries)
    - backend/src/services/draw-financial.service.js (created in Task 1 — confirms the import path and exported function names)
    - .planning/phases/11-drawfinancial-foundation/11-PATTERNS.md (sections 2 + 5 — worker shape and constants additions)
    - .planning/phases/11-drawfinancial-foundation/11-CONTEXT.md (D-13 routing, D-14 guard, D-15 placeholder names)
  </read_first>
  <behavior>
    - Test: worker called with `{ drawId, phase: 'SALES' }` invokes the service `computeAndUpsertSales` and returns `{ success: true, drawId, phase: 'SALES' }`.
    - Test: worker called with `{ drawId, phase: 'PRIZES' }` against a draw with `prizesProcessed = false` throws `PrizesNotProcessedError` (the error propagates to pg-boss, no zero-prize row is written).
    - Test: worker called with `{ drawId, phase: 'UNKNOWN' }` throws an error mentioning the unknown phase.
    - Test: worker called with `{ drawId }` for a non-existent drawId throws `new Error('Draw ${drawId} no encontrado')`.
  </behavior>
  <action>
    Create `backend/src/queue/workers/calculate-draw-financials.worker.js`. Imports: `import { prisma } from '../../lib/prisma.js'`, `import logger from '../../lib/logger.js'`, and named imports from the service module: `import { computeAndUpsertSales, computeAndUpsertPrizes, PrizesNotProcessedError } from '../../services/draw-financial.service.js'` (named imports — Plan 11-02 Task 1 commits the service to named exports).

    Export `async function calculateDrawFinancialsWorker(jobs)`. Unwrap with `const job = Array.isArray(jobs) ? jobs[0] : jobs;`. Destructure `const { drawId, phase } = job.data;`.

    Load the draw with `prisma.draw.findUnique({ where: { id: drawId }, select: { prizesProcessed: true, closedAt: true, drawnAt: true } })`. If null, throw `new Error(\`Draw ${drawId} no encontrado\`)` (Spanish to match existing worker convention).

    Switch on phase:
    - `'SALES'`: log `[calculate-draw-financials] phase=SALES drawId=${drawId}`, call `await computeAndUpsertSales(drawId, draw.closedAt)`, return `{ success: true, drawId, phase: 'SALES' }`.
    - `'PRIZES'`: if `draw.prizesProcessed === false`, throw `new PrizesNotProcessedError(drawId)` (don't call the service — fail-fast at the worker boundary so the error is visible in pg-boss job state). Else log and call `await computeAndUpsertPrizes(drawId, draw.drawnAt)`. Return `{ success: true, drawId, phase: 'PRIZES' }`.
    - default: throw `new Error(\`[calculate-draw-financials] unknown phase: ${phase}\`)`.

    Add unit tests in the same file or a sibling `__tests__/calculate-draw-financials.worker.test.js`. Use Jest mocks: stub the service functions with `jest.mock('../../services/draw-financial.service.js', ...)` so the worker test is hermetic and doesn't hit the DB. Verify each behavior above. The PrizesNotProcessedError test should mock the Prisma client to return `{ prizesProcessed: false }`.

    Modify `backend/src/queue/constants.js`:
    - Add to `QUEUES`: `CALCULATE_DRAW_FINANCIALS: 'calculate-draw-financials'` and `CALCULATE_PROVIDER_COMMISSION: 'calculate-provider-commission'` (placed alphabetically or near STEP_CALCULATE_STATS — match existing ordering convention by reading the file).
    - Add to `QUEUE_CONFIGS`: matching entries with `retryLimit: 3, retryDelay: 5, retryBackoff: true, expireInMinutes: 3` for CALCULATE_DRAW_FINANCIALS (slightly larger window — two-phase + per-provider upserts), and `expireInMinutes: 2` for CALCULATE_PROVIDER_COMMISSION (placeholder, fast).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm test -- --testPathPattern=calculate-draw-financials.worker 2&gt;&amp;1 | tail -30 &amp;&amp; node -e "const c = require('./src/queue/constants.js'); if (!c.QUEUES.CALCULATE_DRAW_FINANCIALS || !c.QUEUES.CALCULATE_PROVIDER_COMMISSION) { process.exit(1); } if (!c.QUEUE_CONFIGS[c.QUEUES.CALCULATE_DRAW_FINANCIALS] || !c.QUEUE_CONFIGS[c.QUEUES.CALCULATE_PROVIDER_COMMISSION]) { process.exit(2); } console.log('queues ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm test -- --testPathPattern=calculate-draw-financials.worker` passes all 4 tests.
    - `grep "CALCULATE_DRAW_FINANCIALS" backend/src/queue/constants.js` returns at least 2 matches (QUEUES entry + QUEUE_CONFIGS entry).
    - `grep "CALCULATE_PROVIDER_COMMISSION" backend/src/queue/constants.js` returns at least 2 matches.
    - `grep "PrizesNotProcessedError" backend/src/queue/workers/calculate-draw-financials.worker.js` returns at least 2 matches (import + throw).
    - `grep -E "import \{[^}]*computeAndUpsertSales[^}]*\}" backend/src/queue/workers/calculate-draw-financials.worker.js` returns at least 1 match (named-import form enforced — matches the service's named exports from Task 1).
    - `grep "Array.isArray(jobs)" backend/src/queue/workers/calculate-draw-financials.worker.js` returns at least 1 match (pg-boss v10 mandatory unwrap).
    - `grep "phase: 'SALES'" backend/src/queue/workers/calculate-draw-financials.worker.js` returns at least 1 match.
    - `grep "phase: 'PRIZES'" backend/src/queue/workers/calculate-draw-financials.worker.js` returns at least 1 match.
  </acceptance_criteria>
  <done>
    Worker routes correctly by phase, fails fast on missing prizesProcessed, constants updated with both new queue names and configs, unit tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire register.js — boss.createQueue then boss.work for the real worker AND the Phase 12 placeholder; smoke-test queue rows</name>
  <files>backend/src/queue/register.js</files>
  <read_first>
    - backend/src/queue/register.js (full file — confirm the existing EXECUTE_DRAW block at ~lines 66-92 and the SYNC_SCRAPE_TICKETS block at ~lines 127-141 with its F-11 warning comment; identify the cleanest insertion point — likely after the STEP_CALCULATE_STATS block so both DrawStats and DrawFinancial workers register together)
    - backend/src/queue/constants.js (after Task 2 edits — confirm the two new QUEUES keys exist)
    - backend/src/queue/workers/calculate-draw-financials.worker.js (created in Task 2)
    - .planning/phases/11-drawfinancial-foundation/11-PATTERNS.md (section 6 — exact register.js shape, including the inline Phase 12 placeholder handler that logs and returns)
    - .planning/research/PITFALLS.md (F-11 in full — createQueue before work mandatory)
    - .planning/phases/11-drawfinancial-foundation/11-CONTEXT.md (D-15 Phase 12 placeholder constraints — must NOT throw)
  </read_first>
  <action>
    Modify `backend/src/queue/register.js`. Add a dynamic import for the new worker: `const { calculateDrawFinancialsWorker } = await import('./workers/calculate-draw-financials.worker.js');` placed in the same import section/style as the other worker imports in the file.

    Add a Phase 11 + Phase 12 placeholder registration block. Order matters per F-11: createQueue → work.

    1. `await boss.createQueue(QUEUES.CALCULATE_DRAW_FINANCIALS)`
    2. `await boss.createQueue(QUEUES.CALCULATE_PROVIDER_COMMISSION)` (D-15 placeholder — registered HERE in Phase 11 to prevent F-11 silent-drop on Phase 12's first deploy)
    3. `await boss.work(QUEUES.CALCULATE_DRAW_FINANCIALS, QUEUE_CONFIGS[QUEUES.CALCULATE_DRAW_FINANCIALS], calculateDrawFinancialsWorker)` — no parallel spread; matches the simpler workers in the file. Optionally add `{ ...QUEUE_CONFIGS[...], ...parallel }` if the existing block uses parallel — mirror whatever the STEP_CALCULATE_STATS work() call does in the SAME file.
    4. `await boss.work(QUEUES.CALCULATE_PROVIDER_COMMISSION, QUEUE_CONFIGS[QUEUES.CALCULATE_PROVIDER_COMMISSION], async (jobs) => { const job = Array.isArray(jobs) ? jobs[0] : jobs; logger.info(\`[calculate-provider-commission] phase-12 placeholder, drawId=${job.data?.drawId}\`); return { placeholder: true }; })` — inline arrow function, MUST NOT throw under any circumstance (D-15). If `job.data` is undefined, the optional chain returns undefined; log accepts undefined.
    5. Add a single `logger.info('[pg-boss] Workers calculate-draw-financials + commission-placeholder registrados');` immediately after the two work() calls.

    Do NOT gate this block behind a PGBOSS_* env flag — per ./CLAUDE.md "PGBOSS_* env flags are no longer load-bearing" and 11-PATTERNS.md "Recommend NO flag (always-on)".

    Smoke test after edits: start the backend locally via `cd backend && npm run dev` for ~5 seconds, then in a separate shell run `docker exec tote_postgres psql -U tote_user -d tote_db -c "SELECT name FROM pgboss.queue WHERE name LIKE 'calculate-%'"`. Both `calculate-draw-financials` and `calculate-provider-commission` must appear. Kill the dev server after the check.

    If running a long-lived dev server is awkward in CI, an equivalent smoke test is: write a small one-shot Node script that imports `getBoss` (or the same bootstrap path), calls the registration, queries the pgboss.queue table, and exits. Either is acceptable.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -c "boss.createQueue(QUEUES.CALCULATE_DRAW_FINANCIALS)" src/queue/register.js | grep -q "^[[:space:]]*1$" &amp;&amp; grep -c "boss.createQueue(QUEUES.CALCULATE_PROVIDER_COMMISSION)" src/queue/register.js | grep -q "^[[:space:]]*1$" &amp;&amp; grep -c "boss.work(QUEUES.CALCULATE_DRAW_FINANCIALS" src/queue/register.js | grep -q "^[[:space:]]*1$" &amp;&amp; grep -c "boss.work(QUEUES.CALCULATE_PROVIDER_COMMISSION" src/queue/register.js | grep -q "^[[:space:]]*1$" &amp;&amp; node -e "const fs = require('fs'); const src = fs.readFileSync('src/queue/register.js', 'utf8'); const cqIdx = src.indexOf('boss.createQueue(QUEUES.CALCULATE_DRAW_FINANCIALS)'); const wIdx = src.indexOf('boss.work(QUEUES.CALCULATE_DRAW_FINANCIALS'); if (cqIdx === -1 || wIdx === -1 || cqIdx >= wIdx) { console.error('F-11 violation: createQueue must precede work'); process.exit(1); } console.log('F-11 order ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "boss.createQueue(QUEUES.CALCULATE_DRAW_FINANCIALS)" backend/src/queue/register.js` returns exactly 1.
    - `grep -c "boss.createQueue(QUEUES.CALCULATE_PROVIDER_COMMISSION)" backend/src/queue/register.js` returns exactly 1.
    - `grep -c "boss.work(QUEUES.CALCULATE_DRAW_FINANCIALS" backend/src/queue/register.js` returns exactly 1.
    - `grep -c "boss.work(QUEUES.CALCULATE_PROVIDER_COMMISSION" backend/src/queue/register.js` returns exactly 1.
    - The byte-offset of `boss.createQueue(QUEUES.CALCULATE_DRAW_FINANCIALS)` is less than the byte-offset of `boss.work(QUEUES.CALCULATE_DRAW_FINANCIALS` (the node script in `verify` asserts this; an F-11 violation exits non-zero).
    - `grep "phase-12 placeholder" backend/src/queue/register.js` returns at least 1 match (the D-15 marker string).
    - Smoke test passes: after a brief boot of the backend, `SELECT name FROM pgboss.queue WHERE name LIKE 'calculate-%'` returns at least 2 rows (`calculate-draw-financials` and `calculate-provider-commission`).
    - The placeholder handler does NOT call `throw` anywhere in its body (`grep -A 5 "phase-12 placeholder" backend/src/queue/register.js | grep -c "throw"` returns 0).
  </acceptance_criteria>
  <done>
    register.js registers both new queues with F-11-correct ordering, the placeholder is wired and proven non-throwing, the pgboss.queue table contains both rows after boot.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg-boss job payload -> worker | `job.data.drawId` is supplied by trusted internal callers only (close-and-ingest worker, step-process-prizes worker, backfill script). External webhook input never reaches this queue directly. |
| Worker -> Prisma queries | All queries are parameterized via Prisma; no raw user input concatenated into SQL. |
| `$queryRaw` per-provider breakdown | drawId is a UUID string from the queue payload; Prisma tagged template literal binds it as a parameter, not interpolated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | Malicious caller enqueues `{ drawId: <victim-drawId>, phase: 'PRIZES' }` to corrupt totals | accept | pg-boss queue submission requires DB write access (admin/service credentials); attack surface is internal. Worker is idempotent — replay produces the same result. |
| T-11-06 | Tampering | `$queryRaw` SQL injection via drawId | mitigate | Use Prisma's tagged template literal binding (`${drawId}`) — Prisma converts to a parameterized query. Do NOT use string concatenation in $queryRawUnsafe. |
| T-11-07 | Information disclosure | DrawFinancial reads cross-tenant ticket data | accept | Single-tenant system (one operator entity per CONTEXT.md out-of-scope list). |
| T-11-08 | Denial of service | Phase 12 placeholder accidentally re-enqueues itself or throws | mitigate | D-15: placeholder handler MUST NOT throw; verified by acceptance criterion grepping for `throw` in the handler body. Handler simply logs and returns. |
| T-11-09 | Elevation of privilege | Worker receives `phase: 'OVERRIDE'` and bypasses the prizesProcessed guard | mitigate | Phase routing is a strict switch with `default: throw`; no path silently accepts unknown phase. |
</threat_model>

<verification>
1. `cd backend && npm test -- --testPathPattern=draw-financial` exits 0 (service tests pass).
2. `cd backend && npm test -- --testPathPattern=calculate-draw-financials.worker` exits 0 (worker tests pass).
3. Both queues registered correctly (F-11 ordering enforced by the node script in Task 3 verify).
4. After backend boot, both queue rows exist in pgboss.queue.
5. The placeholder handler never throws (grep verifies).
</verification>

<success_criteria>
- All 6 FIN-AGG requirements addressed by this plan (01, 02, 03, 04, 06, 07) have implementing code with passing tests.
- Aggregation goes through TicketDetail.drawId — proven by unit test 6 (multi-draw ticket case).
- Worker is idempotent — proven by unit test 2 (re-run produces single row) and 5 (NULL apiSystemId no-duplicate).
- prizesProcessed guard throws PrizesNotProcessedError — proven by unit test 3.
- Phase 12 commission queue placeholder registered correctly with non-throwing handler.
- F-11 createQueue-before-work order enforced for both queues by the node script in Task 3.
</success_criteria>

<output>
After completion, create `.planning/phases/11-drawfinancial-foundation/11-02-SUMMARY.md` summarizing:
- Confirmation that named exports (`computeAndUpsertSales`, `computeAndUpsertPrizes`, `PrizesNotProcessedError`) were used (locked by this plan; the chosen form — arrow const vs `async function` — should be noted).
- Exact function signatures of `computeAndUpsertSales` and `computeAndUpsertPrizes`.
- The PrizesNotProcessedError shape (so Plan 11-03 can reference it if needed).
- Queue names and configs added to constants.js.
- The smoke-test command operators run after deploy to confirm both pgboss.queue rows exist.
- The test count and what each test proves (the goal-backward truths above).
</output>
</content>
</invoke>