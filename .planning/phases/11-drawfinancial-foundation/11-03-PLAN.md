---
phase: 11
plan: 3
type: execute
wave: 3
depends_on: [2]
files_modified:
  - backend/src/queue/workers/close-and-ingest.worker.js
  - backend/src/queue/workers/step-process-prizes.worker.js
  - backend/src/queue/workers/__tests__/draw-financial-pipeline.integration.test.js
autonomous: true
requirements:
  - FIN-AGG-01
  - FIN-AGG-02
user_setup: []

must_haves:
  truths:
    - "After a draw closes (all 3 close return paths), boss.send(QUEUES.CALCULATE_DRAW_FINANCIALS, {drawId, phase: 'SALES'}) is fired inside try/catch (D-10)"
    - "After prize processing finishes, boss.send(QUEUES.CALCULATE_DRAW_FINANCIALS, {drawId, phase: 'PRIZES'}) is fired alongside the existing STEP_CALCULATE_STATS send (D-11)"
    - "phase-SALES failure logs a warning and never blocks the close return"
    - "Integration test proves: closing a draw with seeded tickets produces a DrawFinancial row with correct totalSales within a few seconds"
    - "Integration test proves: prize processing produces a DrawFinancial row with totalPrize + totalizedAt set"
  artifacts:
    - path: "backend/src/queue/workers/close-and-ingest.worker.js"
      provides: "phase-SALES trigger at all 3 close return paths"
      contains: "QUEUES.CALCULATE_DRAW_FINANCIALS"
    - path: "backend/src/queue/workers/step-process-prizes.worker.js"
      provides: "phase-PRIZES parallel trigger alongside STEP_CALCULATE_STATS"
      contains: "phase: 'PRIZES'"
    - path: "backend/src/queue/workers/__tests__/draw-financial-pipeline.integration.test.js"
      provides: "End-to-end test verifying the pipeline produces DrawFinancial rows"
      min_lines: 80
  key_links:
    - from: "close-and-ingest.worker.js"
      to: "QUEUES.CALCULATE_DRAW_FINANCIALS"
      via: "boss.send inside try/catch — never blocks close return"
      pattern: "phase: 'SALES'"
    - from: "step-process-prizes.worker.js"
      to: "QUEUES.CALCULATE_DRAW_FINANCIALS"
      via: "boss.send alongside STEP_CALCULATE_STATS send (no try/catch — symmetrical)"
      pattern: "phase: 'PRIZES'"
---

<objective>
Wire the new calculate-draw-financials worker into the existing draw pipeline at the two trigger points identified in CONTEXT.md: phase-SALES from close-and-ingest.worker.js (best-effort, never blocks close) and phase-PRIZES from step-process-prizes.worker.js (parallel to existing STEP_CALCULATE_STATS send). Prove the wiring works end-to-end with an integration test against the live local DB.

Purpose: This plan delivers FIN-AGG-01 and FIN-AGG-02 at the pipeline level — the worker is no longer just a unit-tested island, it actually fires when draws complete. The integration test is the gate before backfill (Plan 11-04) writes ~2600 rows.

Output: Two modified worker files with surgical additions matching the existing best-effort patterns, and one integration test proving the pipeline produces correct DrawFinancial rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-drawfinancial-foundation/11-CONTEXT.md
@.planning/phases/11-drawfinancial-foundation/11-PATTERNS.md
@.planning/phases/11-drawfinancial-foundation/11-01-SUMMARY.md
@.planning/phases/11-drawfinancial-foundation/11-02-SUMMARY.md
@./CLAUDE.md

<interfaces>
From backend/src/queue/workers/step-process-prizes.worker.js (existing trigger pattern at ~lines 42-46 and ~19-23):
```javascript
const boss = getBoss();
await boss.send(QUEUES.STEP_CALCULATE_STATS, { drawId }, {
  singletonKey: `stats-${drawId}`,
  ...QUEUE_CONFIGS[QUEUES.STEP_CALCULATE_STATS],
});
```
Existing imports in this file already include `getBoss` from `'../boss.js'` and `QUEUES, QUEUE_CONFIGS` from `'../constants.js'` — no new imports needed.

From backend/src/queue/workers/close-and-ingest.worker.js (best-effort try/catch convention at ~lines 30-34, 60, 169):
```javascript
try {
  const r = await apiIntegrationService.importSRQTickets(draw.id, { allowClosed: true });
  imported = r.imported || 0;
} catch (e) {
  logger.warn(`[close-and-ingest:terminal] SRQ falló: ${e.message}`);
}
```

Three close return paths exist (per 11-PATTERNS.md section 7):
- Inside `closeTerminalDraw` near line 73: `return { closed: true, method: 'terminal', ... }`
- Admin preselect path near line 173: `return { closed: true, method: 'admin_preselect' }`
- Main normal path near line 205: `return { closed: true, method: 'awaiting_preselect', ... }`

All three need the phase-SALES trigger inserted BEFORE the return statement, inside a try/catch.

The `getBoss` import may or may not already exist in close-and-ingest.worker.js — read the imports block and add only if absent.

From Plan 11-02 (DrawFinancial worker): handles the side effect; this plan only enqueues to its queue.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phase-SALES boss.send to close-and-ingest.worker.js at all three close return paths</name>
  <files>backend/src/queue/workers/close-and-ingest.worker.js</files>
  <read_first>
    - backend/src/queue/workers/close-and-ingest.worker.js (FULL file — locate every `return { closed: true, ... }` statement; identify which imports already exist for `getBoss`, `QUEUES`, `QUEUE_CONFIGS`; observe the existing best-effort try/catch pattern for SRQ at ~lines 30-34 and admin notification at ~lines 155-170)
    - .planning/phases/11-drawfinancial-foundation/11-PATTERNS.md (section "7. backend/src/queue/workers/close-and-ingest.worker.js" — exact code shape to insert at each return path, including the singletonKey `df-sales-${drawId}`)
    - .planning/phases/11-drawfinancial-foundation/11-CONTEXT.md (D-10 best-effort semantics — failure logs warning, never blocks close)
    - backend/src/queue/constants.js (after Plan 11-02 edits — confirm QUEUES.CALCULATE_DRAW_FINANCIALS exists)
  </read_first>
  <action>
    Modify backend/src/queue/workers/close-and-ingest.worker.js.

    Ensure imports include `getBoss` from `'../boss.js'` and `QUEUES, QUEUE_CONFIGS` from `'../constants.js'`. Add only what is missing — do not duplicate existing imports.

    For EACH of the three `return { closed: true, ... }` statements (terminal path, admin_preselect path, main awaiting_preselect path):
    - Insert immediately BEFORE the return, inside the same function scope where `drawId` is in scope:
      - Open a try block.
      - Get the boss: `const boss = getBoss();` (only declare if not already in scope at that point; reuse the existing variable otherwise — read the surrounding code to see if `boss` is already declared earlier in the function).
      - Call `await boss.send(QUEUES.CALCULATE_DRAW_FINANCIALS, { drawId, phase: 'SALES' }, { singletonKey: \`df-sales-${drawId}\`, ...QUEUE_CONFIGS[QUEUES.CALCULATE_DRAW_FINANCIALS] })`.
      - Catch any error and `logger.warn(\`[close-and-ingest] df-sales trigger falló (best-effort) drawId=${drawId}: ${e.message}\`)`. CRITICAL: the catch block MUST NOT re-throw — D-10 mandates "failures log a warning but never block the close".
    - Naming: the variable holding the draw id may be `draw.id` or `drawId` depending on the function; use whichever exists at that scope. The singletonKey template literal must resolve to a non-empty value.

    Do NOT modify any return value shapes. Do NOT change existing best-effort patterns. The phase-SALES insertion is purely additive.

    Add a single comment above each insertion: `// Phase 11 (D-10): fire-and-forget phase-SALES trigger. Never blocks the close.`

    No tests in this task — the integration test in Task 3 of this plan covers it end-to-end.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -c "phase: 'SALES'" src/queue/workers/close-and-ingest.worker.js | grep -qE "^[[:space:]]*[3-9]$" &amp;&amp; grep -c "df-sales-" src/queue/workers/close-and-ingest.worker.js | grep -qE "^[[:space:]]*[3-9]$" &amp;&amp; grep -c "Phase 11 (D-10)" src/queue/workers/close-and-ingest.worker.js | grep -qE "^[[:space:]]*[3-9]$" &amp;&amp; node --check src/queue/workers/close-and-ingest.worker.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "phase: 'SALES'" backend/src/queue/workers/close-and-ingest.worker.js` returns at least 3 (one per return path).
    - `grep -c "df-sales-" backend/src/queue/workers/close-and-ingest.worker.js` returns at least 3 (singletonKey appears at each insertion).
    - `grep -c "Phase 11 (D-10)" backend/src/queue/workers/close-and-ingest.worker.js` returns at least 3 (the marker comment).
    - `grep -B 0 -A 5 "phase: 'SALES'" backend/src/queue/workers/close-and-ingest.worker.js | grep -c "catch"` returns at least 3 (each insertion wraps in try/catch).
    - `node --check backend/src/queue/workers/close-and-ingest.worker.js` exits 0 (syntactically valid).
    - The grep for "df-sales trigger falló" returns at least 3 matches (the warn log line in each catch).
  </acceptance_criteria>
  <done>
    All 3 close return paths fire phase-SALES inside try/catch, none re-throw, file parses cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add phase-PRIZES parallel boss.send to step-process-prizes.worker.js alongside STEP_CALCULATE_STATS</name>
  <files>backend/src/queue/workers/step-process-prizes.worker.js</files>
  <read_first>
    - backend/src/queue/workers/step-process-prizes.worker.js (FULL file — locate the two existing `boss.send(QUEUES.STEP_CALCULATE_STATS, ...)` call sites at ~lines 19-23 and ~lines 42-46; confirm `getBoss`, `QUEUES`, `QUEUE_CONFIGS` are already imported)
    - .planning/phases/11-drawfinancial-foundation/11-PATTERNS.md (section "8. backend/src/queue/workers/step-process-prizes.worker.js" — exact mirror pattern for the parallel send)
    - .planning/phases/11-drawfinancial-foundation/11-CONTEXT.md (D-11 — runs in parallel, no ordering dependency between DrawStats and DrawFinancial)
    - backend/src/queue/constants.js (confirm QUEUES.CALCULATE_DRAW_FINANCIALS exists)
  </read_first>
  <action>
    Modify backend/src/queue/workers/step-process-prizes.worker.js.

    Locate both call sites where `boss.send(QUEUES.STEP_CALCULATE_STATS, { drawId }, { singletonKey: \`stats-${drawId}\`, ...QUEUE_CONFIGS[QUEUES.STEP_CALCULATE_STATS] })` appears. Immediately AFTER each (do not wrap in try/catch — mirror the STEP_CALCULATE_STATS lack of try/catch since the upstream `prizesProcessed = true` is already committed by this point), insert:

    `await boss.send(QUEUES.CALCULATE_DRAW_FINANCIALS, { drawId, phase: 'PRIZES' }, { singletonKey: \`df-prizes-${drawId}\`, ...QUEUE_CONFIGS[QUEUES.CALCULATE_DRAW_FINANCIALS] });`

    Add a single comment immediately above each insertion: `// Phase 11 (D-11): parallel-trigger DrawFinancial phase PRIZES`.

    Do NOT add try/catch wrapping — D-11 explicitly mandates "no try/catch wrapping needed here; STEP_CALCULATE_STATS is already not wrapped". A failed send here will cause pg-boss to retry the entire step-process-prizes worker, which is the correct semantics since prizesProcessed has not yet been committed at that retry boundary (read the file to confirm — if commit order is reversed, downgrade Task 2 to also use try/catch and document in SUMMARY).

    No new imports — `getBoss`, `QUEUES`, `QUEUE_CONFIGS` are already in the file.

    No tests in this task — the integration test in Task 3 covers it end-to-end.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -c "phase: 'PRIZES'" src/queue/workers/step-process-prizes.worker.js | grep -qE "^[[:space:]]*[2-9]$" &amp;&amp; grep -c "df-prizes-" src/queue/workers/step-process-prizes.worker.js | grep -qE "^[[:space:]]*[2-9]$" &amp;&amp; grep -c "Phase 11 (D-11)" src/queue/workers/step-process-prizes.worker.js | grep -qE "^[[:space:]]*[2-9]$" &amp;&amp; node --check src/queue/workers/step-process-prizes.worker.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "phase: 'PRIZES'" backend/src/queue/workers/step-process-prizes.worker.js` returns at least 2 (one parallel to each STEP_CALCULATE_STATS site).
    - `grep -c "df-prizes-" backend/src/queue/workers/step-process-prizes.worker.js` returns at least 2.
    - `grep -c "Phase 11 (D-11)" backend/src/queue/workers/step-process-prizes.worker.js` returns at least 2.
    - The byte-offset of each `df-prizes-` send is greater than the byte-offset of its preceding `stats-${drawId}` send (verify by reading the file: the DrawFinancial send follows immediately after the DrawStats send in BOTH call sites).
    - `node --check backend/src/queue/workers/step-process-prizes.worker.js` exits 0.
    - No new import statements added at the top of the file (`diff` of the imports block versus pre-edit shows no additions — confirm visually).
  </acceptance_criteria>
  <done>
    Both step-process-prizes call sites parallel-trigger phase-PRIZES, file parses cleanly, no new imports needed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test — close a seeded draw, verify DrawFinancial row appears with correct totalSales; process prizes, verify totalPrize + totalizedAt</name>
  <files>backend/src/queue/workers/__tests__/draw-financial-pipeline.integration.test.js</files>
  <read_first>
    - backend/src/queue/workers/close-and-ingest.worker.js (the modified pipeline trigger point — to know what function to invoke for the test)
    - backend/src/queue/workers/step-process-prizes.worker.js (the modified PRIZES trigger point)
    - backend/src/queue/workers/calculate-draw-financials.worker.js (from Plan 11-02 — the worker invoked by the queue; the integration test can invoke it directly with a synthetic job rather than waiting on pg-boss polling, which keeps the test fast)
    - backend/src/services/draw-financial.service.js (from Plan 11-02)
    - .planning/phases/11-drawfinancial-foundation/11-CONTEXT.md (must_haves and goal-backward criteria)
    - Any existing integration tests in backend/src/queue/workers/__tests__/ to mirror the setup/teardown pattern
  </read_first>
  <behavior>
    - Test 1 (phase SALES): Seed a Game, a Draw (status SCHEDULED), a Ticket with apiSystemId = NULL, two TicketDetails for that draw with amounts 100.00 and 50.50, and another Ticket with status = 'CANCELLED' carrying a TicketDetail of amount 999.99. Invoke `calculateDrawFinancialsWorker({ data: { drawId, phase: 'SALES' } })` directly. Assert: a single DrawFinancial row exists for drawId; totalSales = 150.50 (the cancelled ticket excluded, D-17); ticketCount = 1 (distinct ticketIds from non-cancelled TicketDetails); totalizedAt IS NULL (still in SALES window).
    - Test 2 (phase PRIZES throws when prizesProcessed = false): Same seeded draw with prizesProcessed = false. Invoke worker with phase: 'PRIZES'. Assert: throws `PrizesNotProcessedError`. Assert: DrawFinancial.totalizedAt is still NULL (no zero-prize write per FIN-AGG-07).
    - Test 3 (phase PRIZES success): Update the seeded draw to `prizesProcessed = true`, `drawnAt = new Date('2026-05-15T12:00:00Z')`. Add prizes to the TicketDetails (e.g., prize = 50 on one detail). Invoke worker with phase: 'PRIZES'. Assert: same DrawFinancial row updated; totalPrize matches the SUM of TicketDetail.prize (non-cancelled); utility = totalSales - totalPrize; totalizedAt = the drawnAt value (NOT now()).
    - Test 4 (idempotency end-to-end): Re-run worker with phase: 'SALES', then phase: 'PRIZES'. Assert: still exactly 1 DrawFinancial row for drawId; values unchanged; `prisma.drawFinancialProvider.count({ where: { drawId } })` returns 1 (the NULL apiSystemId row, not duplicated).
    - Test 5 (per-provider breakdown with both NULL and a real ApiSystem): Add a second Ticket with apiSystemId = (id of a seeded ApiSystem 'srq'). Re-run phase SALES. Assert: DrawFinancialProvider has 2 rows for drawId — one with apiSystemId = NULL, one with apiSystemId = the srq id. Sum of their totalSales equals DrawFinancial.totalSales.
    - Test 6 (close-and-ingest trigger integration): IF a pg-boss test harness is available in the project, use it to actually enqueue phase-SALES via boss.send and assert it gets processed. Otherwise, directly invoke the worker after observing that close-and-ingest now calls `boss.send` (verified statically by grep in Task 1). Test description should make this fallback explicit.
  </behavior>
  <action>
    Create `backend/src/queue/workers/__tests__/draw-financial-pipeline.integration.test.js`. Mirror the existing integration test patterns in the project (read any neighboring __tests__ directory to find the conventions — beforeAll, afterEach with cleanup by Game prefix, prisma teardown, etc.).

    Use a unique Game name prefix per test run (e.g., `__test-df-${Date.now()}`) so concurrent test runs don't collide.

    For Tests 1-5, invoke the worker function directly with a synthetic `{ data: { drawId, phase } }` job payload. This is faster than pg-boss-driven invocation and is the same pattern other worker tests in the repo use. For Test 6, either use a pg-boss test client OR explicitly note in a code comment that the static grep check in the Task 1 acceptance criteria substitutes for runtime verification (this is acceptable per D-10 — the trigger is a single line of code whose correctness is provable by grep + reviewing the action's structure).

    Reuse helper assertions from Plan 11-02's service tests if they were exported; otherwise inline the assertions. Decimal comparisons should use `.toString()` or convert to Number with tolerance to avoid Prisma Decimal vs JS Number equality issues.

    Cleanup: in afterEach, delete all created TicketDetail, Ticket, DrawFinancialProvider, DrawFinancial, Draw, Game rows for the unique test prefix. Order matters due to FK constraints — delete TicketDetail before Ticket, DrawFinancialProvider before DrawFinancial, Draw last.

    NO production code is modified in this task — only the test file is created.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm test -- --testPathPattern=draw-financial-pipeline.integration 2&gt;&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm test -- --testPathPattern=draw-financial-pipeline.integration` exits 0.
    - The test output lists at least 5 passing tests (Test 6 may be a skipped/static-only test if pg-boss harness is not set up).
    - `grep -c "DrawFinancial" backend/src/queue/workers/__tests__/draw-financial-pipeline.integration.test.js` returns at least 5 (model name appears in assertions throughout).
    - `grep -c "PrizesNotProcessedError" backend/src/queue/workers/__tests__/draw-financial-pipeline.integration.test.js` returns at least 1 (Test 2 assertion).
    - `grep "totalizedAt" backend/src/queue/workers/__tests__/draw-financial-pipeline.integration.test.js` returns at least 2 matches (NULL check + assigned-value check).
    - After the test run, `docker exec tote_postgres psql -U tote_user -d tote_db -c "SELECT COUNT(*) FROM \"DrawFinancial\" WHERE \"drawId\" IN (SELECT id FROM \"Draw\" WHERE \"name\" LIKE '__test-df-%')"` returns 0 (cleanup worked).
  </acceptance_criteria>
  <done>
    Integration test passes against the live local DB; all goal-backward truths for pipeline correctness are proven; cleanup leaves zero test residue.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| close-and-ingest worker -> calculate-draw-financials queue | Internal trusted call; drawId from the same worker's loaded draw object. |
| step-process-prizes worker -> calculate-draw-financials queue | Internal trusted call; drawId from same worker context. |
| Integration test -> live Postgres | Test runs against tote_db (port 5433) — must clean up to avoid polluting dev data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Tampering | Race condition where SALES trigger fires concurrently with itself (e.g., manual re-close) | mitigate | `singletonKey: df-sales-${drawId}` deduplicates simultaneous enqueues; worker upsert is idempotent. |
| T-11-11 | Denial of service | A failure in the phase-SALES boss.send blocks the close pipeline | mitigate | Wrapped in try/catch per D-10; warning logged but close returns successfully. Task 1 acceptance criteria includes grep for `catch` in each insertion. |
| T-11-12 | Denial of service | A failure in the phase-PRIZES boss.send causes step-process-prizes to retry | accept | Same risk profile as the existing STEP_CALCULATE_STATS send; retry policy is bounded by pg-boss retryLimit. Documented in plan as intentional symmetry. |
| T-11-13 | Information disclosure | Integration test leaks credentials in logs | accept | Test uses the local Docker DB password from docker-compose; no production secrets in the test file. |
| T-11-14 | Tampering | Test pollutes dev DB if cleanup fails | mitigate | afterEach delete-by-prefix; final acceptance criterion verifies COUNT = 0 after run. Test names use `__test-df-` prefix to make pollution easy to spot. |
</threat_model>

<verification>
1. Static checks: grep counts in Task 1 and Task 2 confirm trigger insertions at every required call site.
2. node --check passes on both modified worker files.
3. `cd backend && npm test -- --testPathPattern=draw-financial-pipeline.integration` exits 0.
4. Manual smoke check (optional): start backend dev, manually invoke close-and-ingest for a test draw, watch logs for `[calculate-draw-financials] phase=SALES drawId=...`, then check `SELECT * FROM "DrawFinancial" WHERE "drawId" = '...'`.
5. No regression in existing tests: `cd backend && npm test` overall exits 0.
</verification>

<success_criteria>
- FIN-AGG-01 and FIN-AGG-02 are satisfied end-to-end through the live pipeline (proven by integration test).
- All three close return paths in close-and-ingest fire phase-SALES inside try/catch.
- Both STEP_CALCULATE_STATS call sites in step-process-prizes have a parallel phase-PRIZES send.
- The integration test cleans up after itself.
- The overall backend test suite still passes (`npm test` exit 0).
</success_criteria>

<output>
After completion, create `.planning/phases/11-drawfinancial-foundation/11-03-SUMMARY.md` documenting:
- The 3 exact line numbers (or function names) where phase-SALES was inserted in close-and-ingest.worker.js (for code-review trail).
- The 2 exact line numbers (or function names) where phase-PRIZES was inserted in step-process-prizes.worker.js.
- The 5+ integration tests and what each proved.
- Any deviation from the patterns in 11-PATTERNS.md sections 7 and 8, with rationale.
- A green-light statement: "Pipeline is proven correct on N live draws. Plan 11-04 is now safe to run."
</output>
